# Design pass review for Liam (2026-09-14, overnight)

Every dashboard surface was designed in Claude Design from the requirement documents under docs/superpowers/design/requirements, by an opus designer, then screenshot-critiqued at 1440 and 375 in light and dark by a sonnet critic, then revised once where the critic said FIX or REDO. Nothing is ported. Tick a module once you have looked at it; mark it SHIP (port as is), FIX (list what to change) or REDO.

Project: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66 (open a file with ?file=<name>; the app shell is "Tahi App Shell.html", audience and device through the Tweaks pill). Preview pages per module are listed below; open them the same way.

## Summary

| | Module | Pages | First verdict | Final verdict |
|---|---|---|---|---|
| [ ] | auth | 28 | FIX | SHIP |
| [ ] | calculator-analytics | 24 | not critiqued | not critiqued |
| [ ] | clients | 40 | FIX | SHIP |
| [ ] | content-marketing | 59 | FIX | SHIP |
| [ ] | finance | 18 | FIX | SHIP |
| [ ] | messages | 18 | FIX | SHIP |
| [ ] | onboarding | 34 | REDO | SHIP |
| [ ] | ops | 51 | FIX | SHIP |
| [ ] | overview | 11 | FIX | SHIP |
| [ ] | portal-account | 32 | FIX | FIX |
| [ ] | portal-files | 20 | FIX | FIX |
| [ ] | portal-home | 21 | FIX | SHIP |
| [ ] | portal-money | 39 | FIX | FIX |
| [ ] | reports | 11 | FIX | SHIP |
| [ ] | requests | 33 | FIX | FIX |
| [ ] | sales-artifacts | 76 | FIX | SHIP |
| [ ] | sales-pipeline | 51 | FIX | SHIP |
| [ ] | settings | 70 | FIX | SHIP |
| [ ] | tasks | 26 | FIX | FIX |

## auth

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fauth-preview.html
- Files: auth.css (29 KB), auth-app.jsx (19 KB), auth-kit.jsx (14 KB), auth-data.jsx (5 KB), previews/auth-preview.html (3 KB), Tahi Auth.html (2 KB)
- Pages: sign-in, sign-in-loading, sign-in-field-error, sign-in-page-error, sign-in-submitting, sign-in-oauth, sign-in-redirect, sign-in-invite, sign-up, sign-up-field-error, sign-up-page-error, sign-up-submitting, sign-up-invite, verify, verify-submitting, verify-error, verify-resend, forgot, forgot-error, forgot-submitting, reset, reset-error, reset-submitting, continue-loading, continue-stranded, continue-pick, continue-activating, continue-failed
- Covered: REVISION PASS. Only auth.css changed this round (29769 bytes, written with if_match on etag 1789302353781581); auth-app.jsx, auth-kit.jsx, auth-data.jsx, previews/auth-preview.html and Tahi Auth.html are byte-identical to the reviewed pass and are listed for completeness only. No REDO pages were issued, so no page was redrawn. FIX 1, the 42px "Forgot your password?" link (5 pages: sign-in, sign-in-field-error, sign-in-page-error, sign-in-submitting, sign-in-invite). Root cause: .cl-formFieldAction inherited a "normal" line box from the font shorthand, so 0.75rem of padding landed on an 18px co (more in the journal)
- Left out: The 37px height of the Terms and Privacy Policy links themselves is left as-is, and this stays a disclosed exception rather than a fix. They are inline words inside a running sentence, not standalone actions: taking them to 44px means a 44px line box inside a 12px legal sentence, which forces the sentence's two lines apart and breaks acceptance criterion 10 (the legal line must not push into the form fields above). The current 0.625rem of vertical padding is the largest value that widens the tap (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - Should the primary CTA (cl-formButtonPrimary) take the leaf radius like the card does, or is the current 0.5rem rounded rect right for keeping Clerk widget buttons visually distinct from Tahi native buttons? Held at 0.5rem for now.
  - Is the "You are accepting an invite to Giant Group" trust cue worth building when a tahi-invite-token cookie is present, or is the current silent handoff fine? It is a proposal, not in the backlog, and both invite pages carry a "Design file only" ribbon until you say.
  - Terms and Privacy Policy in the legal sentence measure 37px tall, under the 44px rule. They are inline prose, not standalone actions, and padding them to 44 pushes the sentence's lines apart. Confirm the exception, or accept the alternative of promoting them to their own 44px row under the sentence.
  - Is the single 1024px breakpoint acceptable long term for this group, or do you want an explicit 768px tablet treatment distinct from the 375px phone layout?
  - The trust avatar stack still uses placeholder stock photos, flagged in lib/auth-shell-config.ts as "swap for real client faces or logos when available." Do you want real client faces, client logos, or the line without avatars?
  - /continue got its first design this pass (loading, stranded, picker, activating, failed). Does it ship as designed, or wait until a dual membership person actually reports friction?

## calculator-analytics

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fcalculator-analytics-preview.html
- Files: C:\Users\Work\Projects\tahi-dashboard (Claude Design project 57bf60cf-5e6d-450f-9e2f-e25c8d12fd66) calculator.jsx (38 KB), calculator-kit.jsx (24 KB), calculator-data.jsx (12 KB), calculator.css (33 KB), calculator-ct19.jsx (8 KB), calculator-ct19.css (2 KB), previews/calculator-analytics-preview.html (10 KB)
- Pages: calc, calc-computing, calc-oneoff, calc-deal, calc-org, calc-capacity, calc-error, calc-draft, calc-denied, calc-blocked, analytics, analytics-ct19, analytics-loading, analytics-empty, analytics-error, analytics-denied, affiliates, affiliates-connect, affiliates-loading, affiliates-rows, affiliates-filtered, affiliates-error, affiliates-denied, affiliates-codes
- Covered: All three routes from requirement section 2, every state from section 3, drawn honestly per section 5. /calculator (10 keys). The single two column surface: left form (Project shape, Scope by discipline with per line hours plus an Ourselves/Contractor toggle plus a contractor rate that only appears when contracted, Timeline, Retainer which disappears on a One-off and says so in its place, Client and complexity with the four multipliers and the returning client discount, Notes) and the right sticky output rail (Quote target hero with the floor/stretch range drawn under it, Recommendation, Draft (more in the journal)
- Left out: 1. The CT.19 enlarged scope is NOT the default. Open question 1 asked small scope or CT.19 scope; the brief says design the option that keeps today's live behaviour, so ?page=analytics is the page exactly as it stands. Because the requirement itself says the pass should plan for the richer version, CT.19 is drawn as a separate key (?page=analytics-ct19) in two files that add nothing to the default page: four sections (source breakdown, close rate by source, stage velocity, sales cycle length) ab (more in the journal)
- Questions for you:
  - Sales analytics scope. Two pictures are in the preview: ?page=analytics is the page as it stands (funnel, donut, monthly bar, one band), ?page=analytics-ct19 is the same page plus the four sections CT.19 would move in from /reports. Picking the small one now risks a second design pass when CT.19 lands; picking the large one commits to deleting those six sections from /reports before that has shipped. Which one ships?
  - Calculator entry point. The dealId and orgId plumbing works but nothing in the app links to it, so the anchored states in the preview are unreachable in practice. Add a 'Calculate for this deal' action on /deals/[id] and a 'Calculate for this client' action on the client detail page, or leave the calculator a standalone sidebar tool?
  - Is Rewardful still the intended affiliate platform? It is not in CLAUDE.md's integration table, the GET is a permanent stub, the sync POST never calls out, and the only affiliate data the studio actually holds is leads.affiliateCode, which the reactivation cron already groups. ?page=affiliates-codes designs that second shape as a proposal. If the answer is leads.affiliateCode, most of the Rewardful-shaped table goes.
  - Affiliates honesty in the interim. The design marks the page 'Connected, and not syncing' and keeps the totals blank rather than showing zeroes, on the reasoning that a zero is a claim. Confirm that framing is what you want a team member to see, rather than hiding the page from the nav until N6 lands.
  - Calculator recompute affordance. The code comment says it recomputes on blur; it actually debounces 400ms on every keystroke. The design assumes the debounce is the truth and shows a save state that moves on every change, plus a 'Recomputing, these are the last figures' line on the rail. Confirm the debounce stays, because a blur trigger would want a quieter header.
  - Not a design question, but it came out of this pass: /api/admin/deals is never run through lib/access-scoping.ts, so a team member scoped to specific clients who holds the deals and sales_analytics grants sees studio-wide pipeline value. Same class of leak the catalogue calls out for /reports. Worth a backlog line before anyone but you holds the sales_analytics grant.

## clients

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fclients-preview.html
- Files: C:\Users\Work\Projects\tahi-dashboard (Claude Design project 57bf60cf-5e6d-450f-9e2f-e25c8d12fd66) clients.jsx (58 KB), clients-kit.jsx (58 KB), clients-detail.jsx (66 KB)
- Pages: list, list-cards, list-loading, list-error, list-stale, list-empty, list-empty-filters, list-empty-scope, list-page-end, list-new, list-archived, detail, detail-requests, detail-invoices, detail-files, detail-people, detail-contact, detail-papers, detail-calls, detail-calls-book, detail-money, detail-money-profit, detail-money-time, detail-money-deals, detail-settings, detail-danger, detail-merge, detail-delete, detail-project, detail-paused, detail-loading, brand, brand-fallback, brand-empty, brand-loading, brand-missing, contact, contact-empty, contact-loading, contact-missing
- Covered: REVISION PASS, three FIX verdicts, all three resolved and verified by render. FIX 1, list-page-end (clients.jsx). The preset's page:3 was being thrown away on mount because useEffect(()=>setPageNo(1),[q,filters,savedView]) fires on the first commit. Guarded with a `mounted` ref so the reset only runs after the first paint. The state now actually reaches the DOM: at 1440 light the page renders "Nothing on this page / This page is past the end of the list. The clients are on the earlier pages." with a real "Back to page 1" action. Section 3 also specifies that CTA label, which the old code did n (more in the journal)
- Left out: previews/clients-preview.html was not rewritten. It already loads the DS bundle, app-shell.css, head-band.css, the requests kit and every clients file through the same script tags the shell uses, mounts inside window.TahiShell.App, and honours ?page=, ?theme=dark, ?device=phone, ?audience= and ?seat= by driving the shell's own Tweaks control. Nothing in this revision changed its wiring, so touching it would only have risked the harness the critic renders. SHIP pages were left alone. The critic r (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - The Files tab now shows only what the repo backs: one flat list of files that arrived on requests, served from R2, with a search box. The previous design proposed a small drive per client, four studio folders (Brand assets, Deliverables, From the client, Contracts and invoices), an upload that lands on the client rather than on a request, and a comment thread per file. Do you want any of that built, and if so in what order? My suggestion if yes: folders first (a cheap column on the files table), then upload-from-here, and the per-file thread last since the request thread already carries the conversation.
  - Should the Files tab be able to delete a file? I removed the Delete action from the file panel because the requirement doc does not list it and I could not confirm the route exists. If it does, say the word and it goes back on the panel foot behind a confirm.
  - MC.10b: is Physitrack's two-Stripe-customer-id conflict ready for your judgement now, or does it stay parked until the current sprint batches land? It affects the hero MRR and the Money tab for that one client, and the design currently just shows the figure without a caveat.
  - Should /clients/brands/[id] and /clients/contacts/[id] go through this design pass before porting, or is a direct port onto existing v3 primitives fine for two leaf pages that carry no novel interaction? They now have a design (clients-people.jsx) either way, so this is really about whether the port waits on your review of it. (A: design first / B: port directly)
  - The live build dropped the design file's per-client Health rules editor and its Portal-visibility template picker in favour of studio-wide defaults plus the /permissions builder. Do you want either rebuilt as a genuine per-client override, which needs new schema, or does the current answer stand for good? (yes/no on each)
  - Should /clients/[id]/page.tsx gain the same explicit requirePageFeature('clients') server-side call that /clients, /clients/brands/[id] and /clients/contacts/[id] already have, purely for consistency, even though the API layer enforces the same boundary today? (yes/no)
  - Is the nine-tab Money-as-a-container composition (Revenue, Profitability, Time and Deals folded under one Money tab behind a segmented control) the shape you want kept, or should any of those four move back out to their own top-level tabs? (A: keep Money as a container / B: split one or more back out, name which)
  - A viewer denied clients.billing_card who follows a stale ?tab=invoices or ?tab=money link is silently bounced to Overview with no explanation. The design keeps that silence deliberately, because a toast would tell them a money tab exists. Confirm silence is what you want, or should they get a neutral line like "That section is not on your seat"?

## content-marketing

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fcontent-marketing-preview.html
- Files: content-kit.jsx (22 KB), content-pages.jsx (53 KB), content.jsx (75 KB), content-data.jsx (45 KB), content.css (53 KB), content-fixes.css (2 KB), previews/content-marketing-preview.html (15 KB)
- Pages: health, health-loading, health-empty, health-error, ideas, ideas-loading, ideas-no-clusters, ideas-no-matches, idea-panel, idea-new, drafts, drafts-loading, drafts-empty, draft-panel, draft-publish, links, links-empty, schedule, schedule-empty, schedule-panel, site-index, site-index-empty, audits, audits-empty, backfill, backfill-empty, round-table, round-loading, round-brief-gate, round-failed, round-cost-capped, sitemap, sitemap-loading, sitemap-empty, sitemap-none, sitemap-review, sitemap-boardroom, sitemap-confirm, sitemap-staci, social, social-loading, social-unconfigured, social-disconnected, social-empty, social-error, reviews, reviews-loading, reviews-expanded, reviews-draft, reviews-no-matches, reviews-error, announcements, announcements-loading, announcements-empty, announcements-no-matches, announcement-view, announcement-new, announcement-new-clients, announcement-new-error
- Covered: Revision pass only. Both FIX items from the critic are resolved, plus the sweep the critic asked for. 1. Blank empty-state icon tiles (ideas-no-clusters, backfill-empty, reviews-no-matches, announcements-empty, and the unchecked siblings drafts-empty, schedule-empty, site-index-empty, audits-empty, sitemap-empty, social empties). Root cause was exactly as diagnosed: content-kit.jsx borrowed InvKit's Empty unwrapped, so icon names were resolved against InvKit's glyph set and this group's own names (layers, megaphone, star, flask, target, robot, scales, thumb, tree, pen, globe, chart) fell throu (more in the journal)
- Left out: SHIP and unflagged pages were left alone except where the shared kit forced a change. The Empty/Btn/IconBtn wrap in content-kit.jsx is a shared-file change and does reach every page in the group, but only by adding glyphs that were being requested and silently dropped; no markup, class or layout changed, so the pages the critic passed keep the geometry they were measured at. Still deliberately not drawn, and why: - No Send button on /reviews anywhere, per the module note and requirements section (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - Reviews Send: build it for real against the same Resend fan out /announcements already uses, or keep it absent forever and leave Copy link as the only outreach action? The design currently assumes the second and says so on the page.
  - Announcements Specific clients: the design ships the client picker now (option A) rather than hiding the option. Confirm that is the direction before it is ported, because the alternative is a one line change to the Audience select.
  - Sitemap: does it ever leave the two person email allowlist and become a normal permission gated page, or stay a Liam and Staci planning tool forever? The design assumes it stays allowlisted and keeps Staci's AI machinery absent rather than disabled.
  - Backfill: the design keeps the standalone tab and drops the embedded card under Health. Confirm which one survives.
  - Social: stay a read only Buffer mirror, or gain compose and schedule? The design assumes read only.
  - Approved Case Studies (Decision #018): scope it into this design pass alongside /reviews, or defer it until CT.15 lands and the Send and Copy link lies are actually deleted in code?
  - Sitemap Add CMS behaviour: the button adds a CMS collection node at the foot of the tree, the same kind the tree already renders. If a collection should instead be added as a child of the currently selected node, or should open a field template straight away, say so and the toast and flow change with it.

## finance

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Ffinance-preview.html
- Files: finance-fixes.css (3 KB), previews/finance-preview.html (11 KB)
- Pages: billing, billing-loading, billing-empty, billing-error, billing-scoped, reports, reports-loading, reports-error, reports-denied, reports-add-charge, reports-detect, invoices, invoices-empty, invoices-error, invoices-scoped, invoice-detail, invoice-chase, invoice-new
- Covered: REVISION PASS. Every FIX item from the critic is resolved and every unverified page key is now captured and measured. No REDO verdicts existed for this module. FIX 1, the 42px search input (billing, billing-loading, billing-empty, billing-scoped). Root cause found by DOM measurement, not guessed: InvKit's Search renders a .inv-search box at min-height 2.75rem with a 1px border on all four sides and a bare input stretched inside it, so the INPUT measures 2.625rem (42px) while its wrapper measures 44px. Fixed by growing the box to 2.875rem and stating min-height 2.75rem on the input, inside the  (more in the journal)
- Left out: /time is not drawn: the ops module owns it, per the module note. That means requirement section 3's /time states, IC.9's four sub-44px controls and open question 3 (on-page timer card versus the existing top nav TimerChip) are all deliberately out of this module and belong with the ops designer. The client portal halves of /invoices and /billing are out of scope by the requirement doc's own section 7 (client-invoices.md and client-billing-services.md own them). The studio branch here never rende (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - /billing structure (requirement open question 1). This pass keeps the current KPI-strip-plus-two-tables information and re-skins it onto the house pattern: a four cell headline band with Recurring revenue as the lead tile carrying a meter, the left Views and Filters rail, then the interval summary and the two tables as cards. That is option A, kept because it is what /requests and /clients do. Confirm A, or say the word and it becomes a structural rethink.
  - T708, the two Outstanding figures (requirement open question 5). This pass designs option B, keep them distinct and label them precisely: the honesty line under the band reads "Outstanding is read from the ten invoices listed below, which is what this page has loaded, while Financial reports totals the whole ledger, so the two figures can differ. Reconciling them is open as T708." If they should instead read from one computed source (option A), the line comes out and the band tile changes meaning.
  - /financial-reports loading and error (requirement open question 2). This pass designs the fuller treatment: a jump nav skeleton plus section skeletons that occupy the same boxes as the ready page so nothing jumps when data lands, and an error state with a retry rather than a one line subtitle swap. Confirm that is worth the weight on a page that can never legitimately render empty.
  - The studio invoice list on a phone (requirement open question 4). This pass answers yes: under 48rem the table stacks into labelled row cards rather than scrolling horizontally, matching the client branch. It now also grows the row title to a 44px target, which makes each row about 6px taller at 375px. Worth a look at the invoices list on a real phone before the port to confirm the rows do not read loose.
  - finance-fixes.css is a port instruction, not a permanent file. Its five rules all belong in invoices-studio.css inside @container inv (max-width: 48rem). Confirm that whoever ports MR.6 folds them in and deletes the file, rather than carrying a second stylesheet into the app.
  - The chase drafter is reachable in the preview as its own page key (invoice-chase) because the card is idle until pressed. That is the honest live behaviour. If the review wants the drafted state to be what a reviewer sees first on the detail page, that is a product decision (auto draft on open for an overdue invoice) with a real token cost per page view, not a preview change.

## messages

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fmessages-preview.html
- Files: messages.jsx (29 KB), messages-kit.jsx (16 KB), messages-data.jsx (13 KB), messages.css (29 KB), previews/messages-preview.html (5 KB)
- Pages: client-inbox, client-thread, client-loading, client-error, client-empty, client-filtered, client-idle, client-thread-loading, client-thread-error, client-thread-empty, client-composer, client-readonly, client-denied, studio-inbox, studio-internal, studio-quiet, studio-empty, studio-new
- Covered: One surface, both audiences, exported as window.TahiMessages.Messages and mounted through the shell's messagesContent prop (app-mount.jsx and "Tahi App Shell.html" untouched). COMPOSITION. Page header (title, sub, studio-only New conversation) + the shared headline band from head-band.css (.hbandw/.hband/.hband-c, four read-only tiles, lead first, counts computed off the same rows the list draws so band and list can never disagree) + one bordered box holding two panes separated by the 1px grid gap. The left pane is the rail in the house idiom: the canonical search field, a Views section built  (more in the journal)
- Left out: Deliberately not drawn, because section 4 lists them as missing or planned and section 7 is unanswered: - A client-facing New message button. Question 7.1 is unanswered, so the design keeps today's behaviour: the standing line plus one thread per request are the client's whole compose surface. The studio side does get New conversation because /api/admin/conversations exists today. - The abandoned mock's offline banner. No offline state exists on this route. - Message edit and delete, edited or r (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - Question 7.1: I drew option B, no client-facing New message. The studio keeps a New conversation dialog because POST /api/admin/conversations exists. Confirm the client never composes a new room, or say the word and I will design the client entry point with a write path beside it.
  - Question 7.3: I kept today's 1024px behaviour, drawn as a stack point at 800px of page width (the content well beside an expanded sidebar at a 1024px window). A 768px window is therefore stacked. Confirm, or say lower it so a tablet gets two panes.
  - Question 7.4: attachments open the file, they do not open a preview drawer. Confirm Messages should deep link into the eventual Files drawer rather than grow its own.
  - Question 7.5: I used the shared 'No conversations yet' copy for the client, with a client-specific body line. Confirm no separate first-run wording is needed.
  - Question 7.2 is untouched by this design and still needs your call: when Messages is allowed for a client org, does the mobile tab bar swap a tab out to keep four primary tabs, or is three plus More acceptable for that org?
  - Studio direct and group rooms are designed as first-class rows, but lib/messages-inbox.ts projects only channel and request sources today. Porting the studio rail as drawn needs Batch E1 to project conversations of type direct and group into the same list shape. Is that in scope for the port, or should the first port ship channels and request threads only?
  - The band's 'Waiting on us' and 'Waiting on you' tiles are computed client-side from the inbox rows (who wrote last, and request status client_review). Confirm that is honest enough, or the inbox payload should carry the last message's side explicitly.
  - The app shell's client nav still lists Messages for every client session. Per the 2026-09-13 default-deny it should only appear for an allowed org. That is a shell change, outside my files, but it makes the denied state look designed-around rather than real.
  - The studio 'Not in their portal' chip and the thread banner read feature_visibility per org. Confirm you want Messages to surface that, and that Open permissions should land on Team and access.
  - The voice note player is drawn as a real control, but playback is still on the QA broken list from 2026-05-21. If that bug is still open at port time, the player should not ship looking ready.
  - Minor, and your call at port time: the rail does not scroll a selected room into view when a room is opened from outside the visible part of the list.

## onboarding

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fonboarding-preview.html
- Files: onboarding-app.jsx (40 KB), onboarding-states.css (14 KB)
- Pages: chooser, enquiry, enquiry-sent, welcome-selfserve, welcome-invited, welcome-masked, welcome-existing, welcome-existing-retainer, welcome-error, plan, pay, pay-processing, pay-declined, pay-invoice-refused, details, details-invited, invite, invite-added, kickoff, kickoff-loading, kickoff-empty, kickoff-unavailable, kickoff-readonly, kickoff-error, kickoff-booked, continue, cut-brief, cut-payoff, cut-video, team-welcome, team-profile, team-seat, team-error, team-cut-payoff
- Covered: REVISION PASS. Only the four pay pages changed; every other page in the set is byte-identical in behaviour and was re-rendered to confirm no regression. REDO 1 (pay, pay-declined, pay-invoice-refused): the invented card form is gone. StripeBlock no longer renders any app-styled input, no VISA badge, no pre-filled 4242 test card, no expiry or CVC, no cardholder field, and no Card/Bank/Apple Pay tabs. In its place the design draws only what Tahi actually owns around the element (the Payment details header with the Stripe lockup, the currency choice, the amount summary, the trust line, the invoic (more in the journal)
- Left out: 1. onboarding.css was deliberately not rewritten. It still carries the superseded mock-card rules (.ob-pe input, .ob-pe-field, .ob-pe-tab, .ob-cardno, .card-icon, .ob-mobile .ob-pe-tab). No page references them any more, and they are now documented as dead in the onboarding-states.css payment comment. Rewriting a 42 KB shared stylesheet that every SHIP page in the group depends on, to delete eight unused selectors, was a worse trade than leaving them inert; the brief says to leave SHIP pages unt (more in the journal)
- Critic: SHIP (first pass REDO)
- Questions for you:
  - Open question 1 (unchanged, still needs your call): should the onboarding card ever follow the dashboard dark-mode preference, or is a permanently light card the final intent? Today the canvas darkens and the card stays cream, so a reviewer will keep asking.
  - The pay step now reserves an honest, visibly Stripe-owned region rather than drawing card fields. Do you want the port to keep that framing copy visible in production as a short caption above the element, or is the element alone enough once it is really mounted?
  - During Stripe confirmation the currency control is disabled with 'Locked while Stripe confirms'. Confirm that matches the real behaviour: the checkout session's presentment currency is fixed at creation, so re-picking mid-confirm should not be possible.
  - Open question 2: is the design file's 'Your brief' step (brand asset links plus a one-line what-are-we-building) wanted in the shipped project path, or should it be cut from the file and the META table cleaned up?
  - Open question 3: is the cream post-onboarding first-run checklist still the intended payoff inside /onboarding, or is routing straight to /overview with the client-home checklist (LW.9) the permanent plan?
  - Open question 4: for /welcome, build resolveToken against a real teammate invite record, or strip the hard-coded role, start date, gear and buddy until T1.17 lands?
  - Should the superseded mock-card CSS rules be deleted from onboarding.css in a separate, low-risk cleanup pass, so the port never inherits them?

## ops

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fops-preview.html
- Files: permissions.css (8 KB)
- Pages: time, time-entries, time-rates, time-day, time-log, time-edit, time-xero, time-delete, time-nomatch, time-loading, time-empty, time-error, team, team-chart, team-access, team-edit, team-invite, team-remove, team-nomatch, team-loading, team-empty, team-error, capacity, capacity-answer, capacity-person, capacity-loading, capacity-empty, capacity-error, tracks, tracks-client, tracks-attention, tracks-today, tracks-loading, tracks-empty, tracks-error, portal-tracks, portal-tracks-loading, portal-tracks-empty, portal-tracks-error, permissions, permissions-clients, permissions-roles, permissions-features, permissions-copy, permissions-history, permissions-nomatch, permissions-empty, permissions-empty-clients, permissions-empty-history, permissions-loading, permissions-error
- Covered: Revision pass. Both critic findings were in one file, so permissions.css is the only file rewritten (5264 to 8597 bytes); every other module file is untouched and still under the 90 KB ceiling (ops.jsx 33695, ops-kit.jsx 35122, ops-data.jsx 22596, ops.css 68653, ops-fixes.css 17253, ops-team.jsx 39945, ops-tracks.jsx 25296, permissions.jsx 47758, previews/ops-preview.html 13564). FIX 1, raw hex for the danger tone (raised on permissions, permissions-error, and by implication permissions-clients). settings.css already ships --danger as a first class token and already flips it for dark (#C0392E  (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - /tracks: delete it (CT.18) or build the studio-wide cross-client track board the design carries? The client tenancy risk is already gone since the S1 redirect, so the decision is now purely whether the studio wants a cross-client capacity and queue board. Building it needs a new cross-client aggregation endpoint, not just the UI.
  - Is T1.15 (deny by default) actually done? lib/permissions.ts and lib/access-scoping.ts both read as already deny-by-default with the MCP service token and unseeded-install exceptions documented, but TASKS.md still has the checkbox open. If it is done, the permissions pane's "Nothing is granted by default" honesty strip is plain truth; if not, that strip is a promise the server does not keep yet.
  - Should /team's legacy Access rules slide-over (teamMemberAccess: role, scope, plan, track type) be retired in favour of /permissions owning all scope editing, or kept with the UI saying plainly which of the two models is authoritative? Today both are drawn, and a Liam edit in one place will not show up in the other.
  - Is capacity being unseeded in the permission catalogue (so every team_member is denied until an explicit allow) intentional and permanent, or should at least project_manager get a seeded resource row?
  - Does the permissions Team-members tab need the bespoke fresh-workspace empty state? The design assumes yes and ships three of them (no team members, no clients, nothing changed yet) plus the honesty strip; if a two-row list is considered fine, those states can be dropped at port time.
  - Should the client-home TrackBoard gain the "Waiting on you" and "Delivered, 30 days" lanes and the typical-turnaround stat that lib/track-lanes.ts and lib/track-stats.ts already compute, or does the simpler current-plus-queue card stay the permanent client-facing shape with the richer four-lane view reserved for wherever /tracks lands?
  - The T1.6 gap (a client feature deny is nav-cosmetic, not a route-level 403 on every portal route) is marked in the design with the "not enforced yet" annotation. Confirm that marker should stay visible to Liam in the ported pane until the route enforcement lands, rather than being dropped as noise.

## overview

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Foverview-preview.html
- Files: overview-kit.jsx (30 KB), overview.jsx (32 KB), overview-states.css (5 KB), previews/overview-preview.html (6 KB)
- Pages: owner, owner-loading, owner-empty, owner-error, owner-new, owner-brief, teammate, teammate-loading, teammate-empty, teammate-error, teammate-preview
- Covered: REVISION PASS, both FIX verdicts resolved. 1. owner-error FIX (Hero and Vitals rendered ready figures on a failed load). Hero now has a third state alongside loading and ready: on error it keeps its label, replaces the figure with a hatched mask (two blocks sized like the real number and its sub line, aria-labelled "figure unavailable because the source did not answer"), and prints an honest note plus a Try again button: "Could not load MRR. The overview figures did not answer. Last month's number is held back rather than shown as though it were today's." No MRR value, no delta, no spark line  (more in the journal)
- Left out: 1. The page-wide px to rem spacing conversion of overview.css. The critic marked this non-blocking on both owner-loading and owner-error and asked for it to be bundled. I did not do it: write_files can only replace a whole file, so converting the 585-line, 48 KB sheet would mean re-emitting every byte by hand, and a transcription slip in a stylesheet that four page states depend on is a worse outcome than the units themselves. The sheet's own header already states the convention it shipped with  (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - Requirement question 4, now answered in the design but worth confirming: the failed state is one shared treatment, a masked figure plus a named source and a Try again, applied to every card AND to the hero and vitals. Is the masked-figure treatment right for the money hero, or would you rather the hero show the last known MRR with a visible 'as of' stamp and a warning chip? The current design deliberately refuses to show an undated figure.
  - Requirement question 2, still open: should the Daily brief card and the nav-bar briefing popover be unified by expanding the home card (popover teases) or by folding the home card into the popover? A or B. The design keeps today's behaviour, both present, one payload.
  - Requirement question 3, still open: should the teammate Time tracking card's Pause and Switch task become real inline timer controls, or is routing to /time the intended behaviour? The card currently says out loud that both buttons open Time.
  - Requirement question 5, still open: while an admin previews a teammate, the design shows the RoNote banner naming the T1.19 limitation. Keep the banner, or hide preview entirely for this page until the me-scoped routes resolve the previewed member?
  - Wiring: overview-states.css is a new file, loaded by the preview. The final shell wiring step needs to add it next to overview.css, the same way ops-fixes.css and tasks-fixes.css are handled, or the masked error states will not paint inside the app shell.
  - Messages still shows in the shell sidebar and the mobile tab bar behind this page. Should the shell drop it now that Messages is hidden platform-wide, or is it intentionally parked there for a later unhide?

## portal-account

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fportal-account-preview.html
- Files: portal-account.jsx (55 KB), portal-account-rooms.css (7 KB)
- Pages: notifications, notifications-unread, notifications-past, notifications-kinds, notifications-filters, notifications-loading, notifications-error, notifications-empty, notifications-empty-past, notifications-empty-unread, notifications-empty-kinds, notifications-readonly, account-you, account-you-loading, account-you-readonly, account-signin, account-signin-email, account-signin-password, account-appearance, account-alerts, account-team, account-team-member, account-team-invite, account-team-invite-held, account-team-edit, account-team-edit-owner, account-team-remove, account-team-revoke, account-team-readonly, account-team-loading, account-team-empty, account-team-error
- Covered: REVISION PASS. Both measured defects are fixed and re-measured with Playwright (viewport-measured DOM boxes, not impressions). FIX 1, the 768 rail squeeze (hit notifications, notifications-unread, notifications-past, notifications-kinds, notifications-loading, notifications-readonly). The page now measures itself and folds the rail at 56rem, the exact width head-band.css folds the canonical .lrow at, so a tablet inherits the same rail/sheet breakpoint Requests and Tasks use, which is what the requirements doc assumed. Under that width the views become the segmented ViewTrack above the feed, th (more in the journal)
- Left out: The preview page itself was not rewritten: it already loads the DS bundle, app-shell.css, head-band.css and the module files through the shell's own script tags, mounts window.TahiShell.App, and honours ?page=, ?theme=dark, ?device=phone, ?audience= and ?label=1. The page keys are unchanged, so every critic URL from the first pass still resolves to the same page. head-band.css was deliberately left alone. The --bg-tertiary hole is a shared-sheet bug that makes the BandSkeleton invisible on every (more in the journal)
- Critic: FIX (first pass FIX)
- Still open after the revision:
  - notifications-kinds (FIX): 768 light: one capture in a full batch run (heavy parallel load) rendered this specific page key with the desktop rail still present (both Views and Kinds groups, .hasrail class), reproducing the exact pre-fix squeeze: a title wrapped to 6 lines in a narrow column. Two isolated re-loads of the same URL folded correctly (Filters button with a '2' count badge, no rail, matching the claim). Not reliably reproducible, so likely a first-paint race between mount and the ResizeObserver in useNarrow (state starts false/wide until the effect fires) rather than a deterministic bug, but it is a real risk: a slow device or a burst of concurrent renders could show a user the unfolded layout for a frame or more before it corrects. Recommend measuring synchronously (useLayoutEffect, or seed initial state from container width before first paint) so 'narrow' can never start wrong.
  - notifications-kinds (FIX): When it does fold correctly, 'Filters 2' shows the count badge exactly as claimed, and 'Clear filters' / 'Email preferences' land at the end of the feed.
  - notifications-loading (FIX): --bg-tertiary is correctly defined on the page root in both themes: computed style confirms rgba(26,25,20,.12) light and rgba(236,234,227,.12) dark, exactly matching the designer's claim, and the token no longer resolves to transparent.
  - notifications-loading (FIX): Contrast is marginal even when rendering correctly: measured pixels bg(255,255,255) -> bar(227,227,226) light, a contrast ratio of about 1.27:1 -- perceptible but faint, close to the edge of 'legible' the acceptance line calls for.
  - notifications-loading (FIX): 1440 light: in one full-batch screenshot capture, all four band tiles' skeleton bars painted with 0 measurable pixel difference from their tile backgrounds (sampled the full bar bounding box from getBoundingClientRect(), found zero non-background pixels) -- i.e. completely invisible, reproducing the original bug. Two isolated re-captures of the same URL/state rendered the bars correctly at the expected low-but-present contrast. This points to a color-mix()-driven custom property racing with first paint under load, not a deterministic failure, but it is the same failure mode the fix set out to close, so a wider safety margin (higher alpha, e.g. 20-24%) is worth doing rather than relying on a value this close to the visibility threshold.
  - notifications-error (FIX): Error state reuses BandSkeleton(n:4) for the band, same as loading, and inherits the same finding: correct token/colour in computed style (verified both themes), same marginal ~1.27:1 contrast, and the same batch capture showed all four tiles with zero pixel difference from background at 1440 light (see notifications-loading for the paired evidence -- both pages failed in the same run, which argues for a shared root cause rather than two coincidences).
  - notifications-error (FIX): The error card itself (icon, message, 'Try again') is correct, on-brand, and the rail counts are genuinely absent rather than stale, matching section 8's acceptance line.
- Questions for you:
  - The fold now fires under 56rem of page width, which means a 1024 tablet also gets the segmented track and the Filters sheet rather than the rail, exactly as Requests and Tasks do at that width. If you would rather Notifications hold its rail longer than the table pages do, the single number to move is RAIL_MIN in portal-account.jsx.
  - head-band.css paints every BandSkeleton bar with var(--bg-tertiary), which nothing in the project defines, so the loading band is four empty boxes on every page that uses it, not only this one. I patched it inside this module only. Do you want the token defined once in app-shell.css instead, and this module's local definition removed at port time?
  - Still open from the requirements doc, question 1: is the five-room segmented Account page still the target, or has the flat SettingsShell sub-nav that actually shipped won as the real information architecture?
  - Requirements question 2: should Sign-in (change email, change password, two-step) be built as a real room, given none of it exists today and Clerk's hosted pages are the only current route?
  - Requirements question 3: does Appearance stay a room inside Account, as this design has it, or move back out to a standalone settings entry?
  - Requirements question 4: does People gain the one-click Resend this design draws for a pending invite, wired to re-sending the existing onboardingInvites token, or is delete-and-reinvite the permanent behaviour?
  - Requirements question 5: should an admin previewing a client see People's invite, edit and remove controls rendered disabled with an explanation, the way this design draws them, rather than today's silent server-side refusal?
  - Requirements question 6: does the parallel POST /api/portal/invites bulk path ever surface here as a bulk invite, or stay onboarding-exclusive?

## portal-files

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fportal-files-preview.html
- Files: portal-files.jsx (40 KB)
- Pages: library, folder, search, file, file-versions, file-details, drop, uploading, upload-failed, new-folder, rename, move, delete, loading, empty, empty-folder, no-match, error, stale, client-view
- Covered: Revision pass. Both critic FIX items are fixed in portal-files.jsx and verified in a headless browser. (1) rename: the PAGES-driven initial dialog no longer hardcodes value to an empty string; a single seedDialog(P) helper reads the target file and seeds value from f.name, so ?page=rename opens with "Stockist list, September.xlsx" in the File name field (DOM probe on .pfl-field input confirms the value at 1440). (2) move: the same helper seeds value from f.folderId, so ?page=move opens with Uploads checked (DOM probe on .pfl-pick.on returns "Uploads"). The demo path and the live kebab path now (more in the journal)
- Left out: Only portal-files.jsx was rewritten. portal-files-kit.jsx, portal-files-data.jsx, portal-files.css and previews/portal-files-preview.html were read in this pass and left byte identical, because the two measured issues were both confined to one initial-state constructor and the brief says to leave SHIP work untouched unless a shared file forces a change. I deliberately did not add a "it is here now" marker or a disabled guard to the Move dialog's primary button: either would have needed a new CSS (more in the journal)
- Critic: FIX (first pass FIX)
- Still open after the revision:
  - library (FIX): 1440 light/dark, 375, 768: file cards in the grid carry a version pill ('v3', 'v2') and a comment-count pill (speech-bubble icon + '3', '1', '2') sourced from portal-files-data.jsx's local sample array. Per requirements section 5 ('Honesty requirements') and acceptance item 8.7, a version count or comment count may only appear on screen if backed by a real column/query; today there is no messages.fileId and no file-version table, and nothing in the rendered UI marks these numbers as proposal-only. This is the exact 'controls that section 5 calls dishonest' check the brief asks the critic to run. Bounded fix: remove the badges from FileCard/FileRow until the schema exists, or add a visible 'not tracked yet' treatment; do not change portal-files-kit.jsx/data.jsx without also updating this note.
  - library (FIX): Everything else on this page key is correct: shared PageHeader/headline band (Band/BandCell components, not a bespoke header), one 14.5rem rail via window.TahiRail (Folders/Filters/Sort/foot, no second-level folder tree, no horizontal filter toolbar), band prints only file counts and a sizeBytes sum ('391 MB in total, across 5 folders'), no storage quota, no New badge, no seen dot. 375 collapses the sidebar to the bottom tab bar and the rail to a 'Folders and filters' disclosure with New folder/Upload files buttons at 2.75rem (44px) tall. Dark mode has no contrast regression. Console/page errors: none.
  - folder (FIX): 1440 light: the list view's 'NOTES' column prints a comment count per row (0, 1, 2, 3) taken from the same unbacked sample array as the library page's pills. Same fix as library: gate behind a real column or remove.
  - folder (FIX): Breadcrumb (All files > Deliverables > TR-1047 Autumn campaign landing page), per-request auto-folder, and list/grid toggle all work as specified. 768 keeps the sidebar and shows the rail inline (no third rail idiom). No console errors.
  - search (FIX): 1440 light: search results ('2 of 19 files' for query 'palette') still show the same fabricated version/comment pills on result cards. Same root cause and fix as library/folder.
  - file (FIX): 1440 light/dark, 375: the FileDrawer's Notes tab renders a full fabricated comment thread (three named people, timestamps, realistic conversational copy) with no in-app indication that this is proposal-only UI with no backing messages.fileId column yet. The tab strip itself shows 'Notes 3' and 'Versions 3' count badges. This is new surface per requirements section 2 ('not a port of something live') and section 7 open question 3 leaves whether Comments/Versions ship at all as unresolved; shipping it fully functional-looking with sample data, with no visible flag, fails acceptance item 8.7.
  - file (FIX): Everything else is correct: built on the shared SlideOver (matches request/task detail pattern per section 6, not the design's own bespoke Drawer), footer has Download / Share to a request / kebab (Rename, Move, Delete, correctly gated by canEdit and 'mine'), 375 correctly becomes a bottom sheet. Dark mode contrast is fine. No console errors.
  - file-versions (FIX): 1440 light/dark, 375: the Versions tab lists v3/v2/v1 with named authors, dates and a 'Current' pill plus working 'Get' buttons, all from the same unbacked sample array. Same honesty gap as the Notes tab: no visible marker that this is a proposal with no version-history column behind it yet.
  - file-details (FIX): 1440 light: the Details tab body itself is honest (Type, Size, Added, Shared by, Folder, Visible to, Request link, all real-shaped fields with no fabricated counts), but the tab strip above it still shows the same 'Notes 3' / 'Versions 3' badges from the shared FileDrawer header, carrying the library/file finding onto this page key too.
- Questions for you:
  - Uploads under Client View: the page keeps them on, matching the live impersonation-cookie behaviour, and turns off rename, move and delete. Confirm that is the answer to requirement question 1 before the port, since the design's original read-only-preview rule would regress a shipped path.
  - Standing folder names: this design uses Deliverables, Brand, References, Uploads plus a client-made Season 2027, per TASKS.md CL.1 wording, and keeps Contracts out as its own route. Confirm that list over the older Brand / Content / Contracts / Deliverables / Shared by you set.
  - Is the per-file comment thread and version history in scope for the first ship of CL.1, or do folders plus drag-and-drop go first? Both are drawn and both need schema work (folderId on files, and a file target on messages) before either can be wired.
  - Empty-state copy: the page keeps the live wording, "No files yet" and "Delivered work shows up here...". Confirm, or switch to the design's "Nothing here yet" wording.
  - Is there an agreed maximum upload size? Nothing is drawn, so the only upload failure shown is a transfer that stops partway and can be retried. A cap needs a number and a route change before it becomes UI.
  - app-mount.jsx still reaches for window.TahiPortalFiles.Messages, which no longer exists. The single wiring step at the end of the pass needs to point messagesContent at the messages module.

## portal-home

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fportal-home-preview.html
- Files: portal-home-boards.css (14 KB), portal-home-page.jsx (19 KB), previews/portal-home-preview.html (9 KB)
- Pages: home, home-one-track, home-three-tracks, plan-setup, project, project-undated, billing-xero, quiet, empty, loading, card-errors, member-seat, view-as-client, act-as-client, first-run-fresh, first-run-mid, first-run-finish, first-run-hidden, plan-sheet, project-sheet, welcome-video
- Covered: Every FIX on the critic's list was the same measured item, repeated on all 21 pages: at 768 the shell keeps its 240px client rail, the .pf container measures 528px (33rem), and the page fell through the 48rem container rule into the fully stacked phone layout instead of the 6+6 tablet layout acceptance item 5 requires. I confirmed against the repo that this is real product behaviour, not a preview artefact: components/tahi/app-sidebar.tsx documents "Tablet (768 to 1023px) persistent rail" and app/globals.css pins .tahi-sidebar at width 240px with no tablet override, so this page's own containe (more in the journal)
- Left out: The 404 on uploads/liam-iceland-profile-picture.jpg is still there and is left there on purpose. It is the shell's owner avatar, written as a project-root-relative path in app-shell.jsx, fetched during the instant the shell renders the owner audience before the harness switches it to client, and it 404s only because previews are served from previews/. I tried a harness-side retry and proved it cannot work: the image errors while still detached from the document, so no capture-phase listener on d (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - The 768 two-column layout only exists because the sidebar stays 240px wide on a tablet. Should the client rail collapse to the 64px icon strip below 1024px instead? That would give the page 48rem of container at 768 and let it use the designed 7+5 spans rather than an even 6+6, and it is a one-line change in globals.css. It is a shell decision, so it is yours, not this module's.
  - Requirement open question 1: does the first-run onboarding checklist (LW.9, flagged off site-wide) come back before or after the port, so a builder targets something visible? All four of its states are drawn and ready either way.
  - Requirement open question 4, and the biggest one for this page: do the plan and project detail sheets get built as real slide-overs, or does the inline Plan card (which already shows rate, tracks, next invoice and with-us-since) stay the whole answer and the sheets get dropped? Three doors on this page currently point at them.
  - Requirement open question 3: does the dismissible studio notice belong on the home as part of Announcements, or should broadcasts stay list-only on a dedicated /announcements page? It is drawn here as a proposal and is trivial to remove.
  - Requirement open question 5: does the welcome video play inline in a sheet as drawn, or keep opening Loom in a new tab as built? The sheet means embedding a third-party player inside the dashboard's own chrome.
  - client-tracks-schedule.md open question 2: is the simple current-plus-queue TrackBoard the permanent shape of this card, or should it gain the Waiting-on-you and Delivered-30-days lanes and the turnaround stat that lib/track-lanes.ts and lib/track-stats.ts already compute for the orphaned /tracks page? The answer changes how much of this zone gets redrawn, and it is entangled with CT.18 (delete /tracks or redesign it).

## portal-money

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fportal-money-preview.html
- Files: portal-money.jsx (71 KB), portal-money-data.jsx (35 KB), previews/portal-money-preview.html (21 KB), portal-money-invoices.jsx (37 KB), portal-money-billing.jsx (15 KB), portal-money-billing-data.jsx (10 KB), portal-money-kit.jsx (19 KB), portal-money.css (66 KB), portal-money-billing.css (16 KB)
- Pages: invoices, invoices-loading, invoices-empty, invoices-filtered, invoices-error, invoices-denied-member, invoices-denied-feature, invoices-denied-noorg, invoices-readonly, invoices-ask, invoices-ask-readonly, invoice-hosted, invoice-bank, invoice-bank-gbp, invoice-paid, invoice-import-note, invoice-ask, invoice-readonly, invoice-loading, invoice-missing, invoice-error, invoice-denied, invoice-thread, billing, billing-stripe, billing-loading, billing-empty, billing-error, billing-denied-member, billing-readonly, services, services-loading, services-empty, services-error, services-feature-off, services-member, services-roomy, services-custom, services-readonly
- Covered: REVISION PASS. Only three files changed: portal-money.jsx, portal-money-data.jsx and previews/portal-money-preview.html. The other six module files were left byte-identical, because the critic confirmed every Invoices and Billing page as correct and nothing in a shared file forced a change there. Each of the three writes was re-fetched from the server and diffed against my local copy after upload; all three match exactly, and both .jsx files pass `node --check`. FIX 1, the cross-cutting one, Messages in the client shell (flagged on all 39 page keys). The requirement documents both say the desi (more in the journal)
- Left out: Messages was not removed from app-shell.jsx itself. That file is shared by every module being drawn tonight and by fifteen concurrently running designers, and the critic classed the issue as shell-chrome configuration rather than a portal-money defect, so editing a 70 KB shared file to fix my own screenshots would have risked the rest of the pass. The suppression lives in my preview harness and the two-line shell change is written out in the harness comment and above. The locked-placeholder trea (more in the journal)
- Critic: FIX (first pass FIX)
- Still open after the revision:
  - billing (all 7 states: ready/stripe/loading/empty/error/denied-member/readonly) (FIX): Content correct: Current Plan card, invoice history table, Manage Billing gating, denial copy shared with Invoices, all matching requirements.
  - billing (all 7 states: ready/stripe/loading/empty/error/denied-member/readonly) (FIX): Bounded, harness-timing FIX: at a naive ~1.5s post-networkidle screenshot wait, all 7 billing page keys failed to suppress the Messages sidebar item and failed to collapse to the mobile tab bar at 375 (the desktop sidebar rendered at nearly full width, crushing content to roughly 135px, illegible truncated text). A recheck with a 4s settle wait showed all 7 states render correctly (Messages hidden, correct mobile tab bar, no overlap) on both 1440 and 375, and the underlying billing.jsx markup itself is correct. This means Billing specifically settles measurably slower than Invoices/Services in this preview harness; the designer should re-verify the Messages/mobile-chrome fix on Billing with a generous wait or a real click-through before calling it 'measured', and should confirm the real (non-harness) app-shell wiring isn't similarly borderline once the two-line CLIENT_NAV change lands.
  - billing (all 7 states: ready/stripe/loading/empty/error/denied-member/readonly) (FIX): Dark mode (billing-1440-dark) is clean and legible; no issues there.
- Questions for you:
  - Section 7 question 5, now the live one: should a member seat see a locked "Your plan" placeholder on /services and /billing, or is absent right? This pass draws absent, matching the shipped portal. If you want the locked card back, say so and it returns with the lock note and no figures.
  - Section 7 question 2: /billing's client branch works end to end but has no nav entry at all (CT.16 removed it deliberately). Does it go back into the client nav, or stay unlisted but reachable by URL? The preview mounts it under the Invoices nav slot because the shell has no honest place to put it.
  - Section 7 question 1 (CT.11): when a client orders from the Services catalogue beyond asking a question, does that mint a request in their queue or start a Stripe checkout? Nothing in the catalogue offers to buy anything until you answer.
  - Section 7 question 4: port the fuller MR.7 showcase (forest plan stage, usage block, editorial catalogue, add-on shelf) that this file draws, or keep shipping the simpler live build (plan card, ladder, uniform grid)? The design file is the fuller one; the live port is not.
  - Billing read-only preview: Manage Billing stays enabled while a Tahi admin views as a client, because it is a read that opens the real Stripe portal for that org. The screen says so out loud rather than deciding it quietly. Confirm that is what you want, or it gets disabled with the same read-only reason as every write.
  - Invoices list open questions from Pass 3 are still unanswered and still drawn as designed: the fourth "On the record" summary tile (shipped version has three), and the Views/Filters living in the shared Rail rather than an inline segmented control. Both are additive proposals, not fixes.
  - For an hourly or project client, should the catalogue keep the plan-free fit vocabulary this pass introduced ("In a retainer" instead of "In your plan"), or would you rather those clients see no fit pill at all?

## reports

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Freports-preview.html
- Files: reports-kit.jsx (21 KB), reports.css (23 KB), reports.jsx (21 KB), reports-data.jsx (14 KB), previews/reports-preview.html (10 KB)
- Pages: reports, reports-loading, reports-empty, reports-error, reports-partial, reports-member, reports-export, reports-stopgap, reports-restricted, reports-sync-locked, reports-client
- Covered: REVISION PASS. Every FIX was the one shared defect the critic measured on all seven pages: the sticky jump nav was a single horizontal scroller with the scrollbar hidden, so labels clipped ("Retainer healt", "Team utilisatio", "Client margi") and, at 375, a whole anchor sat off screen with no cue. Two files changed, reports-kit.jsx and reports.css; reports.jsx, reports-data.jsx and the preview needed no edit, so the SHIP-worthy content of every page is untouched. The fix, in three parts. (1) The section row now WRAPS at desk and tablet width instead of scrolling. Measured after the change at 1 (more in the journal)
- Left out: A visible scrollbar was specified as a fourth cue but Chromium renders overlay scrollbars here, so the track measures zero height at rest; scrollbar-width thin and a styled webkit track are in the CSS for platforms that honour them, and the fade plus the nudge buttons carry the affordance on their own. At a 768 viewport the shell keeps its 240px sidebar, so the content well is about 30rem and the nav still scrolls rather than wraps: wrapping ten anchors into a 480px well would build a 300px tall (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - CT.19 shape: does /reports survive at all after its duplicates move, or is it deleted outright? The preview answers both, page=reports for the surviving four sections and page=reports-stopgap for the ten section hold, but the section list needs your ruling before either is built.
  - If a smaller page survives, does Retainer health stay on Reports or move to Sales analytics with the rest of the pipeline content? It is drawn here under the Sales group label, which is where the live jump nav puts it.
  - Fixed costs: delete this page's commitments editor outright, or merge it and the /financial-reports one into a single shared component both pages import? The design assumes delete, so the stopgap shows no CRUD form here at all.
  - Restricted sections: visible but locked, as drawn, or hidden entirely from a member who lacks financial_reports and sales_analytics? Visible-but-locked was chosen because hiding leaves a reader unable to name what to ask for, but it does advertise the existence of the money screens.
  - Is /reports worth designing before the triage lands? This pass designed both shapes rather than guessing. If you rule A on question 1, the ten section stopgap pages can be dropped from review entirely.

## requests

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Frequests-preview.html
- Files: requests-listview.jsx (21 KB), requests-states.jsx (17 KB), requests.jsx (21 KB), requests-extra-fixes.css (3 KB), requests-workload.jsx (5 KB), requests-capacity.jsx (4 KB), requests-audience.jsx (1 KB)
- Pages: list, list-loading, list-empty, list-nomatch, list-error, list-readonly, board, board-loading, workload, timeline, teammate-list, detail, detail-scope, detail-quiet, detail-loading, detail-error, detail-notfound, dialog, dialog-sub, dialog-doc, wizard, wizard-drafts, wizard-offline, wizard-drawer, bulk, client-list, client-empty, client-nomatch, client-board, client-detail, client-review, client-dialog, client-wizard
- Covered: REVISION PASS. All four FIX items are resolved and verified on the rendered preview; no SHIP page was redrawn. 1. list (FIX, empty Priority cell). requests-listview.jsx no longer writes `createElement(PriorityTag) || createElement(NoneCell)`. Two helpers, priorityCell(pri) and dueCell(offset), branch on the row's own value (D.PRIORITY / D.dueInfo) before an element exists, so the fallback can actually run. Verified at 1440 light scrolled to the bottom of the 18 rows: TR-1018 "Newsletter, August issue" now reads "No priority" (.qa-screens/design/req-list-bottom.png). The fix is inside ListView, (more in the journal)
- Left out: - requests-detail.jsx still holds eight em dashes (three comments, three copy strings including "Scope flagged - check before continuing", and two "-" placeholder cells in the rail's Due and Estimate fields). It was not in the critic's FIX list, it is 84.9 KB, and this tool can only write a file whole, so retyping the module's reference detail page to change five strings is a bigger risk to a SHIP page than the defect. The two placeholder cells are also unreachable with today's sample data (no r (more in the journal)
- Critic: FIX (first pass FIX)
- Still open after the revision:
  - detail-scope (FIX): 1440 light and dark: the scope-flag banner reads 'Scope flagged, check before continuing', an em dash in visible sample copy. This breaks CLAUDE.md rule 6 (no em/en dash anywhere) and studio-requests.md acceptance item 10. Confirmed present in requests-detail.jsx source (not fixed in this revision) and directly visible in the rendered screenshot, not just in code comments. The designer disclosed this as a known, deliberately-deferred issue (one of 8 dashes in the 85KB requests-detail.jsx, left out because the file can only be rewritten whole) rather than a hidden defect, but it is still a measurable, screenshot-visible acceptance-criteria failure that should be a one-line string fix in a follow-up pass.
- Questions for you:
  - Can a request exist with no due date? The list is ready for it ("No date") but no sample row has one. Say yes and I will make TR-1018 dateless and fix the detail rail's "-" placeholder in the same move.
  - The read-only scene models an admin in act mode, so Export CSV survives. Is that the only read-only case? If a client can ever land on their own page read-only (a suspended account, say), they currently get no header controls at all, which I think is right but is not written down anywhere.
  - The band skeleton bug is in head-band.css and affects every module's loading band. Do you want the one-line token fix landed there centrally, or left as the per-module patch I shipped?
  - In the client lens the shell signs you in as Ana Rewiri of Mahana Orchards while the requests data and the read-only strip say Physitrack. Which org should the client persona be, so the sidebar and the rows tell the same story?
  - requests-detail.jsx's five visible dashes: worth me rewriting the whole 85 KB file to clear them, or do you want that done in the repo port instead, where it is a five-character edit?

## sales-artifacts

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fsales-artifacts-preview.html
- Files: sales-artifacts-builders.jsx (51 KB)
- Pages: proposals, proposals-loading, proposals-empty, proposals-filtered, proposals-error, proposal, proposal-slide, proposal-package, proposal-decisions, proposal-comments, proposal-versions, proposal-share, proposal-reading, proposal-saving, proposal-draft, proposal-accepted, proposal-declined, contracts, contracts-loading, contracts-empty, contracts-filtered, contracts-error, contract, contract-terms, contract-terms-draft, contract-slots, contract-audit, contract-reading, contract-draft, contract-signed, schedules, schedules-loading, schedules-empty, schedules-filtered, schedules-error, schedule, schedule-row, schedule-linked, schedule-risks, schedule-raci, schedule-cover, schedule-share, schedule-reading, schedule-draft, templates, templates-loading, templates-empty, templates-preview, templates-edit, v-proposal, v-proposal-deciding, v-proposal-asking, v-proposal-declining, v-proposal-accepted, v-proposal-declined, v-proposal-question, v-proposal-expired, v-proposal-preview, v-proposal-loading, v-proposal-notfound, v-contract, v-contract-read, v-contract-signed, v-contract-fully, v-contract-expired, v-contract-invalid, v-contract-loading, v-schedule, v-schedule-plan, v-schedule-loading, v-schedule-notfound, papers-proposals, papers-contracts, papers-schedules, papers-empty, papers-loading
- Covered: Revision pass, one file touched. The critic returned exactly one verdict, schedule-risks FIX, and that is what changed. 1. The FIX. RiskEditor's Owner and Impact controls were fully controlled Selects wired to onChange={() => {}}, so every pick snapped back to the value the row started on. The register now holds its rows in local state seeded from s.risks (and reseeded when the schedule id changes). Each Select writes to that state and reports it honestly ("Owner set to Tahi", "Impact set to Low"), matching the RowEditor pattern beside it. Add and Remove now move the same list instead of only  (more in the journal)
- Left out: RaciEditor's Add a workstream button still only raises a toast, and its five Inputs stay uncontrolled. The critic did not flag it and no control there is dead the way the Selects were (typing works, nothing snaps back), so extending the same local-state treatment to the matrix is a change of scope the verdict did not ask for. Worth one line in the next pass if Liam wants both tables to behave identically. Every SHIP page was left alone. The proposals list, the proposal editor, the contract edito (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - Should the risk register save per row, or does a risk only exist inside the published snapshot? There is no risks API today, so the design shows it as a working table whose rows do not survive a reload, the same as the gantt's Add row. If it should persist, that is a schedule sections write endpoint, not a design change.
  - Should the RACI matrix get the same treatment the risk register just got, rows in state, a working Add and Remove, and an empty state, so the two tables in the same editor behave identically? Right now the matrix's Add only raises a toast.
  - Proposals section 7, still unanswered: does Withdraw ship in this port (one button, behind a confirm, hidden once decided), or does the withdrawn status get removed since nothing today can reach it?
  - Proposals section 7, still unanswered: should the proposal editor lock read-only once accepted, declined or withdrawn, the way the design models it, or stay fully editable the way the shipped editor works?
  - Proposals section 7, still unanswered: build Draft the contract from an accepted proposal in this pass, since it crosses into Contracts, or hold it? And do the Comments and Versions panes bundle into AR.4 or split into their own backlog items? Neither has a TASKS id.
  - The sidebar avatar 404s on every preview in this pass (uploads/liam-iceland-profile-picture.jpg resolves against /previews/). It needs one line in app-shell.jsx, which is shared by every module rendering tonight, so it belongs to the wiring step rather than to any one designer. Want it folded in there?

## sales-pipeline

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fsales-pipeline-preview.html
- Files: sales-pipeline-leads.jsx (47 KB), sales-pipeline-calls.jsx (39 KB), sales-pipeline-deals.jsx (54 KB), sales-pipeline-kit.jsx (32 KB), sales-pipeline-kit.css (17 KB)
- Pages: leads, leads-loading, leads-empty, leads-filtered, leads-error, leads-selection, leads-briefing, leads-import, leads-tools, leads-promote, leads-scoped, lead, lead-edit, lead-reply, lead-unenriched, lead-loading, lead-error, calls, calls-past, calls-details, calls-details-set, calls-schedule, calls-loading, calls-empty, calls-filtered, calls-error, calls-scoped, call, call-extract, call-no-transcript, call-unlinked, call-loading, call-error, deals, deals-list, deals-stalled, deals-capped, deals-peek, deals-new, deals-loading, deals-empty, deals-filtered, deals-error, deals-scoped, deal, deal-stalled, deal-nudge, deal-stall, deal-close, deal-loading, deal-error
- Covered: REVISION PASS. Every critic FIX is resolved and verified by measurement on the shipped preview. 1. leads-error, calls-error, deals-error (the systemic "live figures beside a failed read" finding). New shared primitive StatBandBlank plus blankCells in sales-pipeline-kit.jsx: on demo==='error' the band keeps its box, its four tiles and its tile height so nothing on the page jumps, and every figure reads "--" with "Not available" underneath. New rule in sales-pipeline-kit.css drops the lead tile's accent colour to --text-faint so the blanked figure is not painted as a live headline; measured colo (more in the journal)
- Left out: 1. The Leads bulk bar at 375. With three or more leads selected its five ghost actions plus the clear button run to about 618px inside a bar that is overflow-x:auto, so Delete and the close control are reachable only by scrolling the bar sideways, and unlike the board there is no edge button saying so. It scrolls, so it is honest rather than broken, the critic passed leads-selection, and wrapping the actions would restyle a SHIP page. Left as is. 2. The lead and call tables at 768. The shell kee (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - The blanked band reads '--' with 'Not available' on all four tiles. The alternative is hiding the band entirely on an error. I kept the box so the page does not jump between the loaded and failed states, but four tiles all saying 'Not available' is repetitive. Do you want it blanked like this, or gone?
  - Should the head-band.css narrow rule be fixed at source (align-items:stretch inside the 56rem container query) so every list page in the repo gets the phone main column fix, not just Sales? It is a one-line change in a shared file and would want a pass over Requests, Tasks, Clients and Ops at 375 before it lands.
  - At 768 the shell keeps the full sidebar, which leaves about 496px for the list and makes the lead and call tables scroll sideways with no cue. Do you want the sidebar to auto-collapse at tablet, or the tables to fold to cards at 768 the way they do at 375?
  - On an error state the rail view counts and the count beside the search now read as unavailable. If the real API can return a trustworthy total even when the page read fails, say so and I will show the total and blank only the in-view figures.
  - The Leads bulk bar scrolls sideways at 375 rather than wrapping. Would you rather the five actions wrap onto two rows on a phone, or collapse into one 'Actions' menu?

## settings

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Fsettings-preview.html
- Files: settings-kit.jsx (20 KB), settings-sections-b.jsx (22 KB), settings-sections-c.jsx (36 KB), settings.jsx (11 KB), settings-extra.css (12 KB), settings-extra-mobile.css (3 KB)
- Pages: profile, appearance, notifications, booking, booking:empty, branding, branding:loading, modules, announce, studio, studio:loading, studio:error, forms, forms:loading, forms:empty, forms:error, forms:dialog, kanban, kanban:empty, kanban:error, kanban:dialog, kanban:seed, tasktpl, tasktpl:empty, tasktpl:error, tasktpl:dialog, pipedef, pipedef:loading, stages, stages:empty, stages:error, stages:dialog, leadauto, integrations, integrations:loading, integrations:error, webhooks, webhooks:empty, webhooks:error, webhooks:dialog, automations, automations:empty, automations:error, automations:dialog, crons, crons:empty, aicontext, aicontext:picker, emaildelivery, emaildelivery:log, emaildelivery:widen, teamaccess, teamaccess:loading, teamaccess:empty, teamaccess:error, subscription, plans, plans:empty, plans:dialog, reserves, reserves:empty, reserves:error, reserves:dialog, audit, audit:empty, audit:loading, audit:error, danger, danger:confirm, seat:member
- Covered: REVISION PASS, both FIX items resolved, nothing else redrawn. 1. forms:error (FIX, resolved). The inline Note/banner under the list is gone. On state error the section now pushes one toast through the same useToasts/Toasts channel the save and delete confirmations already use ("Request forms could not be loaded. Nothing a client sees has changed, this is a read failure on this page."), and the list card itself renders a new LoadError block (alert tile in the danger tone, title, one line, a Try again button) instead of either the stale rows or the leaf empty state, because "there are none" and  (more in the journal)
- Left out: - SHIP pages were not redrawn. Studio details, Email delivery, Modules, Announcements, Team and access, Danger zone, Profile, Appearance, Notifications, Booking link and Branding keep the markup the critic passed; the only thing that reached them is the shared toast primitive (error toasts now persist and carry a dismiss), which their own confirmations do not use. - settings.css (the base layer) was not touched. The toast, portal, card and sub nav rules there are the shipped page's own file; the (more in the journal)
- Critic: SHIP (first pass FIX)
- Questions for you:
  - Requirement open question 3 is now the live one: the seven list sections use the canonical leaf EmptyState in this design, not the shipped EmptyRow text row. Confirm the upgrade, or tell me to put EmptyRow back and record the divergence as a permanent exception for dense admin lists.
  - Error toasts in this pass do not auto dismiss, they wait for the close control, while save and delete confirmations still fade after 3.6 seconds. Is that the rule you want across the whole product, or should a failure also time out?
  - A failed read now hides the section's primary action (New form, Add endpoint, New pot) and the Kanban Seed the defaults button, on the reasoning that you should not write to a list you could not read. Correct, or would you rather keep the action enabled and let the write fail loudly?
  - Try again in the failure block re requests that one section only. Should it also clear and refetch the rest of the pane, given several sections read the same settings endpoint?
  - Requirement open question 1 still stands and is a product decision, not a drawing one: keep Studio details as one all or nothing save across twelve keys, or split it into studio identity, getting paid and project manager saves so a rejected Xero code cannot leave the other fields reading as saved.
  - Requirement open questions 2, 4 and 5 are unanswered and unchanged: where Zapier config lives, whether the Everyone mode confirm should name the exempted org ids explicitly, and whether workspace deletion stays a manual Tahi completed process forever.
  - Preview only nit, not a design defect and not mine to fix: the shell's sidebar avatar requests uploads/liam-iceland-profile-picture.jpg relative to previews/ and 404s, so it falls back to initials. It comes from app-shell.jsx, which this pass must not edit. Worth a one line asset base fix in the wiring step.

## tasks

- Preview: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=previews%2Ftasks-preview.html
- Files: C:\Users\Work\Projects\tahi-dashboard (Claude Design project 57bf60cf-5e6d-450f-9e2f-e25c8d12fd66) tasks.jsx (19 KB), tasks-kit.jsx (44 KB), tasks-fixes.css (3 KB), previews/tasks-preview.html (7 KB)
- Pages: list, list-loading, list-empty, list-filtered, list-error, board, board-loading, week, week-loading, week-empty, detail, detail-linked, detail-blockers, detail-level, detail-loading, detail-delete, promote, new-task, new-task-ai, template, wizard, readonly, readonly-detail, no-access, no-access-proposal, client
- Covered: REVISION PASS, both FIX items resolved, no SHIP page redrawn. FIX 1, list-error (the band quoted real counts next to a failed fetch). The page now carries one explicit `errored` state that every count reader gates on, not just the list body. `base` returns [] on error as it already did on loading and empty, so no derived figure can escape. The headline band renders BandSkeleton (the same box, same four tiles, same 6.125rem height, static bars, aria-hidden) instead of four figures, so nothing moves when the retry lands. The rail and the sheet drop their per-view counts entirely rather than prin (more in the journal)
- Left out: Every other page key was left byte-identical in behaviour. The only shared-file edit is tasks-kit.jsx, forced by FIX 1; the new `unknown` prop defaults falsy, so List, Board, My week, the detail sheet, both dialogs and the wizard render exactly as the Pass 3 SHIP verdict saw them (confirmed against tasks-self-check.png). Deliberately not done: - head-band.css was not patched, even though the dark placeholder defect lives there and affects every list page's loading band in the catalogue. It is sh (more in the journal)
- Critic: FIX (first pass FIX)
- Still open after the revision:
  - list-loading (FIX): New defect, same honesty class as FIX 1 but left unaddressed: during pure loading (not the error state), the rail's saved-view items (All tasks, Assigned to me, Due this week, Overdue, Blocked, Client-linked, Internal, Done) each print a confident '0' at 1440 light and 1440 dark, while the toolbar total simultaneously reads 'Counting' and the band correctly shows its BandSkeleton placeholder. In Toolbar (tasks-kit.jsx), `unknown` is only passed as `!!errored`, never as `!!loading`, so `railCount()`'s suppression only fires on the error path; the `blank = loading || unknown` count computation still feeds literal `0` values into the rail during loading, contradicting section 3's own instruction that 'rail counts and the toolbar total both read the skeleton state via a shared loading flag.' A zero next to 'Assigned to me' while nothing has loaded is exactly the lie section 5 warns against ('a confident zero here would be a lie').
  - list-loading (FIX): Same measurable defect at 375 (rail counts read 0 in the Filters context) and would reproduce on 768 and dark, since it is a data-computation bug in Toolbar's counts object, not a styling issue.
  - board-loading (FIX): Same rail-shows-zero-during-loading defect as list-loading: rail items all read '0' at 1440 light while the toolbar says 'Counting' and the board itself correctly shows BoardSkeleton pulse columns.
  - week-loading (FIX): Same rail-shows-zero-during-loading defect: rail items read '0' at 1440 light (rail itself is otherwise dimmed/inert for My week per acceptance criterion 5, but the zeros are still visible text inside the inert rail, which is its own small honesty problem layered on top of the intended inertness).
- Questions for you:
  - TP.3 multi-day allocation: the design holds option A, one dueDate that follows the last planned day with the spanned days as a separate per-day allocation. Confirm A, or say the due date itself should become a range (B).
  - Scoped seat with zero teamMemberAccess rules: should /tasks say something? The default frame (page key no-access) says nothing, which is today's behaviour. The drawn alternative is on page key no-access-proposal, flagged PROPOSAL on screen. Yes (ship the banner) or no (leave as is).
  - Task Activity/history card: wanted at all? It needs a new table and a write on every mutation, so it is a schema task before a design task. Yes or no.
  - Create request from task (promote): stay detail-panel only (A, no change), or add a row-level affordance on List and Board (B)?
  - Shared file, for the lead rather than for Liam: head-band.css draws skeleton bars in var(--bg-tertiary), which is invisible on the lead tile in dark (measured contrast about 1.05). Every list page's loading band has this. Worth one line in head-band.css after the pass, at which point the scoped rule in tasks-fixes.css can be deleted.

## Shell wiring

Verified at 06:30 NZ: every module the review lists is registered in app-mount.jsx and reachable from the shell nav for its audience (owner 31 of 33 nav ids land on a module, teammate 6 of 7, client 14 of 14; the misses are the legacy shims, not new pages); the command palette indexes them from the nav groups. Zero console errors, zero failed requests across all three audiences. "Tahi App Shell.html" was repaired: ten stylesheets that loaded twice now carry the ids the modules check for, head-band.css moved to sit right after app-shell.css so modules override it rather than the other way round, requests-focus.css precedes requests-detail.css, sales-pipeline-kit.css follows the band sheet. Captures in .qa-screens/design: owner overview, owner messages (light and dark), owner reports, owner calculator, owner invoices, owner proposals, client messages, client invoices, owner requests at 375.

How to look: open https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=Tahi+App+Shell.html, use the Tweaks pill (bottom right) for audience, theme and device, and the command palette (Ctrl or Cmd plus K) to jump to any page by its nav label.
