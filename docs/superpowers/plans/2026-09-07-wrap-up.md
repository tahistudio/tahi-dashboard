# Wrap-up for Liam, Monday 7 September 2026 (written 02:00 NZST, refreshed 06:00 NZST after the night streams landed)

Kia ora. This is the full wrap-up you asked for: what is designed in Claude Design, what is built in the repo, and how far we are from an MVP and from the full platform. LIVE means on portal.tahi.studio behind the gate (type-check, worker tsc, lint, the whole vitest suite, a build, migrations on both D1s first, push, health probe, browser smoke). DESIGNED means a module in the Claude Design project "Tahi dashboard" or its local mirror, critiqued and fixed.

## Headline

| Measure | By surface count | By engineer days |
|---|---|---|
| To a friendly-client MVP | 70.8% | 88.4% |
| To the FULL platform (every spec) | 64.8% | 78.9% |

Arithmetic, MVP: (LIVE 10 x 1) + (PARTIAL 14 x 0.5) + (zero weight 0 x 0) = 17; 17 / 24 = 0.708 = 70.8%. Days: 1 - (26 remaining / 225 scope days) = 0.884 = 88.4%.
Arithmetic, FULL: (LIVE 23 x 1) + (PARTIAL 37 x 0.5) + (zero weight 4 x 0) = 41.5; 41.5 / 64 = 0.648 = 64.8%. Days: 1 - (126 remaining / 597 scope days) = 0.789 = 78.9%.

Read the two columns together. By count, half-built surfaces drag the number down; by days, most of the heavy lifting is sunk and what remains is short, well-defined work. My own judgement sits between them: the friendly-client MVP is about 80 percent there with roughly 26 engineer days of work left, and about 4 of those wait on you (the ManyRequests token, the sending domain, the Giant Group go). The full platform is about 70 percent there with roughly 126 engineer days left. With agents running the way they have this weekend, an engineer day is closer to half a calendar day, so: friendly client by 13 September still holds, full cutover before your 16 September renewal is tight but possible if the import lands early in the week, and the full platform is a November finish.

The inventory behind these numbers is docs/superpowers/audits/2026-09-07-spec-coverage-inventory.md (64 surfaces, each with evidence paths, status, days and the missing behaviours). It is a static read of the tree at f47b8805 revised at 06:00 for the surfaces that shipped overnight; the headline numbers above are the revised ones.

## Designed in Claude Design (comment there; switch the audience to client for the portal)

Written back to the project and reviewed by you:

- Clients list and client detail (ported, live).
- Client portal: home, requests list and detail, new request, Files as a mini Drive with threads, Messages, invoices, services, notifications, account, welcome, offline. Home, invoices, files, account, notifications and messages are ported and live.
- Notifications with the left filter rail; Messages (both live).
- Account with the segmented slider and the settings look (design done; the port is the client account item below).
- Sales pipeline: leads list and detail, calls list and call detail, deals board and deal detail (you approved all three).
- Sales artifacts: proposals, contracts and schedules, each with list, editor, templates and the public viewer.
- Ops: time week grid with the Xero export drawer, team with access scope in plain words, capacity, tracks mini kanbans plus the client-facing tracks view.
- Studio invoices and the Services showcase v2 (both critiqued FIX, strong).
- The shell itself: sales, artifacts and ops wired into the app shell, re-checked SHIP across 18 surfaces at desktop, 375 and dark.

Staged on disk, not yet written back (the Claude Design MCP lost its design scopes at 00:05 and needs you to run /design-login; TASKS DL.0 to DL.4):

- Proposals editor rework: three panes, the wiring bug fixed, the pane issues from your afternoon note.
- Consistency pass across all pages: the filter rail on the left everywhere (withheld or collapsible on wide-table pages), one head band implementation and height, consistent headline feature cards, rail families aligned.
- Studio invoices wired under Finance in the shell.
- Services plan ladder: what they have, lower and higher plans, per-plan suggestions, upsell only near limits.
- Stalled as a flag on any deal stage rather than a stage.

The write set with its manifest (paths, base etags, order) is being assembled by the local design fixer; its state is noted at the end of this document.

## Built in the repo this run (all LIVE on production unless marked)

- Invoicing channel per client (Stripe or Xero, studio default in Settings), New Invoice defaults from the client, one Xero status mapper with forward-only reconcile and paginated payment sync, the Xero pay link captured on approval, dashboard mark-paid pushing back to Xero or Stripe, Getting paid settings, How to pay in emails and the portal. Migrations 0089 to 0091.
- Predictive autofill on New Request and New Task (grounded Haiku suggestions for due date, priority, size, estimate and category, marked Suggested, clearable, never over a field you touched).
- Mobile top bar: five slots plus a More sheet; Sign out reachable on phones; every button clears 2.75rem below md app-wide.
- Clients list and detail redesign: saved views rail with true counts, endpoint filters, cards view, bulk actions, server paging, Archived and Restore; detail hero, Needs you, tracks, nine tabs including Money. Morning fixes: no duplicate plan text, Industry beside Plan, first and last name.
- Client portal port: one client status vocabulary, Waiting on you hero, masthead with plan, skeletons everywhere, honest participants on request detail, invoices with Pay now and How to pay, Services showcase, Notifications for both audiences, Offline page. Client-walk fixes re-landed after the incident: Client view resolves the audience server-side on every studio page, client money pinned to the client currency, branded not-found, TRIAGE badge hidden from clients, rail contrast in dark.
- Notifications with the left filter rail and Email preferences placed in the rail; Messages page ported for both audiences (unique indexes and inbox tidy in migration 0092, after full D1 exports to .claude/backups).
- Email delivery allowlist: one door out (lib/email-delivery.ts), every Resend caller through it, Clerk invitations and Stripe customer creation gated with a 409 that says why, blocked addresses checked ahead of every rule, widening needs super admin, every withheld send logged to email_suppressions (migration 0094). Production reads closed: allowlist mode, business@tahi.studio only, Staci and Nathan blocked, no client org exempt.
- Act as client (your option A): the Client view banner gains Act as client for super admins, every write is recorded with an acting_as_client audit row, Back to read-only and Exit preview hatches. Smoke-tested on production (banner, a portal request created, one audit row, request archived after).
- ManyRequests importer (merged this hour; deploy state at the end of this document): idempotent on the new manyrequests_id spine (migration 0093, applied to both D1s), direct D1 writes only, a static guard proving the import graph reaches no mail, invite or notification call, dry run by default, windowed request and message walks, invoice overlap refusal against the Xero and Stripe rows already in D1, cleanup with a 30-table delete or refuse policy derived from the schema. The reconciliation (read-only) and the runbook are in docs/superpowers/audits/2026-09-07-manyrequests-import-report.md. The first dry run waits on one step only you can do: the ManyRequests token as a dashboard worker secret (commands in the report and in TASKS MC.3).
- Test suite grew from 2621 to 3290 tests across the run; Playwright covers blockers, the week strip and the AI create view on tasks.

One incident: the client-walk fix deploy broke every signed-in page for twelve minutes at 06:25 Sunday (a server layout called a use-client export). Rolled back, re-landed with the helpers moved to a server-safe file, a boundary guard test, and a post-deploy health probe on every deploy since.

## What is left to the friendly-client MVP (9 surfaces, 26 days)

- **Studio tasks: list, board, my week, detail** (PARTIAL, 3d): Roughly a dozen behaviours have only ever run against the local e2e harness, never on the deployed URL: bulk complete, promote to request, the timer, an AI wizard run, save as default across a reload, the CSV download, both drags, and dark mode on the board and week planner (TASKS.md T2.5); My week shows the open list against no real week dates and cannot span a task across multiple days; it needs a design and probably a per day allocation column (TASKS.md TP.3)
- **ManyRequests reconciliation, import and cutover** (PARTIAL, 3d): Operator: MANYREQUESTS_API_TOKEN on the dashboard worker (staging and production), then the dry run, apply, verification and cleanup per the report
- **Client services catalogue** (PARTIAL, 2d): No order path: Liam has not decided whether ordering mints a request or a Stripe checkout (TASKS.md CT.11); The plan ladder rule (current plan, lower and higher, upsell only near limits) is designed and in the write set but not ported (TASKS.md MC.5 and MR.7)
- **Sign in, sign up, password reset** (PARTIAL, 1d): Forgot password has never been clicked through on the live Clerk build (TASKS.md T1.3); Branded error and verification states at 375px are not built (TASKS.md T1.8)
- **Client invite, onboarding and provisioning** (PARTIAL, 1d): Invoice-me preference and kickoff booking step in the invited onboarding flow (TASKS.md T1.4); Operator: CLERK_WEBHOOK_SECRET on both workers and the endpoint registered in Clerk
- **Studio home: owner and teammate** (PARTIAL, 1d): The home brief card and the nav bar briefing are two surfaces that do not overlap; one source of truth is still open (TASKS.md T2.7); The teammate home has never been visually verified by a human (STATUS.md)
- **Studio invoices list** (PARTIAL, 1d): The redesigned studio invoices list module is designed and in the write set but not wired into the shell or ported (TASKS.md MR.6 and DL.3)
- **Email delivery, allowlist and templates** (PARTIAL, 1d): lib/contract-fully-signed-emails.ts still hardcodes its own From address instead of going through RESEND_FROM_EMAIL (TASKS.md CT.F); A per org digest for bulk status emails is still open (TASKS.md CT.F)
- **Mobile shell, tab bar and touch targets** (PARTIAL, 1d): One full live client lap on production at 375px and dark has never run: submit, reply both ways with email, approve, pay link, files (TASKS.md CT.10 and T2.QA)

Your operator steps inside that list: the ManyRequests token secret (staging and production), the sending domain and branded From address for Resend, the Getting paid bank details in Settings, and the word on Giant Group (exempt their org in the email allowlist, then invite).

## What FULL entails beyond the MVP (27 surfaces, 100 days)

- **North star: discovery to onboarding to delivery automation** (SPEC_ONLY, 12d): AI drafted proposal from a call transcript plus enrichment plus pricing logic is unbuilt; Capacity aware timeline proposal is unbuilt
- **Proposals: list, editor, templates, public viewer** (PARTIAL, 7d): Share never snapshots, and the public GET falls back to live tables when publishedSnapshot is null, so the standard journey serves live rows (app/api/public/proposals/[token]/route.ts:55, TASKS.md T3.1); Accept notifies nobody: no email, no notification, no deal move, while the viewer promises a reply within one business day (app/p/proposal/[token]/proposal-viewer.tsx:1107, TASKS.md T3.3)
- **Affiliates and referral commissions** (PARTIAL, 6d): The Rewardful route is an explicit stub that always returns empty affiliates, referrals and commissions regardless of connection state (app/api/admin/integrations/rewardful/route.ts:8); The sync route never calls Rewardful (app/api/admin/integrations/rewardful/sync/route.ts:29) and the page Refresh button does not even call it (app/(dashboard)/affiliates/affiliates-content.tsx:76)
- **Content studio** (PARTIAL, 6d): Slice 7 signal expansion (LinkedIn engagement, competitor RSS, YouTube transcripts, Webflow news) is deferred (TASKS.md content engine ops); Slice 8 citation tracker and quarterly refresh is deferred
- **Files as a shared library with folders and threads** (SPEC_ONLY, 6d): Folders per client (Deliverables, Brand, References, Uploads) do not exist; A comment thread per file does not exist
- **Client papers: proposals, contracts, schedules in the portal** (DESIGN_ONLY, 5d): No client branch exists on /proposals, /contracts or /schedules; the nav items were removed rather than left bouncing (components/tahi/nav-model.tsx:120); Designed as client papers in the Claude Design shell but never ported (TASKS.md MR.1)
- **Permissions builder and enforcement** (PARTIAL, 5d): Client feature_visibility denies are nav cosmetic only; a denied feature is hidden but the route still answers, breaking the visible equals permitted invariant (TASKS.md T1.6); Deny by default is not flipped: a roleless member resolves to admin and a member with no team row is unrestricted (TASKS.md T1.15)
- **Third party integrations** (PARTIAL, 5d): Slack posts nothing: the send path is a stub comment where chat.postMessage should be (app/api/admin/integrations/slack/route.ts:149); Mailerlite unsubscribe is a stub that returns a message instead of calling the API (app/api/admin/integrations/mailerlite/route.ts:56)
- **Retainer and billing model depth** (PARTIAL, 5d): customMrr and billingModel editors are unbuilt (TASKS.md T668); Retainer health filter, MRR forecast end date awareness and auto churn are unbuilt
- **Cross surface design consistency pass** (DESIGN_ONLY, 5d): Two rules Liam set are not applied everywhere: filters in a left rail on every list page, and one headline feature card anatomy across pages (TASKS.md AR.5); The consistency patch is staged locally at .claude/qa/patched/ but cannot be written back to Claude Design until Liam runs /design-login (TASKS.md DL.0 and DL.2)
- **Deals pipeline and sales kit** (PARTIAL, 4d): A nudge scheduled for later inserts a row that no cron ever reads, so it never sends (app/api/admin/deals/[id]/nudges/route.ts, TASKS.md T3.8); The Auto Nudges toggle flips a flag with zero consumer (app/(dashboard)/deals/[id]/deal-detail.tsx:822)
- **Contracts: list, editor, templates, signing** (PARTIAL, 4d): The fully signed PDF exists only as an email attachment; there is no R2 write and no signedStorageKey column (lib/contract-fully-signed-emails.ts:170, TASKS.md T3.6); bodyHtml accepts any PATCH with no status guard, so a contract stays editable after signatures (app/api/admin/contracts/[id]/route.ts:90, TASKS.md T3.7)
- **Operational reports** (PARTIAL, 4d): The Sales pipeline tab renders six components covering the same questions as /sales-analytics (app/(dashboard)/reports/reports-content.tsx:412, TASKS.md CT.19); The Finance group is five sections duplicating /financial-reports
- **Team, org chart, access scopes and hire onboarding** (PARTIAL, 3d): A hire invited into the Tahi Clerk org resolves to no member row and currently gets full admin including financials (TASKS.md T1.17); The teammate invite token flow is dead: lib/onboarding-entry.ts resolveToken returns null and POST /api/admin/onboarding-invites flow team has zero callers (TASKS.md T1.20)
- **Schedules: list, editor, templates, public viewer** (PARTIAL, 3d): The gantt is a 64rem strip, so a phone client pinch scrolls it (components/tahi/gantt-grid.tsx:122, TASKS.md T3.5); app/layout.tsx applies the dark class from localStorage on every route including the public viewer, which has no toggle, so a teammate dark setting bleeds into the client document (app/layout.tsx:72, TASKS.md T3.4)
- **Project and contract calculator** (PARTIAL, 3d): The pricing dial in never happened: three input modes, per component cost visibility and calibration from past project actuals are all still spec, marked not started (WORKFLOWS.md Phase H+); It does not pull live cost of services, pipeline or capacity as the spec asks
- **Notification engine, SSE and digests** (PARTIAL, 3d): Weekly digest is unbuilt (TASKS.md T698 to T699); Web Push has no service worker handler (STATUS.md Stubs)
- **Offline page, PWA install and web push** (PARTIAL, 2d): Web Push has no service worker handler (STATUS.md Stubs); PWA install has not been tested on iOS or Android (CLAUDE.md Mobile and PWA)
- **Automation rules and outgoing webhooks** (PARTIAL, 2d): The event bus and webhook delivery went live 2026-07-07 but the live smoke of automation fire, webhook delivery and announcement fan out has never run (STATUS.md, TASKS.md W-QA); Zapier outgoing webhooks have an engine but no config surface (TASKS.md T570)
- **MCP worker tool parity and auth** (PARTIAL, 2d): The worker /authorize endpoint auto approves on client_id alone, which is not a secret, so anyone can mint full admin (workers/mcp-server/src/index.ts:2675, TASKS.md T1.11); A live ManyRequests token is hardcoded at workers/mcp-server/src/index.ts:97 and is now in git history (TASKS.md T1.12)
- **Automated client health scoring** (PARTIAL, 2d): The scorer exists and reads, but nothing recomputes health on a schedule: zero callers (docs/superpowers/audits/2026-09-06-next-surfaces-assessment.json, TASKS.md:103)
- **Client billing page** (PARTIAL, 1d): Manage Billing only console.errors on failure, so the popup fails silently (app/(dashboard)/billing/billing-content.tsx:110); Liam has not decided whether the page belongs in the client nav at all (TASKS.md CT.16)
- **Client tracks page** (PARTIAL, 1d): Linked from nowhere and HTML5 drag only; the client home TrackBoard already tells the same story with buttons (TASKS.md CT.18); Liam has to decide delete or keep
- **Scheduled jobs page and cron delivery** (PARTIAL, 1d): The GitHub Actions TAHI_DASHBOARD_URL repo variable still points at the retired webflow.io host and the workflows send x-cron-secret rather than Authorization Bearer (TASKS.md T1.13)
- **Reviews and testimonial outreach** (PARTIAL, 1d): The Send button only writes outreachStatus; no email and no notification ever leave (app/api/admin/reviews/route.ts:74, TASKS.md CT.15); Copy review link builds /review?token= while the page is app/review/[token]/page.tsx, so every copied link 404s (app/(dashboard)/reviews/reviews-content.tsx:151)
- **Studio billing admin** (PARTIAL, 1d): Legacy page: three raw tables, no shared primitives (TASKS.md T2.9)
- **Announcements and banners** (PARTIAL, 1d): The email fan out honours per user preferences in code but has never had a live smoke (STATUS.md, TASKS.md W-QA)

Two production blockers sit outside any surface row and should be treated as MVP work even though the definition does not name them: the MCP worker /authorize hole (TASKS T1.11, anyone holding the client id can mint full admin) and the manual GitHub environment approval on every deploy.

## Live today (28 surfaces)

Client portal home, Client requests: list, detail, new, thread, files, approve, Client files browser and upload, Client invoices and pay, Notifications page and bell, Messages inbox, both audiences, Client account and organisation settings, Studio requests: list, board, timeline, detail, AI triage, Clients list, Client detail, nine tabs, Contact and brand detail pages, Capacity planning, Docs hub, Studio settings, eight groups, Audit log viewer, Design system catalogue, Leads, AI scoring and enrichment, Discovery and client calls, Sales analytics, Studio invoice detail, Stripe and Xero invoicing rails, Time tracking and timers, Financial reports, Studio services catalogue editor, Sitemap planner, Social, Buffer read only, Client view, act as client and private mode, AI wizards: request, task, triage, draft reply, predictive autofill.

## Recommended order for the week

1. You: set the ManyRequests token secret, run /design-login, fill Getting paid. Ten minutes total.
2. Me: dry run the import on staging, read the four checks, apply, verify zero sends, then production; archive the ten dummy orgs, hard-delete the e2e fixtures, hand you the merge list for the fourteen uncertain orgs.
3. Me: write the staged designs back to Claude Design and port the Services ladder, the studio invoices list, the consistency pass and Stalled as a flag.
4. Me: the MVP list above in days order (services org scoping, tasks live lap and My week days, invoice numbers, onboarding write paths, the time rate), then the full 375 client lap and the cross-org isolation proof.
5. You: Giant Group go.

## State of the last two streams at the time of writing

- Importer deploy: PENDING (see the run log for the final health probe).
- Local design fixer and write set: PENDING (restarted from cache at 01:04 after a 35 minute hang).

## What landed between 02:00 and 06:00 (all LIVE on production, each through the full gate)

- **Time tracking and timers**: PARTIAL 1d to LIVE 0d. CT.12 and the follow-ups: one writer, rate persisted with the org default, Rate column, logger always a team member.
- **Stripe and Xero invoicing rails**: PARTIAL 2d to LIVE 0d. CT.13 guarded hourly Xero export (idempotent, hourly-only, currency, named refusals, dry run); CT.14 invoice numbers mirrored into the Xero payload.
- **Client account and organisation settings**: PARTIAL 1d to LIVE 0d. T1.14 one portal admin rule on every write path, profile returns isAdmin; AR.8 preview shows the portal as the primary contact.
- **Studio invoice detail**: PARTIAL 2d to LIVE 0d. T2.10 composed from the shared primitives, every action kept; invoice number as the title.
- **Studio invoices list**: PARTIAL 3d to PARTIAL 1d. CT.14 real invoice numbers from the settings prefix, unique, dry-run backfill of 93 rows waiting on Liam.
- **Client invite, onboarding and provisioning**: PARTIAL 2d to PARTIAL 1d. Clerk webhook links second-seat users to contacts (svix verified, replay safe); studio lead resolved from the org PM.
- **Client services catalogue**: PARTIAL 3d to PARTIAL 2d. CT.11 scoping: services carry org_id and visibility, the portal fails closed.
- **Studio services catalogue editor**: PARTIAL 1d to LIVE 0d. CT.11 scoping: Audience and Visibility controls, Private to badge, MCP parity.
- **ManyRequests reconciliation, import and cutover**: PARTIAL 4d to PARTIAL 3d. Importer LIVE with migration 0093; reconciliation and runbook in the import report.

Each of these deployed green with the health probe (sign-in 200, unauthenticated overview 307) and a production smoke: a time entry with a typed rate round-tripped under the right team member, the Xero export dry run answered with named refusals and no call, the preview shows the portal as Liam (client test) with admin rights, the unsigned Clerk webhook answers 503 until the secret exists, the invoice number backfill dry run scanned 124 rows and would fill 93, the services catalogue and the invoice detail rendered clean at desktop, 375 and dark on the QA server.

Operator steps that unlock the rest, all yours: the ManyRequests token secret, the Clerk webhook secret and endpoint, /design-login for the design write-back, the invoice number backfill apply, Getting paid bank details, the Resend sending domain, and the Giant Group go.

