{
 "results": [
  {
   "parameter": "unused",
   "summary": "A client Services showcase already exists and ships today (`components/tahi/portal/services/portal-services.tsx`, 565 lines, plan panel + catalogue cards + Ask sheet). MR.7 (TASKS.md:161) asks for a brand-forward redesign of exactly that surface, and CL.3 (TASKS.md:167) adds tasteful upsell on top; CL.3 is blocked on CT.11 (TASKS.md:93, the catalogue is global with no orgId).\n\nWhat a redesign can actually draw on, in four buckets:\n\n1. CATALOGUE. `db/schema.ts:1811` `services` is thin: name, description, price (cents), currency, isRecurring, recurringInterval, showInCatalog, category ('service'|'topup'|'addon'). No orgId, no outcome/inclusions/timeline/imagery, no \"who it is for\". `lib/portal-service-view.ts` already synthesises those from the one free-text `description` field by convention (first paragraph = outcome, `- `/`* `/`\u2022 ` lines = inclusions, a `Timeline:` line = timeline) and derives a delivery badge (Ongoing / Add on / Top up / Project) so no price is ever shown. The portal route (`app/api/portal/services/route.ts:31-44`) deliberately projects away price and currency. Anything richer than that (hero image, \"who it is for\", from-price) needs a schema change, which is CT.11's natural home.\n\n2. PLAN MODEL. Two competing sources. The REAL one is the settings-K/V plan catalogue: `lib/plan-catalog.ts:36` `loadPlanCatalog` merges `plan_catalog` JSON over `DEFAULT_PLAN_COPY` (`lib/plan-catalog-shared.ts:43`), giving `{id, name, tag, feats[], rec, monthlyRate, trackRate}` \u2014 admin-edited in Settings > Client plans. It ships only Maintain (NZ$1,500 base / $1,000 track) and Scale ($4,000 / $1,500). `lib/plan-utils.ts:172` `getPlanLabel` knows six plan ids (maintain, scale, tune, launch, hourly, custom) but only maintain and scale have prices, Stripe products (`lib/stripe-plans.ts:80`), track entitlements or catalogue copy. Tune/Launch/Hourly/Custom exist only as labels and dropdown options. Add-ons live in `lib/billing.ts:44` `CYCLE_BUNDLED_ADDONS` (monthly none, quarterly seo_dashboard, annual seo_dashboard + extra_track + priority_support) valued at `ADDON_VALUES` (150 / 500 / 350).\n\n3. USAGE DATA PER CLIENT. Available: tracks and occupancy (`/api/portal/capacity`, `/api/portal/tracks`), requests delivered in the last 30 days with `deliveredAt` + `createdAt` (capacity route lines 81-101, which `lib/track-stats.ts:39` turns into count + average turnaround days), the full delivered history via `/api/portal/requests?status=delivered&limit=500`, recent files (`/api/portal/files`), upcoming calls (`/api/portal/calls`), and the activity pulse (`/api/portal/activity`). NOT available: time logged. `timeEntries` appears in 17 admin routes and zero portal routes. The richest usage model in the codebase (`/api/admin/reports/retainer-health`, which computes utilizationPct, requestsLast30d/90d, churnRiskScore and an explicit `upsellSignal`) is admin-only and would need a portal twin.\n\n4. THE BRAND-FORWARD LOOK. Sign-in (`components/tahi/auth-shell.tsx`) and onboarding (`components/tahi/onboarding-shell.tsx`) share one scene: an always-dark forest panel with a canvas neon leaf, grain, bloom, wordmark, pill, headline, testimonial and a trust avatar row, next to a light card. Onboarding's plan step (`components/tahi/onboarding-content.tsx:516-546`) is the closest existing thing to what MR.7 wants: outcome line, big price, feature ticks, a \"Recommended\" pill, an add-on toggle with its own price, and a value anchor line (\"Hiring the equivalent specialists separately runs $5,200 to $17,500+ a month\"). Both scenes hardcode their colours by design (like the sidebar), unlike the current Services page which is all CSS-var tokens.\n\nThe upsell plumbing already exists and does not need inventing: `POST /api/portal/subscription/change-request` (plan or extra tracks, notifies every admin, writes audit, never mutates billing), the change-plan grid in `components/tahi/settings/sections/plan.tsx:341-350`, `getUpgradeGhostTracks` (`lib/plan-utils.ts:216`) which already writes the Maintain\u2192Scale and priority-support upsell copy, and `PortalAskSheet` (`components/tahi/portal/portal-money-kit.tsx:315`) which files a real request or falls back to mailto. What is missing for CL.3 is the brief (Liam owes it), per-org scoping, and any notion of \"who this service is for\".",
   "facts": [
    "The client Services showcase already exists and is live: components/tahi/portal/services/portal-services.tsx:84 `PortalServices`, 565 lines, shipped in PP.3 (TASKS.md:149). It renders three things: a PlanPanel (line 344), a filterable catalogue of ServiceCards (line 480), and one PortalAskSheet (line 287). Its file header (lines 3-21) states the rules: no prices anywhere, one soft ask per card plus one at the foot, no create/edit/price field on the surface.",
    "app/(dashboard)/services/page.tsx:19-26 is the audience branch: `requirePageAnyGrant()` for admins, `requirePageFeature('services')` for clients; an admin previewing with the `tahi-impersonate-org` cookie gets `<PortalServices preview />` instead of the admin editor (this was the CL.2 fix, TASKS.md:166).",
    "db/schema.ts:1811 `services` table columns: id, name, description, price (integer, CENTS, default 0), currency (default 'NZD'), isRecurring, recurringInterval, showInCatalog (default 1), category ('service' | 'topup' | 'addon'), timestamps. There is NO orgId, no image, no 'who it is for', no inclusions and no timeline column.",
    "app/api/portal/services/route.ts:31-44 projects only id, name, description, category, isRecurring, recurringInterval; price and currency are deliberately excluded so a catalogue price can never read as a quote. Filter is `showInCatalog = 1` (line 41), order is alphabetical by name (line 44).",
    "app/api/portal/services/route.ts:20-21 documents the CT.11 gap in a comment: \"`services` has no orgId, so every client sees every catalogue row. Org scoping is CT.11 and needs a schema change.\"",
    "lib/portal-service-view.ts is the whole card-shaping layer and is pure + unit-tested (lib/__tests__/portal-money-view.test.ts:163-223). parseServiceDescription (line 97) splits the single `description` field by convention: first paragraph -> outcome, lines starting `-`/`*`/`\u2022` -> inclusions, a line starting `Timeline:` -> timeline. serviceDelivery (line 137) maps category 'addon'/'topup' directly, else isRecurring -> 'ongoing' | 'project'. SERVICE_DELIVERY_COPY (line 42) holds the four badge labels, tones and consequence hints.",
    "lib/plan-catalog-shared.ts:43 DEFAULT_PLAN_COPY ships only two plans: maintain (base 1500, track 1000, tag 'Steady upkeep, handled.', 4 feats) and scale (base 4000, track 1500, rec: true, tag 'Ongoing design & build, handled.', 4 feats). PLAN_TRACK_RATES at line 19 mirrors the same numbers.",
    "lib/plan-catalog.ts:36 `loadPlanCatalog` reads the settings K/V key `plan_catalog` (PLAN_CATALOG_KEY, line 16 of the shared file), sanitises it, and falls back to lib/billing PLAN_MONTHLY_RATES for any missing base price. This is the admin-editable catalogue behind Settings > Client plans (components/tahi/settings/sections/plans-retainers.tsx:187, gated superAdminOnly at settings-shell.tsx:95).",
    "lib/plan-utils.ts:172 `getPlanLabel` recognises six plan ids: maintain, scale, tune, launch, hourly, custom. Only maintain and scale have a price anywhere: lib/billing.ts:31 PLAN_MONTHLY_RATES, lib/stripe-plans.ts:80 STRIPE_PLANS, lib/plan-catalog-shared.ts:43, lib/utils.ts:31 PLAN_MRR, and lib/plan-utils.ts:22 getTrackEntitlements all cover maintain/scale only. Tune, Launch, Hourly and Custom exist only as labels (lib/utils.ts:20) and dropdown options (app/(dashboard)/clients/[id]/_kit/subscription-card.tsx:32, new-client-panel.tsx:51 which prices Tune at 'NZ$750 one-off').",
    "lib/billing.ts:44 CYCLE_BUNDLED_ADDONS: monthly [], quarterly ['seo_dashboard'], annual ['seo_dashboard','extra_track','priority_support']. ADDON_VALUES (line 51): seo_dashboard 150, extra_track 500, priority_support 350 NZD/month. calculateBundledSavings (line 60) sums them; calculateGst (line 14) charges 15% only when country === 'NZ'.",
    "lib/plan-utils.ts:22 getTrackEntitlements is the capacity model: maintain -> 1 small track (2 with priority support), scale -> 1 large + 1 small (2 large + 1 small with priority support), anything else -> zero. resolveTracksConfig (line 90) lets a per-client override (organisations.tracksMode 'auto'|'custom'|'off', customSmallTracks, customLargeTracks, MAX_TRACKS 4) win over the plan default.",
    "lib/plan-utils.ts:216 `getUpgradeGhostTracks` ALREADY writes the plan-aware upsell copy CL.3 asks for, as {type, headline, subline, cta}: a Maintain client without priority support sees 'Run two projects at once' + 'Take on bigger builds / Upgrade to Scale'; a Scale client without it sees 'Two big projects in parallel'; Scale + priority support returns [] (top tier). It is only consumed by app/(dashboard)/tracks/tracks-content.tsx:39, and the header comment at line 201 says to 'Lead with the capability, not the price (per the Services & Pricing doc)'.",
    "app/api/portal/subscription/route.ts:23 GET returns clientType ('retainer' | 'project'), the subscription (planType, planLabel, monthlyRate, trackCount, nextInvoiceDate = currentPeriodEnd, createdAt, addonDetails, hasPrioritySupport, hasSeoAddon), a billing block (cycleMonths, cycleTotal, monthlySavings, cycleSavings, gst) and the full `plans` catalogue. Line 37: financial data is workspace-admin-only via isOrgAdmin, so a member seat gets a 403 and the Services plan panel simply is not rendered (portal-services.tsx:104-110 treats the 403 as a rule, never an error).",
    "app/api/portal/subscription/change-request/route.ts:26 is the existing upsell write path: POST {kind:'plan'|'tracks', targetPlanId|targetTracks, note}. It never mutates the subscription; it validates the target against loadPlanCatalog (line 64), notifies every admin and writes an audit entry. Gated to contacts with portalRole === 'admin' (line 43) and 403s under impersonation (line 31).",
    "app/api/portal/capacity/route.ts:81-101 is the only usage feed with delivered work: requests delivered in the LAST 30 DAYS (isInternal = false), plus tracks, queue, entitlements, a `summary` string and `showGhosts`. lib/track-stats.ts:39 trackDeliveredStats turns that into {count, avgTurnaroundDays} and lib/track-lanes.ts:58 bucketTracks buckets it into Up next / In progress / Review / Delivered lanes.",
    "Full delivered history IS reachable from the portal without new work: app/api/portal/requests/route.ts:86-89 supports `?status=delivered` (any exact status, plus 'active' and 'all') with `limit` up to 500, and the projection includes deliveredAt, createdAt, estimatedHours, revisionCount and category, so requests-delivered-per-month and turnaround can be computed client-side.",
    "Time logged is NOT exposed to the portal at all: `timeEntries` appears in 17 routes under app/api/admin/ (time, time-entries, reports/utilization, reports/worklog, clients/[id]/profitability, export/time, ...) and zero routes under app/api/portal/. Surfacing hours to a client is a new endpoint plus a policy decision.",
    "app/api/admin/reports/retainer-health/route.ts:18-24 already computes the per-client usage story the showcase would want: monthsActive, openRequests, requestsLast30d, requestsLast90d, billableHoursLast30d, hoursPerMonth, utilizationPct, churnRiskScore and an explicit `upsellSignal` boolean. It is isTahiAdmin-gated and team-scoped, so a portal equivalent would have to be built.",
    "The client home's plan card is components/tahi/overview/homes/client-home.tsx:1598-1636: title 'Your plan', plan label + Active chip, '{monthlyRate}/mo \u00b7 N tracks', sub-rows for Next invoice and Tracks, and an 'Ask about your plan' ghost button that just opens the New request dialog. The comment at lines 1600-1602 records that a 'Manage' link was removed because it pointed at /billing, which is not in the client nav. The project-client twin is at line 1563 ('Your project': phase, next milestone, next invoice).",
    "components/tahi/overview/homes/client-home.tsx:592 TrackBoard is the retainer work view ('Your work in motion', '{planLabel} plan \u00b7 N tracks running in parallel', reorderable queue). The masthead at line 1294 already prints '{orgName} on the {planLabel} plan'.",
    "components/tahi/auth-shell.tsx:1-25 documents the brand-forward auth scene: a two-column split, an always-dark forest panel (58% desktop) with hardcoded colours, drifting glows, film grain, slow sheen, wordmark, pill, headline, subcopy, a peer testimonial and a trust avatar row (TAHI_TRUST_AVATARS, line 330), next to a light-pinned card holding the Clerk widget (tahiClerkAppearance, line 503). app/(auth)/sign-in/[[...sign-in]]/page.tsx:11-28 supplies the copy, including a real Physitrack testimonial and 'Trusted by some of the biggest companies.'",
    "components/tahi/onboarding-shell.tsx:1-22 reuses that scene with the split flipped (forest 42% / card 58%) and exports the reusable pieces: SceneShell (line 198), Ledger (line 235, the left step list), Stepper (line 249, mobile dots), and ONBOARDING_CSS (line 385). Both shells hardcode their scene colours on purpose, like the sidebar.",
    "components/tahi/onboarding-content.tsx:516-546 is the existing brand-forward plan presentation and the closest model for MR.7: a radiogroup of plan cards each with an outcome line, a large price, '+ tax where it applies', feature ticks and a Recommended pill; then an add-on toggle for the parallel track with its own '+$X /mo'; then the value anchor at line 71 ('Hiring the equivalent specialists separately runs $5,200 to $17,500+ a month. This is one calm line item, change it anytime.').",
    "components/tahi/onboarding-content.tsx:67-70 hardcodes its own PLANS array rather than reading the plan_catalog. It duplicates DEFAULT_PLAN_COPY exactly today, but an admin edit in Settings > Client plans would change the Services page and the portal Plan tab while leaving onboarding stale. lib/stripe-plans.ts:15 acknowledges the duplication ('Mirrors the plan copy in components/tahi/onboarding-content.tsx').",
    "components/tahi/portal/portal-money-kit.tsx:315 PortalAskSheet is the reusable ask primitive: two modes, 'request' (POST /api/portal/requests with {title, description}, line 362) and 'email' (mailto TAHI_CONTACT_EMAIL = business@tahi.studio, line 352), plus a readOnly path for impersonation. portal-services.tsx seeds it per card with the delivery hint as the subtitle so the CTA cannot overpromise.",
    "The client portal Plan & billing tab (components/tahi/settings/sections/plan.tsx, registered at settings-shell.tsx:105 with audience 'client') already renders the full catalogue as a change-plan grid with a 'Most popular' badge and a current-plan state (lines 341-350), an extra-track stepper, and a live price breakdown, all wired to the change-request endpoint. Any Services upsell must not duplicate or contradict it.",
    "components/tahi/nav-model.tsx:141 puts Services in the CLIENT_NAV 'Library' group alongside Files, marked clientOnly + clientVisible. lib/feature-tree.ts:63 registers the 'services' feature key with appliesTo ['client'] and route '/services', so an org can be denied the page entirely via feature visibility.",
    "TASKS.md:161 (MR.7) is the design-first ask: 'Services showcase redesigned to be brand-forward like onboarding, home and sign-in: the client's plan, how they are using it, what else Tahi does; then ported over the current showcase.' TASKS.md:167 (CL.3) is the upsell ask: 'what it is, who it is for, from price, what you get', an Ask about this CTA into a pre-filled request or enquiry, plan-aware suggestions (a Maintain client sees Scale; a project client sees a retainer), never a form to create a service. It is explicitly blocked on CT.11 and on Liam's brief.",
    "TASKS.md:93 (CT.11, 3d) is the blocker: '/services: the portal catalogue is unscoped (every client sees every client's custom retainer names and prices once ManyRequests' services are imported); scope per org, then an order path. Liam decides whether ordering mints a request or a Stripe checkout.'",
    "docs/superpowers/plans/2026-09-06-morning-report.md:45 records the open question verbatim: 'Services upsell brief (CL.3): the showcase is live without pricing pressure; tell me how hard to sell.'",
    "STATUS.md:77 still lists /services as a legacy route with zero primitives, and TASKS.md:184 (T2.8) is a separate '/services v3 lap (client catalogue; worst legacy page)'. Both refer to the ADMIN branch (services-content.tsx, raw tables and hand-rolled dialogs); the client branch was rebuilt on primitives in PP.3, so T2.8 and STATUS.md line 77 are stale for the portal half.",
    "An order path exists for retainers only: POST /api/portal/checkout (app/api/portal/checkout/route.ts:22) creates a Stripe subscription inline for plan 'maintain'|'scale' with an optional track add-on, in one of six presentment currencies (lib/stripe-plans.ts:32). Nothing in the catalogue can be bought: there is no per-service Stripe price, no order endpoint and no buy CTA."
   ],
   "deadEnds": [
    "The admin services catalogue is create-and-list only. app/api/admin/services/ contains just route.ts (GET line 10, POST line 26) and coupons/route.ts. There is no /api/admin/services/[id], so no PATCH and no DELETE: a service cannot be edited, hidden or removed after creation. The 'visible to clients' control at app/(dashboard)/services/services-content.tsx:212 is a coloured dot with a title attribute, not a toggle; showInCatalog can only be set once, in the create dialog (line 618).",
    "Coupons are write-only and orphaned. app/api/admin/services/coupons/route.ts has GET/POST/DELETE against settings rows prefixed `coupon_`, but a repo-wide grep for 'coupon' outside that route and services-content.tsx returns nothing except subscriptions.referralCouponId. Nothing redeems a coupon, and the portal never sees one. Matches the ship-readiness audit line 152.",
    "There is no MCP tool for the Tahi services catalogue on the worker. workers/mcp-server/src/index.ts only proxies ManyRequests' services (lines 2834 and 2837). Any new services capability trips CLAUDE.md rule 14 (MCP parity) on arrival.",
    "/tracks is built but unreachable for clients. app/(dashboard)/tracks/page.tsx + tracks-content.tsx render the per-track mini-kanban with ghost upsell cards, but Tracks is not in CLIENT_NAV (components/tahi/nav-model.tsx:125-152). The client only sees track data through the home's TrackBoard. So getUpgradeGhostTracks currently renders to nobody.",
    "The `services` table cannot answer CL.3's brief as it stands: 'who it is for' and 'from price' have no column, and the price it does have is a single flat cents value with no 'from' semantics. lib/portal-service-view.ts's description-parsing convention is an explicit stopgap (its header says 'none of that can land tonight (no schema change, no migration)'), not a design to build on.",
    "No portal endpoint exposes hours, utilisation or any per-month usage rollup. 'How they are using it' has to be assembled client-side from /api/portal/capacity (30-day window) and /api/portal/requests?status=delivered, or a new portal report route has to be written. The admin retainer-health endpoint cannot be reused directly: it is isTahiAdmin-gated and iterates every org.",
    "Do not model the showcase's plan copy on components/tahi/onboarding-content.tsx:67 PLANS. That array is hardcoded and does not read the plan_catalog settings key, so it silently diverges from what Settings > Client plans publishes. The server-side source of truth is lib/plan-catalog.ts loadPlanCatalog, surfaced to the client via /api/portal/subscription's `plans` field.",
    "Tune, Launch, Hourly and Custom cannot be shown as purchasable plans. They exist as labels in lib/plan-utils.ts:172 and lib/utils.ts:20 and as admin dropdown options, but they have no catalogue entry, no monthly rate, no track entitlement (getTrackEntitlements returns all zeros) and no Stripe price. A client on one of those plan types renders 'No plan'-shaped capacity today.",
    "PP.5 (TASKS.md:151) was reverted on 2026-09-06 after every signed-in page 500'd in production (digest 1095162490). Its content included 'client money pinned to the client's currency with no switcher'. So the Services plan panel still formats the monthly rate through useDisplayCurrency (portal-services.tsx:357), and any redesign should not assume the currency-pinning behaviour landed.",
    "STATUS.md:77 and TASKS.md:184 (T2.8) both describe /services as legacy with zero primitives. That is now only true of the ADMIN branch; the client branch was rebuilt on PageHeader / Card / Badge / EmptyState / TahiButton in PP.3. Reading either line as a description of the client showcase will send work in the wrong direction."
   ]
  },
  {
   "summary": "The studio invoices surface is two big client components (invoice-list.tsx 1381 lines, invoice-detail.tsx 1348 lines) sitting behind server pages that fork admin vs portal at the top (`app/(dashboard)/invoices/page.tsx:27`, `[id]/page.tsx:25`) \u2014 a Tahi login with the `tahi-impersonate-org` cookie is routed to the portal components, so the studio branch never has to render a client view (the `!isAdmin` code paths inside both files are now dead weight for the admin surface but still shape the columns and mobile cards).\n\nLIST: PageHeader with three actions (Export CSV, Import from Stripe, Create invoice), a FilterBar (search + Status multiselect + Source multiselect) plus a hand-rolled inline \"Due:\" date-range pair, and a `DataTable` with six admin columns (Client, Amount, Status, Source, Due, Created). No row actions, no selection, no bulk bar, no pagination \u2014 it fetches `?status=all` (server caps at 50) and filters entirely client-side. The New Invoice slide-over is the sophisticated part: IC.3 landed a resolver (`lib/invoice-defaults.ts`) that lands destination/currency/due-date from the picked client once per pick, with a three-state \"known / pending / unknown\" rail model and two different warning sentences depending on whether the client names a rail or inherits the studio default.\n\nDETAIL: breadcrumb, a big-amount header card with a status pill, an eight-slot metadata grid, admin pay-page links (Stripe + Xero side by side), a flat row of up to eight admin actions, a persistent push-back outcome line, an AI Chase email card (generate / edit / send / regenerate / dismiss), read-only Notes, and a hand-rolled line-items table with subtotal / GST / discount / total. Feedback is split across three idioms: toast (`showToast`), inline `role=\"status\"` sentences (send result, push-back), and eight native `alert()` / two `confirm()` calls.\n\nBACK END: six routes under `app/api/admin/invoices/**` plus the CSV export and the hourly Xero export. IC.1\u2013IC.5 are shipped and the money semantics are unusually carefully reasoned (paidAt/sentAt normalisation, forward-only Xero mapping, mark-paid push-back to the rail with reported skip reasons, Xero-rail email mode). What is still open is exactly what TASKS.md says: CT.14/IC.7 invoice numbers (no `invoices.number` column, `invoice_number_prefix` has zero readers, three different renderings of \"the invoice number\" in the codebase) and CT.13/IC.6 the hourly Xero export guard (no idempotency, no billing-model filter, hardcoded NZD, silent zero-rate skip).\n\nFor a redesign the highest-value structural findings are: (1) Delete Invoice is offered on ANY status including paid, and the route has no draft-only guard despite its own comment, so one click can erase revenue /financial-reports keys off `paid_at`; (2) a `viewed` invoice has no Mark as Paid button and a `paid` or `written_off` invoice has no un-do; (3) nothing on the page can edit amounts, line items, due date or notes after creation; (4) Export CSV ignores every active filter and, unlike the list GET, applies no team-member org scoping; (5) `xero-sync` applies no org scoping at all; (6) the invoices row already carries `reconciliationStatus` / `airwallexTxnId` that no invoice surface reads.",
   "facts": [
    "ROUTING: app/(dashboard)/invoices/page.tsx:22 computes `previewing` from the `tahi-impersonate-org` cookie server-side (deliberately a prop, not the per-tab client store), and :27 returns `<PortalInvoiceList preview={previewing}/>` for `!isAdmin || previewing`, so `InvoiceList` at :29 is studio-only.",
    "ROUTING: app/(dashboard)/invoices/[id]/page.tsx:25 does the same fork to `<PortalInvoiceDetail invoiceId={id} preview={previewing}/>`; `InvoiceDetail` at :27 is studio-only. Portal counterparts live at components/tahi/portal/invoices/portal-invoice-list.tsx (744 lines) and portal-invoice-detail.tsx (743 lines).",
    "DEAD BRANCH: both studio components still carry full `!isAdmin` code paths (invoice-list.tsx:882 `isAdmin = isAdminProp && !isImpersonatingClient`, the Pay column at :1133-1152, InvoiceMobileCard at :149-212, the portal-denial branch at :1173-1186; invoice-detail.tsx:191, the client pay CTA at :423-461, the denial branch at :274-290). Since the server pages already fork, none of it can render for the studio audience.",
    "STATUS VOCABULARY (list): app/(dashboard)/invoices/invoice-list.tsx:79-86 `STATUS_TONE` = draft/neutral, sent/warning, viewed/info, overdue/danger, paid/positive, written_off/neutral (label 'Written Off').",
    "STATUS VOCABULARY (detail): app/(dashboard)/invoices/[id]/invoice-detail.tsx:73-80 `STATUS_CFG` uses DIFFERENT tokens for the same six statuses: draft -> --status-draft-*, sent AND viewed -> --status-submitted-*, overdue -> --color-danger-*, paid -> --color-success-*, written_off -> --status-archived-*. The list uses `Badge tone=` and the detail hand-rolls a pill at :385-398, so the two surfaces cannot be styled together.",
    "EFFECTIVE STATUS: invoice-list.tsx:104-111 and invoice-detail.tsx:96-104 each define their own copy of `isOverdue` + `effectiveStatus` \u2014 'overdue' is derived (status === 'sent' AND dueDate < now), never stored by the UI. Both exclude paid and written_off from the overdue test.",
    "SOURCE BADGE: app/(dashboard)/invoices/source-badge.tsx:10-14 is the shared map \u2014 manual/'Manual'/neutral, xero/'Xero'/teal, stripe/'Stripe'/purple, with null falling back to Manual (:17). Its own header comment records that the list and detail had drifted before it existed.",
    "LIST COLUMNS (admin): invoice-list.tsx:1031-1155 builds six columns \u2014 Client (:1035, sortable, minWidth 14rem, links to /clients/{orgId}), Amount (:1058, right-aligned, 10rem, shows a display-currency conversion line when currency !== displayCurrency), Status (:1079, 8rem, StatusBadge), Source (:1088, admin-only, 7rem, SourceBadge), Due (:1099, 8rem, red when overdue), Created (:1115, 8rem, muted). Default sort createdAt desc (:1345).",
    "LIST COLUMNS: there is NO invoice number column, no paid-date column, no owner, no line-item count, no row-actions menu and no per-row action of any kind for the admin audience. The only non-data column (`pay`, :1133-1152) is gated behind `!isAdmin`.",
    "AMOUNT CONVERSION IS FRAGILE: invoice-list.tsx:1072 renders the secondary currency line as `formatNativeWithDisplay(...).split('\u2248 ')[1] ?? ''` \u2014 string-splitting a formatted string on a literal approximately-equals. invoice-detail.tsx:381 does the same.",
    "LIST FILTERS: invoice-list.tsx:1001-1027 `filterDefs` \u2014 a Status multiselect (six options, tones matching the row badges) and an admin-only Source multiselect (Manual/Xero/Stripe). Both `nonRemovable: true` so they are always visible without the '+ Add filter' affordance.",
    "LIST FILTERING is client-side only: invoice-list.tsx:953-987 filters the already-fetched array by effective status, source (null mapped to 'manual' at :967), date range and a search over org name + raw invoice id (:979-983).",
    "NO PAGINATION: invoice-list.tsx:920-922 fetches `/api/admin/invoices?status=all` once; app/api/admin/invoices/route.ts:28 hardcodes `limit = 50` with a `page` param the FE never sends. The comment at invoice-list.tsx:918-919 admits it. Filtering a 'Paid' chip therefore only searches the newest 50 invoices.",
    "FILTER PERSISTENCE IS LOSSY: invoice-list.tsx:891-895 persists `invoices.activeTab` through `useUserPreference` with a single-value `oneOf` validator, and :914 writes back only `status[0]`. Selecting two statuses persists one; the second is silently lost on reload.",
    "DEAD STATE: `sourceFilter` (invoice-list.tsx:896) is written at :915 and read only at :905 to seed `activeFilters` on mount. `activeTab` is likewise never read by the filter (which reads `activeFilters` directly at :954). Both are write-only after first render.",
    "DATE RANGE is hand-rolled, not a DS primitive: invoice-list.tsx:1264-1312 renders two bare `<input type=\"date\">` at 1.875rem height with a text 'Clear' link. `DateRange` is imported from components/tahi/date-range-picker (:11) for the type only. The comment at :1264-1265 says FilterBar has no date kind yet.",
    "DATE RANGE LOGIC BUG: invoice-list.tsx:972 requires BOTH `from` and `to` before filtering, so a from-only or to-only selection silently does nothing; and it filters on `inv.dueDate ?? inv.createdAt` while the label reads 'Due:' (:1267).",
    "LIST ACTIONS \u2014 Export CSV: invoice-list.tsx:1196-1208 creates an anchor to `/api/admin/export/invoices` with no query string, so the export IGNORES the active status/source/date/search filters even though app/api/admin/export/invoices/route.ts:20-32 accepts status, dateFrom and dateTo.",
    "EXPORT SCOPING GAP: app/api/admin/export/invoices/route.ts:11-34 checks `isTahiAdmin` + `requireFeature('invoices')` but never calls `getOrgScope` or `requireAccessToOrg`, unlike app/api/admin/invoices/route.ts:35-50. A scoped team member who cannot see a client in the list can export that client's invoices.",
    "EXPORT COLUMNS: app/api/admin/export/invoices/route.ts:51 header is `Invoice ID,Client,Status,Amount,Currency,Due Date,Created` \u2014 raw UUID, no source, no paid date, no invoice number.",
    "LIST ACTIONS \u2014 Import from Stripe: invoice-list.tsx:1209-1235 POSTs `/api/admin/integrations/stripe/import-invoices`, toasts `Stripe: N imported, N updated, N skipped` and calls `handleCreated()` to revalidate. Spinner via `importing` state (:886).",
    "IMPORT PERMISSION MISMATCH: app/api/admin/integrations/stripe/import-invoices/route.ts:20 gates on `requireFeature('settings.integrations')`, while every other control on the invoices page gates on `'invoices'`. A seat with Invoices but not Integrations sees a button that 403s.",
    "LIST ACTIONS \u2014 Create invoice: invoice-list.tsx:1236-1242 opens the slide-over; on success `handleCreated` (:989-996) routes to `/invoices/{id}` when an id came back, otherwise just revalidates.",
    "NO BULK ACTIONS: components/tahi/data-table.tsx:201-205 supports `selectable` / `selectedIds` / `onSelectionChange` and components/tahi/bulk-action-bar.tsx exists and is used by clients, requests and tasks \u2014 the invoices list uses none of it (invoice-list.tsx:1340-1369 passes no selection props).",
    "SLIDE-OVER SHELL: invoice-list.tsx:477-485 `SlideOver` icon=FileText, title 'Create invoice', subtitle 'Generate a new invoice in the dashboard, Xero or Stripe.', maxWidth 48rem.",
    "DESTINATION CONTROL: invoice-list.tsx:471-475 `destOptions` = manual/'Dashboard only', xero/'Xero draft', stripe/'Stripe link', rendered as a `SegmentedControl role=\"radiogroup\"` at :546-553 (size='sm'). The long comment at :532-545 records that Badge chips painted an inline box-shadow that outranked .tahi-focus-ring, so the selected chip had no focus ring.",
    "IC.3 DEFAULTS: invoice-list.tsx:304-315 calls `resolveChannelDefaults(selectedOrg.invoiceChannel, studioDefaultChannel, {loading, failed})` from lib/invoice-defaults.ts:79-92, which returns `{clientChannel, effectiveChannel, pending, known}`. `known` goes false when the client names no rail AND the settings read failed, and the effect at :353 then deliberately leaves the destination on 'Dashboard only'.",
    "IC.3 APPLY-ONCE: invoice-list.tsx:338 `defaultsAppliedFor` is a ref holding the org id (not a boolean) so switching client re-applies but typing never does; :340-356 waits for `studioDefaultPending` before landing anything.",
    "IC.3 NOTE COPY: invoice-list.tsx:603-607 renders `Defaults from <client>: <Channel>, <Terms>, <Currency>.`; the override warning at :590-600 says either `<client> usually bills through <Channel>.` (client names a rail) or `No channel set for <client>. The studio default is <Channel>.` The live region wrapper at :563 is mounted unconditionally so SRs announce the change.",
    "IC.3 OVERRIDE TEST: invoice-list.tsx:359-360 `destinationOverridesClient` fires only when the rail is known, destination !== 'manual' and destination !== effectiveChannel \u2014 'Dashboard only' never warns because it pushes nowhere.",
    "STRIPE PRE-FLIGHT: invoice-list.tsx:269-276 fetches `/api/admin/clients/{orgId}/contacts` with `keepPreviousData:false` and computes `orgHasEmailContact` as null|true|false; :509-524 renders an amber warning and :387-390 blocks submit with 'has no contact with an email... Add one on the client's Contacts tab before creating a Stripe link.'",
    "WARNING TOKENS: invoice-list.tsx:513-518 and :577-579 deliberately use `--badge-warning-bg/-border/-text` rather than `--color-warning-bg`/`--color-warning`, because globals.css only re-resolves the badge pair for dark mode.",
    "CLIENT PICKER: invoice-list.tsx:613-691 is a hand-rolled search + absolute dropdown (not a DS combobox): a text Input at :641, a z-index-10 panel at :650-688 with hover-only mouse handlers (:675-676) and no keyboard navigation; once picked it becomes a brand-tinted chip with an X at :615-638.",
    "LINE ITEMS EDITOR: invoice-list.tsx:693-789 \u2014 a currency `Select` sits in the section header (:697-702, options from SUPPORTED_CURRENCIES at :88 = NZD, USD, AUD, GBP, EUR), then rows of Description (flex 3) / Qty (4.5rem) / Amount (7rem) with an X remove button only when length > 1 (:752-770), plus a '+ Add line item' text button (:773-788). No running total is shown anywhere in the form.",
    "FORM TAIL: invoice-list.tsx:792-814 is a `1fr 2fr` grid of Due date (type=date) and Notes (Textarea rows=3, placeholder 'Optional notes for the client...').",
    "SUBMIT LABEL varies by rail: invoice-list.tsx:829-833 \u2014 'Create invoice' / 'Create Xero draft' / 'Create + get payment link'.",
    "CREATE FAN-OUT: invoice-list.tsx:394-409 POSTs /api/admin/invoices (carrying `source: destination`), then :418-428 POSTs xero-sync with `{invoiceIds:[id]}` (toast 'Invoice created as Xero draft' / 'Invoice created (Xero sync failed)'), or :429-457 POSTs stripe-create, copies `payUrl` to the clipboard and toasts, surfacing the real Stripe error inline on failure so the saved draft can be retried.",
    "DETAIL BREADCRUMB: invoice-detail.tsx:337-342 renders `INV-${invoiceId.slice(0,6).toUpperCase()}` \u2014 six characters.",
    "DETAIL HEADER: invoice-detail.tsx:363-384 shows org name (0.8125rem muted) above a 2.25rem/700 total with an optional display-currency line, and the status pill at :385-398.",
    "METADATA GRID: invoice-detail.tsx:401-419 is `repeat(auto-fill, minmax(160px,1fr))` with eight possible slots \u2014 Invoice ID (id.slice(0,8).toUpperCase(), :411), Created (:412), Due Date (red when overdue, :413), Sent (conditional, :414), Paid (conditional, :415), Stripe ID (conditional, :416), Xero ID (first 8 chars, :417), Source (SourceBadge, :418).",
    "ADMIN PAY PAGES: invoice-detail.tsx:472-490 renders both `Client pay page` (Stripe hosted, :484) and `Xero pay page` (:485) side by side with the caption 'What the client sees when they pay.' The comment at :467-471 explains that an empty Xero slot on a Xero-rail invoice is the one-glance answer to 'why has the client not paid this' (the push holds invoices at DRAFT).",
    "PAY URL RESOLUTION: invoice-detail.tsx:325 `payUrl = invoice.payUrl ?? invoice.stripeHostedInvoiceUrl`; :330 `xeroPayUrl = invoice.xeroOnlineInvoiceUrl`. The admin projection returns both columns separately, the portal projection folds them into one `payUrl` (lib/invoice-how-to-pay.ts:149-157 `resolveInvoicePayUrl`).",
    "ACTION 1 \u2014 Email/Resend: invoice-detail.tsx:509-516 mounts `SendInvoiceEmailButton` only when status is neither paid nor written_off; it is `primary` (brand-filled) when status === 'draft', label 'Email to client', otherwise ghost 'Resend email' (:1015).",
    "ACTION 2 \u2014 Mark as Paid: invoice-detail.tsx:517-524, shown only for `invoice.status === 'sent' || 'overdue'`. A `viewed` invoice therefore has NO mark-paid control, even though 'viewed' is a first-class status in both badge maps and the Status filter chip.",
    "ACTION 3 \u2014 Revert to Draft: invoice-detail.tsx:525-532, hidden for draft, written_off AND paid. So a mistakenly-marked-paid invoice cannot be un-done from the UI, although PATCH supports it (app/api/admin/invoices/[id]/route.ts:483-492 nulls paidAt when moving back into UNWINDS_PAYMENT).",
    "ACTION 4 \u2014 Void Invoice: invoice-detail.tsx:533-544, danger variant, native `confirm('Void this invoice? This will also void it in Xero if linked.')` then `patchStatus('written_off')`. There is no un-void action anywhere.",
    "ACTION 5 \u2014 Sync to Xero: invoice-detail.tsx:545-566, shown only when `!invoice.xeroInvoiceId && status !== 'paid'`. There is no RE-sync/update path in the UI even though app/api/admin/invoices/xero-sync/route.ts:214-236 supports POST /Invoices/{id} updates.",
    "ACTION 6 \u2014 Create Stripe Link: invoice-detail.tsx:567-599, shown when `status !== 'paid' && !stripeInvoiceId`; copies payUrl to clipboard and alerts, and on failure alerts the raw Stripe message plus a hint about the missing contact email.",
    "ACTION 7 \u2014 Copy Payment Link: invoice-detail.tsx:600-620, shown when `stripeInvoiceId` exists; GETs `/api/admin/integrations/stripe/provision?invoiceId=...` and alerts 'Payment link copied!' / 'No payment link available' / 'Failed'.",
    "ACTION 8 \u2014 Delete Invoice: invoice-detail.tsx:621-637 is ALWAYS rendered, for every status including paid, behind a native confirm. app/api/admin/invoices/[id]/route.ts:580-581 comments 'Only draft invoices can be deleted' but the handler has NO status guard \u2014 it voids in Stripe (:614-629), voids in Xero (:632-639) and hard-deletes items + row (:641-642). Deleting a paid invoice erases revenue that /financial-reports keys off `paid_at`.",
    "ACTION BUTTON VARIANTS: invoice-detail.tsx:841-877 `ActionButton` has four variants but `primary` and `success` are identical (both `--color-brand` fill, :853-854); `ghost` is bg + border; `danger` is bg + danger text/border. minHeight 44 on all.",
    "FEEDBACK IS THREE IDIOMS: toast for mark-paid (invoice-detail.tsx:250), an inline `role=\"status\"` sentence for push-back (:648-660) and for the send result (:1017-1029), and eight native `alert()` + two `confirm()` for Xero sync, Stripe link, copy link, void and delete (:538, 560, 562, 582, 591, 594, 611, 613, 616, 625, 632, 634). components/tahi/confirm-dialog.tsx exists as the DS primitive.",
    "PUSH-BACK OUTCOME MODEL: invoice-detail.tsx:115-163 defines `PushbackOutcome {rail: 'xero'|'stripe', status: 'done'|'skipped'|'failed', reason?}` and `pushbackCopy` returns a short toast plus a persistent `detail` sentence \u2014 the comment at :129-135 records that the toast caps at 22rem, nowraps and self-dismisses at 3.5s, which clipped exactly the half a human can act on. Ink per tone at :173-178 uses badge text tokens, not indicator colours.",
    "PUSH-BACK COPY: 'Marked paid.' (no rail) / 'Marked paid in Xero too.' / 'Marked paid. Xero not told.' + detail 'Marked paid here. Xero was not told: <reason>' / 'Marked paid. Xero not updated.' + 'Xero did not record the payment: <reason>' (invoice-detail.tsx:143-162).",
    "SEND RESULT COPY: invoice-detail.tsx:926-950 `sendResultMessage` builds independent clauses \u2014 'Sent to a@b, c@d.' + 'Could not reach e@f.' + one of 'Xero emailed its own copy too.' / 'Xero emailed this invoice to the client.' / 'Xero did not send its own copy: <reason>' / 'Xero could not send its own copy: <reason>'. Tone is 'partial' when anything failed, never derived from the HTTP status (:964-968).",
    "CHASE EMAIL CARD: invoice-detail.tsx:664-666 mounts `ChaseDraftCard` only when the EFFECTIVE status is sent or overdue. The card (:1049-1348) has a Sparkles header 'Chase email', an unposed state with the pitch copy at :1170-1173 and a 'Draft chase email' button, then Subject input + 10-row Body textarea, and three buttons: 'Send chase' (:1272-1295), 'Regenerate' (:1296-1317), 'Dismiss' (:1318-1337), plus a token counter at :1338-1342. Success banner reads 'Chase sent to {email}.' (:1164).",
    "CHASE WIRING: GET/POST `/api/admin/invoices/{id}/draft-chase` for the draft, then PATCH + POST `/api/admin/ai-reply-drafts/{draftId}` and `/send` for edit and delivery (invoice-detail.tsx:1095-1101), DELETE to dismiss (:1119). Nothing is ever sent automatically.",
    "CHASE PROMPT: app/api/admin/invoices/[id]/draft-chase/route.ts:36-70 is the system prompt (Tahi voice, no em dashes, sign off 'Liam', escalation on repeat chases, 'DO NOT invent a payment link or bank details'), parsed out of `<subject>`/`<body>` tags at :77-84, model SONNET_MODEL (:30). At most one pending draft per invoice.",
    "LINE ITEMS TABLE (detail): invoice-detail.tsx:687-743 is a hand-rolled `<table>` (minWidth 500, `.h-scroll` wrapper) with columns Description / Qty (100px) / Unit Price (120px) / Total (120px), header 'Line Items' at :697, empty copy 'No line items on this invoice.' at :702. It is NOT the shared DataTable.",
    "TOTALS BLOCK: invoice-detail.tsx:746-792 \u2014 Subtotal (summed from items at :332), then a conditional tax row that prefers stored `taxAmountUsd` and otherwise infers `totalUsd - subtotal` when > 0.01, labelled 'GST (15%)' for NZD and 'Tax' otherwise (:760-773), an optional negative Discount row (:774-779), then Total. All rows are a fixed 240px wide.",
    "NO EDIT PATH: nothing on the detail page can change amounts, line items, due date, notes or the client after creation. `patchStatus` (invoice-detail.tsx:225-258) is the only mutation besides the rail actions; Notes render read-only at :669-685. MCP `update_invoice` exposes status/notes/dueDate/owner/paidAt/sentAt that the UI never offers.",
    "API GET (list): app/api/admin/invoices/route.ts:52-77 selects id, orgId, orgName, status, source, stripeInvoiceId, xeroInvoiceId, totalUsd as totalAmount, currency, dueDate, createdAt, updatedAt; ordered createdAt desc, limit 50. Access scoping via `getOrgScope` at :35-50 (empty scope short-circuits to `{items: []}`).",
    "API POST (create): app/api/admin/invoices/route.ts:83-198 \u2014 validates lineItems shape (:118-132), whitelists currency to NZD/USD/AUD/GBP/EUR falling back to NZD (:134-136), always inserts `status: 'draft'` (:155) with `source` = the caller's intent (:148, :154), and deliberately raises NO client notification (:176-181). Dispatches `invoice_created` (:184-195).",
    "API GET (detail): app/api/admin/invoices/[id]/route.ts:329-374 selects the full row including source, stripeHostedInvoiceUrl, xeroOnlineInvoiceUrl, taxAmountUsd, discountAmountUsd, sentAt/viewedAt/paidAt, and returns `{invoice, items}`.",
    "API PATCH: app/api/admin/invoices/[id]/route.ts:378-578 \u2014 FIELDS allowlist status/dueDate/notes/orgId/paidAt/sentAt (:399), ISO-prefix validation on the stamps (:415-428) with a 400 sentence, refuses `status: 'paid'` with `paidAt: null` (:434-436), auto-stamps paidAt/sentAt on transitions (:480-496), nulls paidAt when unwinding into UNWINDS_PAYMENT = draft|sent|viewed|overdue (:26, :483-492) but NOT for written_off (a write-off keeps its paid date because /financial-reports keys revenue off paid_at).",
    "API PATCH PUSH-BACK: app/api/admin/invoices/[id]/route.ts:536-556 fires only on a real transition into paid from a row with no paid date, skippable with `{pushback:false}`. `pushPaidToXero` (:150-212) reads the Xero payment account code from settings (skips with a reason when unset), refuses a non-AUTHORISED invoice with a plain-English reason (:176-183), and posts the balance XERO reports (`xeroAmountOutstanding`, :95-100) rather than the local total because a dashboard NZD bill gains 15% GST in Xero. `pushPaidToStripe` (:220-245) uses `paid_out_of_band=true`. Non-'done' outcomes are audit-logged as `invoice.pushback_{status}` (:288-305).",
    "API PATCH VOID: app/api/admin/invoices/[id]/route.ts:504-521 also POSTs Status VOIDED to Xero when written_off, swallowing failures silently.",
    "API stripe-create: app/api/admin/invoices/stripe-create/route.ts:34-229 \u2014 auto-creates the Stripe customer (using the FIRST contact with any email, :115-125, not the billing audience), adds invoiceitems, finalizes, then persists stripeInvoiceId + stripeHostedInvoiceUrl and flips the local row to `source:'stripe'`, `status:'sent'` with a first-send sentAt (:181-189) and raises the client bell row for billing contacts only on first send (:196-216). Note the duplicated `requireFeature('invoices')` call at :37-38 and :42-43.",
    "API xero-sync: app/api/admin/invoices/xero-sync/route.ts:36-291 accepts `{invoiceId}`, `{invoiceIds}` or `{orgId}` (which fans out to EVERY invoice for that org, :60-65), supports `dryRun`, matches a Xero branding theme by currency substring in its name (:74-79), sets TaxType OUTPUT2 / LineAmountTypes Exclusive for NZD and NONE/NoTax otherwise (:186-188), always pushes `Status: 'DRAFT'` (:193), and on create only sets `InvoiceNumber = INV-<first 8 of uuid>` (:219). On success it nulls `xeroOnlineInvoiceUrl` (:256-261) because a re-push demotes an approved invoice and Xero revokes the link.",
    "XERO-SYNC SCOPING GAP: app/api/admin/invoices/xero-sync/route.ts has ZERO calls to `requireAccessToOrg` or `getOrgScope` (verified by grep) \u2014 only `isTahiAdmin` + `requireFeature('invoices')` at :38-42. Every other invoice route calls requireAccessToOrg 2-5 times.",
    "API send-email: app/api/admin/invoices/[id]/send-email/route.ts:109-353 \u2014 409s a paid or written-off invoice (:157-162), resolves recipients through `selectBillingContacts` + `selectInvoiceRecipients` with two distinct 400 messages for 'nobody designated' vs 'no email' (:180-191), sends ONE email per recipient (no shared To), promotes only a draft to 'sent' and stamps sentAt only on first send (:306-317), and raises the bell row only on first send (:323-332). Subject: `Invoice <8-char ref> from Tahi Studio` (:271).",
    "XERO EMAIL MODE: app/api/admin/invoices/[id]/send-email/route.ts:260-267 \u2014 on the Xero rail with `invoicing.xeroEmailMode !== 'dashboard'`, `emailInvoiceFromXero` runs FIRST (guarded by `alreadySent` because Xero's Email endpoint has no idempotency key), and our own template stands down only when mode === 'xero' AND Xero actually took it. The response carries `xeroEmail: 'sent'|'skipped'|'failed'` plus a reason (:350-352).",
    "HOW TO PAY: lib/invoice-how-to-pay.ts:177-200 `buildHowToPay` returns a block only when all three hold \u2014 the Xero rail, no pay link, and still owed; `hasBankDestination` (:212-215) is the stricter test the EMAIL renders on, so a 'How to pay' heading with no account to pay into is never sent.",
    "SETTINGS KEYS: `invoicing.defaultChannel` (lib/invoice-channel.ts:31, default 'stripe' at :34, exactly two rails at :23-26), `invoicing.bankDetails` (lib/invoice-pay-settings.ts:40, JSON with accountName/accountNumber/bankName/referenceHint, accountNumber restricted to digits/dashes/spaces at :113), `invoicing.xeroPaymentAccountCode` (:43, /^[A-Za-z0-9-]{1,10}$/ at :213, NO default on purpose), `invoicing.xeroEmailMode` (:46, dashboard|xero|both, default 'dashboard' at :72).",
    "SETTINGS UI: components/tahi/settings/sections/studio-details.tsx:172-181 batch-saves ten keys one PATCH per key \u2014 studio_legal_name, studio_gst_number, studio_address, studio_billing_currency, invoice_number_prefix, invoicing.defaultChannel, invoice_footer_note, invoicing.bankDetails, invoicing.xeroPaymentAccountCode, invoicing.xeroEmailMode. The Xero email mode select uses its own plainer labels ('Send from the dashboard' / 'Let Xero send its own email' / 'Both', :38-42), not the API-facing ones.",
    "INVOICE NUMBER \u2014 CT.14/IC.7 IS OPEN: `db/schema.ts` invoices table (:751-796) has NO `number` column. `invoice_number_prefix` is written at studio-details.tsx:176 and seeded at :95 and read by NOTHING else in the app (verified by grep). Three different renderings exist today: `INV-` + 6 chars (invoice-detail.tsx:340 breadcrumb, draft-chase/route.ts:86-88), `INV-` + 8 chars (xero-sync/route.ts:219), and a bare 8 chars (lib/invoice-billing.ts:130-132 `invoiceReference`, used by every email, notification, How-to-pay reference and the portal).",
    "EXPORT GUARD \u2014 CT.13/IC.6 IS OPEN: app/api/admin/billing/xero-export/route.ts generates draft invoices from billable time with NO billing-model filter (:55-68 selects every billable entry for the month), NO idempotency guard around the insert (:113-134, re-running the same month double-bills), a silent `continue` on a zero rate (:97-98), and hardcoded `currency: 'NZD'` at :120 and CurrencyCode NZD at :146. It also has zero `requireAccessToOrg`/`getOrgScope` calls. Line item template at :100.",
    "TASKS.md IC BLOCK: TASKS.md:109-115 \u2014 IC.1 through IC.5 all `[x]` with production smoke notes (IC.4 is `[~]`: IC.4a and IC.4b both done, only the portal invoice UI deferred to the portal redesign port). IC.6 = CT.13 and IC.7 = CT.14 are the two open items. CT.14 is scoped as 'Invoice numbering (real invoices.number, prefix from settings) then the /invoices/[id] v3 lap' (TASKS.md:96).",
    "LIAM'S ANSWERED DECISIONS (TASKS.md:118-127): push as DRAFT for now with an auto-approve setting later; exactly two channels stripe|xero; use Tahi's own invoice sequence pushed INTO Xero; a hand mark-paid pushes back to the rail; Xero-rail email is 'both, behind a studio toggle'; backfill channel per client by hand (most orgs are dummy); the client sees only a How to pay block plus the pay link, never an internal channel label.",
    "STATUS.md:77 classifies `/invoices/[id]` as a LEGACY detail behind a v3 list, and lists /invoices (the list) among the already-v3 surfaces. That is the redesign brief in one line.",
    "/financial-reports (the surface Liam trusts daily) reads invoices directly off `paid_at`, not status: app/api/admin/financial-reports/summary/route.ts:150-151 (YTD), :176 (quarter), :183-184 (150-day), :210 (outstanding = status IN sent,overdue AND paid_at IS NULL), :230-252 (tax year, this month, same month last year), :256-259 (13-month series), :318-325 (days-to-pay stats), :348-353 (recent payments). Anything the invoices surface does to paid_at is immediately visible there.",
    "RECONCILIATION IS INVISIBLE: db/schema.ts:786-788 carries `airwallexTxnId`, `reconciliationStatus` and `lastReconciledAt` with a documented vocabulary of matched|mismatch|unmatched, but no invoice list or detail column reads any of them, and the only writer in the repo (app/api/admin/integrations/airwallex/sync/route.ts:157) writes `'orphan'`, a value not in that vocabulary.",
    "CURRENCY DEFAULT DRIFT: db/schema.ts sets `currency: text('currency').default('USD')` on invoices, while the create route falls back to 'NZD' (app/api/admin/invoices/route.ts:135-136), both display helpers fall back to 'NZD' (invoice-list.tsx:93, invoice-detail.tsx:85), and lib/invoice-defaults.ts:27 declares NZD the studio currency.",
    "TESTS ALREADY IN PLACE (a redesign must keep them green): app/api/__tests__/admin-invoice-detail.test.ts, admin-invoice-xero-push.test.ts, admin-invoice-xero-email-mode.test.ts, admin-client-invoice-channel.test.ts, admin-settings-invoice-channel.test.ts, billing-invoice-flow.test.ts, portal-invoice-denials.test.ts, portal-invoice-pay-path.test.ts; plus lib/__tests__/invoice-billing, invoice-channel, invoice-defaults, invoice-how-to-pay, invoice-pay-settings, xero-invoice-email, xero-status, xero-sync.",
    "NAV + PERMISSIONS: components/tahi/nav-model.tsx:94 puts Invoices in the studio 'Finance' group (no adminOnly flag, unlike Billing/Time/Financial reports/Reports), and :149 puts it in the client 'Billing' group with `requiresOrgAdmin: true`. lib/feature-tree.ts:62 registers the `invoices` feature key for both team and client audiences at route /invoices.",
    "MOBILE: the admin table keeps horizontal scroll on purpose (invoice-list.tsx:1157-1158, `mobileCard` is passed only for `!isAdmin` at :1347). The detail page ends with an `h-28 md:hidden` bottom-nav spacer (invoice-detail.tsx:796) and every ActionButton is minHeight 44 (:870).",
    "LOADING/EMPTY STATES: list uses DataTable's own `loading` prop (invoice-list.tsx:1346) and an EmptyState that distinguishes 'No invoices yet' + a Create CTA from 'No matches' + 'Try clearing a filter' (:1348-1367); a fetch error swaps the whole table for a centred 'Failed to load invoices.' + Retry (:1317-1338). Detail has a bespoke pulse skeleton (:260-270) and its own error card (:292-317)."
   ],
   "deadEnds": [
    "There are NO bulk actions on the invoices list. I searched for selection state, checkboxes and BulkActionBar usage: `components/tahi/bulk-action-bar.tsx` is only imported by clients/client-list.tsx, requests/request-list.tsx, tasks/tasks-content.tsx and tasks/tasks-list.tsx. The DataTable supports `selectable`/`selectedIds`/`onSelectionChange` but the invoices list passes none of them. If the redesign brief assumed bulk actions exist, it is a net-new feature, not a port.",
    "There is no design prototype or spec for the invoices surface to port from. `docs/superpowers/` holds only audits/, plans/ and specs/, and the single invoice-related file is `docs/superpowers/audits/2026-09-06-invoice-channel-assessment.json`, which is a pre-IC 'how it works today' assessment whose findings have largely been fixed by IC.1-IC.5 \u2014 do NOT treat its bug list as current (it says GET omits `source`, PATCH drops paidAt, OnlineInvoiceUrl appears zero times, currency is hardcoded NZD; all four are now false).",
    "`invoice_number_prefix` looks like a live setting but has zero readers. Do not wire a redesign to it expecting it to already number anything \u2014 the numbering work is CT.14/IC.7 and needs a real `invoices.number` column first.",
    "Do not look for an invoice edit form. There is none anywhere in the repo: no PATCH of amounts or line items exists in the API (app/api/admin/invoices/[id]/route.ts:399 allowlist is status/dueDate/notes/orgId/paidAt/sentAt) and the MCP `update_invoice` description explicitly says 'Amounts and line items are not editable here, delete the invoice and create a fresh one instead.'",
    "`reconciliationStatus` is not a usable signal today. The schema documents matched|mismatch|unmatched but the only writer (app/api/admin/integrations/airwallex/sync/route.ts:157) writes 'orphan', and no reader exists. Surfacing it in a redesign would need the reconciliation pass fixed first.",
    "The initial repo-wide greps were swamped by `.claude/worktrees/wf_*` copies of the same files (40KB+ of duplicate hits). Every search here must pass `--exclude-dir=.claude`; the worktree copies are stale snapshots from other workflow agents and their line numbers do not match main.",
    "The MCP server for this dashboard (`tahi-dashboard`) failed to connect this session (CONNECTION_CLOSED), so I could not verify any of the invoice tool behaviour live. Everything above is read from source; the MCP tool descriptions in workers/mcp-server/src/index.ts:636-646 and :1464-1465 are the written contract and they match the routes I read."
   ]
  },
  "STUDIO INVOICES \u2014 designed, built and verified in Claude Design (project \"Tahi dashboard\").\n\nFILES WRITTEN\n- invoices-studio.jsx \u2014 window.TahiInvoicesStudio = { Invoices, InvoiceDetail }. List, detail, create/edit slide over, chase drafter.\n- invoices-studio-kit.jsx \u2014 window.InvKit. Self contained primitives (icons, Amount, StatusPill, RailChip, ViewRail, ClientPicker combobox, SlideOver, Confirm, BulkBar, Toast, useRo).\n- invoices-studio-data.jsx \u2014 window.TahiInvData. 14 invoices across the existing cast (Physitrack, Allbirds, Kowtow, Mahana Orchards, Sharesies, Northwind), NZD/USD/AUD/GBP, set on Sun 6 Sep 2026 to match ops-data.jsx.\n- invoices-studio.css \u2014 `inv-` prefix, scoped under `.inv`. Verified free: I enumerated every loaded stylesheet in the browser and found 0 `.inv-` selectors outside this file (pm-, pf-, pfl-/pfm-, pa-, sa-, sp-, ops-, req-/rqt-, tsk-, cli-, ov- are the taken ones).\n- archive/scratch/invoices-studio-scratch.html \u2014 temporary mount, left in archive/scratch as asked. Switches: state (ready/loading/empty/error/no scope), theme, stage width (1280/960/375/fill), seat (owner/teammate), display currency, private mode.\n\nPAGES\nList. Money green accent. Totals strip (Outstanding NZ$35,972 / Overdue NZ$9,144 / Collected in September NZ$18,860 / Sitting in drafts NZ$12,133) in the display currency, each tile also a filter. Saved view rail: All invoices, Owed, Overdue, No way to pay, Paid this month, Drafts, By client, each with a count and a money figure. Dense rows: client + what it is for, number, amount native with the display conversion underneath, status, rail chip, due (date + relative), age, row menu. Selection + bulk bar (send, mark paid, export). New invoice slide over with client driven defaults, the IC.3 three state rail model, both override sentences, the Stripe no-contact pre-flight, a live running total, and the next number in the sequence.\nDetail. Hero (client, number, amount, status, rail chip, age, and ONE next action with a sentence saying why). Metadata grid. Lines with subtotal / GST / discount / total plus a display-currency line. Activity strip (created, sent, opened, each reminder, paid) with the push-back outcome sentence. Pay pages (Stripe, Xero, or bank details) with the empty slot explained. Chase drafter. Actions grouped Send / Money / <rail> plus a collapsed Danger zone.\nStates. Loading skeleton, empty, error, no-scope denial, filtered-to-nothing, read-only seat, private mode blur, dark, 375.\n\nDECISIONS (and what they fix from the reader map)\n1. The rail chip carries the PAY LINK STATE, not just the rail. \"Xero \u00b7 held at draft\" in red is the one glance answer to why a client has not paid. It drove a seventh saved view, \"No way to pay\" (owed + missing/held link) \u2014 net new, flag if you do not want it.\n2. Sent and Opened are one family (gold), Opened just adds the evidence. One status vocabulary for list and detail, replacing the two drifted maps.\n3. Mark as paid is offered on every owed status including viewed. Paid gets \"Undo mark as paid\"; written off gets \"Bring it back\". Both confirms name what your financial reports will do.\n4. Delete is draft-only and says so in the row itself; a sent invoice offers Void instead. This is the fix for the \"one click erases revenue\" finding.\n5. Native and display amounts are a structured pair from `D.pair()`, never one formatted string split on an approximately-equals sign.\n6. Overdue is derived once in the data file. Nothing downstream keeps a second copy.\n7. Every disabled control states its reason in place; no native alert() or confirm() anywhere.\n8. Export copy says it carries your filters; bulk export says how many rows.\n9. Money is owner work: `useRo` makes a teammate read-only and a preview session inert.\n\nTWO REAL COLLISIONS FOUND WHILE BUILDING (worth fixing elsewhere too)\n- A bare `rail` class on any element is fatal: `.rail` is the app shell sidebar and app-shell.css hides it on mobile, which blanked the whole surface at 375 until I removed it.\n- The DS bundle ships a Tailwind `.outline` utility that paints a 1.34px solid square outline on anything with class=\"outline\". `.inv-btn.outline` now clears it. `ops-kit.jsx` and `clients-kit.jsx` use the same `kind=\"outline\"` string and are almost certainly carrying the same faint square ring today.\n\nAPP-SHELL WIRING FOR THE INTEGRATOR (I did not touch app-shell.jsx, app-mount.jsx or the shell HTML)\nNav: nothing to add. `app-shell.jsx` line 178 already has `{ id: 'invoices', label: 'Invoices', icon: 'invoices' }` in the `Finance` group of TEAM_NAV, and `invoices` is already a key in the shell's `P` icon map (the ledger/receipt glyph). Label stays \"Invoices\".\n1) `app-shell.jsx`, App signature (~line 763), add one prop:\n   `invoicesContent = null, studioInvoicesContent = null, servicesContent = null,`\n2) `app-shell.jsx`, in the `.page` render chain, insert immediately AFTER the existing portal invoices branch:\n   `active === 'invoices' && invoicesContent && effectiveAudience === 'client' && !denied ?`\n   `invoicesContent(ctx) :`\n   `active === 'invoices' && studioInvoicesContent && effectiveAudience !== 'client' && !denied ?`\n   `studioInvoicesContent(ctx) :`\n   Render it raw, not inside `<div className=\"page-inner\">` \u2014 the module owns its own scroll frame, the same way requests/tasks/clients do.\n3) `app-mount.jsx`, add:\n   `if(window.TahiInvoicesStudio) props.studioInvoicesContent = ctx => React.createElement(window.TahiInvoicesStudio.Invoices, { ctx });`\n4) `Tahi App Shell.html`, add in head after `portal-account.css`:\n   `<link rel=\"stylesheet\" href=\"invoices-studio.css\">`\n   and these three, in this order, before `app-mount.jsx`:\n   `<script type=\"text/babel\" data-presets=\"react\" src=\"invoices-studio-data.jsx\"></script>`\n   `<script type=\"text/babel\" data-presets=\"react\" src=\"invoices-studio-kit.jsx\"></script>`\n   `<script type=\"text/babel\" data-presets=\"react\" src=\"invoices-studio.jsx\"></script>`\nctx contract used: `audience`, `currency`, `isMobile`, `inPreview`, `priv`, `go`. Optional `demo` prop drives the states; it defaults to 'ready' when the shell omits it. `InvoiceDetail` is exported separately for a future `/invoices/[id]` route (props: `inv, ctx, onBack, onPatch, notify, demo`).\n\nFIELDS THE PRODUCT LACKS (this design assumes them; each is a real gap)\n1. `invoices.number` + a studio sequence reading `invoice_number_prefix` (CT.14 / IC.7). Every screen here names invoices TS-1047. Today there are three renderings of \"the invoice number\" and the prefix setting has zero readers. Highest priority: without it none of this is buildable as drawn.\n2. The list GET must also select `stripeHostedInvoiceUrl`, `xeroOnlineInvoiceUrl`, `sentAt`, `viewedAt`, `paidAt`. The rail chip's pay state, the Age column, the \"Paid this month\" view and the \"No way to pay\" view all need them; the current projection has none of the five.\n3. A PATCH for amounts, line items and currency on DRAFT invoices only. \"Edit the invoice\" has no API today; the MCP tool says to delete and re-create.\n4. A reminder/chase log (`invoiceReminders`, or read `auditLog` filtered) so the activity strip can show \"Reminder 1, 2, 3\" with dates. Today only one pending AI draft per invoice exists.\n5. Bulk endpoints: send and mark-paid over an id list. `xero-sync` takes `invoiceIds`; `send-email` and PATCH are per invoice.\n6. Confirm `invoices.ownerId` exists (MCP `update_invoice` exposes owner, no column was named in the audit). The metadata grid shows Owner.\n7. Server-side filtering + pagination. The list is capped at 50 with client-side filters, so \"Paid this month\" is a lie the moment there are 51 invoices.\n8. Export must accept the active view/search and must apply `getOrgScope` (the route currently accepts status/dateFrom/dateTo and applies no scoping at all).\n9. `xero-sync` needs `requireAccessToOrg`. It has none.\n10. `reconciliationStatus`: I show it as \"Bank match\" only because it is the last honest line in the metadata grid, but the only writer emits `orphan`, which is not in the documented vocabulary. Fix the Airwallex writer before shipping that field.\n\nOPEN QUESTIONS FOR LIAM\n1. Keep the seventh saved view \"No way to pay\"? It is the most useful thing on the page and it is not in your brief.\n2. Number format: TS-1047 (studio prefix + sequence) or INV-1047? And does the sequence restart per year?\n3. Sent vs Opened as one gold family, or should Opened get its own colour?\n4. Should a teammate seat see Invoices at all? The nav has no adminOnly flag on it, so today a teammate would land on the money. I built read-only; say the word and it becomes hidden.\n5. Deleting a draft returns its number to the sequence in this design. Do you want gaps instead (never reuse a number)?\n6. Bulk \"Mark paid\" pushes each invoice back to its own rail. Do you want a confirm per rail, or the single confirm I built?\n7. Age column: days since raised. Would \"days since sent\" be more useful for chasing?\n8. The chase drafter sits above Actions on the detail. Should it instead be collapsed until an invoice is actually overdue rather than merely owed?\n\nScratch mount to look at: C:\\Users\\Work\\Projects\\tahi-dashboard is unrelated; the file lives in the Claude Design project at archive/scratch/invoices-studio-scratch.html \u2014 open it from https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=archive%2Fscratch%2Finvoices-studio-scratch.html",
  "SERVICES SHOWCASE REDESIGN \u2014 done, verified in browser at 1312px, 820px and 375px, light and dark, in every state.\n\nFILES CHANGED (Claude Design project 57bf60cf-5e6d-450f-9e2f-e25c8d12fd66)\n- portal-money.jsx \u2014 Services region fully replaced (PlanPanel/ServiceCard/SkCard gone; PlanStage, Lane, UsageBlock, Stat, Story, LineItem, AddOnShelf, ServiceArt, ServicesSkeleton in). Invoices half untouched, byte for byte.\n- portal-money.css \u2014 the services block replaced; a `.pm-svc`-scoped token set added; the 46rem/60rem container-query blocks extended for the new layout. Invoices rules untouched.\n- portal-money-data.jsx \u2014 PLAN extended (promise, lanes, queued, deliveredQuarter, monthLabel), USAGE added, ADDON_OFFERS added, SERVICES given weight/art/who/why, CATS replaced by FAMILY. Invoices data untouched.\n- portal-money-kit.jsx \u2014 NOT touched. Nothing new was needed; every icon and primitive already existed.\n- archive/scratch/portal-services-scratch.html \u2014 new temporary mount (page/theme/width/state/read-only switches). Left in archive/scratch, nothing in the project root, no `*-preview.html` created.\n- app-shell.jsx, app-mount.jsx, \"Tahi App Shell.html\" \u2014 not touched.\n\nTHE PAGE, TOP TO BOTTOM\n1. Plan as a statement. An always-dark forest tile, the same surface as sign-in and onboarding: gradient, drifting glow, film grain, leaf-radius pill with the org name, \"Scale\" set at 3.25rem/800, the plan promise in one editorial line, four ticks in lime. Right column: the two tracks as real objects (what is on each one now, its stage, a busy bar for the month, what pulls in next), then four facts (rate, next invoice, delivered this quarter, waiting in your queue). Two soft CTAs: \"Talk about your plan\", \"See what else we do\" (scrolls to the catalogue).\n2. How you are using Tahi. \"22 things delivered in the last 90 days\", three stats (turnaround 6.4 days, revisions 1.2 of 3, tracks busy 90 percent), then two honest charts: a stacked mix bar by kind with a counted legend, and a six-month delivered strip with the partial month drawn lighter and said out loud. Closing note names the window and says why hours are not on the page.\n3. What else we take on. One featured story (art panel + big outcome + a \"why this is up here for you\" callout + inclusions), four two-up stories, three compact line items in one card. Every one carries an art panel, family colour, fit badge, \"For \u2026\", timeline, and a soft Ask.\n4. Add-ons, quiet. What is on their account (with price, because it is their own bill) plus three things Scale can carry, described with no price at all, each with an ask.\n\nDECISIONS\n- Dark tile, not a card. The plan is the only thing on the page allowed to be loud; the forest surface is hardcoded there and only there, like the rail. Everything below stays token-driven so dark mode falls out.\n- Category chips dropped. Eight editorial cards fighting a chip row read as a directory. Replaced with a two-state, plan-aware toggle: Everything (8) / Fits Scale (4). When the filter drops the featured project, the first story is promoted so the rhythm never collapses.\n- Featured = Content and photography, not Website build. The client's site is four months old; their last shoot was May and blossom is three weeks out. A feature slot needs a true reason, so I added a `why` field rather than featuring the biggest ticket.\n- Family colour instead of eight greens. gold = design, slate = build, teal = content, brand green = strategy, set once as `--pm-tone` per card and read by the art, the kind label and the icon tile.\n- Placeholder art, drawn on purpose. Eight geometric SVG motifs (aperture, page skeleton, campaign spike, season wheel, signal bars, mark, box, shield) on a tone wash. A placeholder beats a bad photo; the slot, aspect and wash survive when real imagery lands.\n- Member seat is a degrade, not a denial. `/api/portal/subscription` 403s for a member. The plan itself is not financial, so the stage still renders with tracks, includes and delivery counts; only rate and next invoice stand down, with a lock line saying who can see them.\n- No hours. The one number a usage page begs for is the one the portal must not invent. The caption says we log them and why we do not publish them.\n- Ask stays soft. `ctx.openRequest(seed)` is an opt-in seam for the integrator to hand off to the real portal request dialog; without it the existing AskSheet files the same request with the service pre-named. No pricing, no \"order\", no create-a-service anywhere.\n\nDATA THE PRODUCT LACKS (all marked PROPOSED in portal-money-data.jsx)\n1. `services` needs columns: `orgId` (CT.11), `weight` (feature|story|line), `art`/`imageKey`, `who`, and per-client `why`. Outcome/inclusions/timeline are parsed out of one free-text `description` today by convention; that is a stopgap and cannot carry an editorial page.\n2. Track lanes. `/api/portal/capacity` returns tracks and a queue but never joins them into \"what is on track one right now, what is next\". Needs a per-lane shape.\n3. Track busy-days. `lane.days / lane.studioDays` and the 90-percent quarter figure have no source: no track-day history is stored. Needs a nightly rollup or a derived read over request status history. Cut the bars if that is not worth building.\n4. Delivered-per-month and by-kind are computable today from `/api/portal/requests?status=delivered&limit=500` (category, createdAt, deliveredAt) plus `lib/track-stats.ts`. No new endpoint needed, but a small portal `/api/portal/usage` would beat 500 rows over the wire.\n5. Add-on catalogue. `lib/billing.ts` knows three add-on keys and their monthly values; there is nothing to describe them with. Needs a table or a settings block.\n6. Split the plan read. Propose a non-financial `/api/portal/plan` (name, promise, includes, tracks, since) separate from the workspace-admin-only `/api/portal/subscription` (rate, next invoice), so a member seat gets the plan without the money instead of a 403 and a blank panel.\n7. Bug found, not fixed (outside my scope): `Sk` in portal-money-kit.jsx destructures `{ w, h: hh, r }` but every call site passes `hh`, so every skeleton in Invoices and Services renders at one height. I gave the Services skeleton its own class-sized bars and left a comment; a one-word fix in the kit would restore the invoice skeletons too.\n\nOPEN QUESTIONS FOR LIAM\n1. The upsell brief (CL.3) is still yours. This page deliberately has zero pricing pressure. Say how hard to sell and I will tune the add-on shelf and the \"Fits Scale\" default.\n2. Keep or kill the category filter? I removed the chip row for a two-state plan toggle. Easy to put back if you want Design/Build/Content/Strategy filtering once the catalogue grows past ten.\n3. Is \"tracks busy 90 percent of studio days\" a number you want a client to see? It is flattering today and awkward in a slow month. Say the word and it comes out, along with the lane bars.\n4. Do you want the featured `why` line written per client by you (an admin field), or derived (last delivery in that category, plus a season rule)?\n5. Should \"Ask about this\" open the real portal request dialog or stay in the sheet? The seam is `ctx.openRequest`; the sheet already files a real request, so this is a preference, not a blocker.\n6. Currency: after the PP.5 revert I left money formatting exactly as it was. Confirm whether client money should pin to the client's currency before this is ported.",
  {
   "pages": [
    {
     "key": "services-plan-stage",
     "verdict": "FIX",
     "issues": [
      "375: the two stage CTAs stack ragged. 'Talk about your plan' measures 244px and 'See what else we do' 246px, left-aligned one above the other, so the right edge is nearly-but-not-quite flush. The module already solves this elsewhere with `.pm-story-f .pm-btn{width:100%}`; `.pm-stage-cta` never got the equivalent. Either full-width them below 46rem or set a shared min-width.",
      "Track one's busy bar renders at 100% lime ('Busy 4 of 4 days'). On a client-facing readout a full bar reads as 'you are out of room' rather than 'we are working on it'. Consider capping the visual fill below 100 or changing the fill treatment at saturation.",
      "Minor: 'Delivered this quarter 18' sits about 40px above the next section's headline '22 things delivered in the last 90 days'. Both numbers are correct and both reconcile against the month strip (Jul+Aug+Sep=18, Jun+Jul+Aug+Sep=22), but a client has to do arithmetic to trust two near-identical counts that close together.",
      "The forest tile itself is the best thing on the page: gradient, glow, grain, 3.25rem/800 'Scale', lime checks, lanes as real objects. It holds in both themes and the member-seat degrade is clean. Nothing to change there."
     ]
    },
    {
     "key": "services-usage",
     "verdict": "FIX",
     "issues": [
      "Contrast: the block's load-bearing copy sits in `--text-faint` (#9A988F, 2.5 to 2.9:1 on white), well under AA. Measured: the 'we log hours but do not publish them' note 12px at 2.68:1, both chart captions 11.5px at 2.89:1, the month labels 10px at 2.89:1, the three stat labels (TURNAROUND / REVISIONS USED / TRACKS BUSY) 10px at 2.54:1, the section eyebrow 10px at 2.68:1. `--text-faint` is a shell token so this is partly inherited, but this block puts whole sentences a client must read into it. Move the sentences to `--text-muted` (#6B6A63, 5.4:1) and keep faint for eyebrows only.",
      "The two charts answer different windows under one headline. The section says '22 things delivered in the last 90 days'; the left card says 'the last 90 days' and its legend sums to 22; the right card says 'Apr to Sep' and its columns sum to 33. Both are labelled honestly and both reconcile, but side by side under a single count they invite a reader to think one of them is wrong.",
      "The numbers, the partial-September treatment and the 'why August is tallest' caption are genuinely good. The mix bar with a counted legend is the right chart for the question. Keep both."
     ]
    },
    {
     "key": "services-catalogue",
     "verdict": "FIX",
     "issues": [
      "The fit hint duplicates the pill, and on one card duplicates itself verbatim. 'Landing pages and campaigns' prints 'Submit it as a request and it joins your queue.' as its `forYou` meta line and again as the `pm-story-hint` next to the CTA, about 30px apart. Across the 2-up grid the same sentence appears three times on one screen, under three identical 'In your plan' pills. The pill already carries the fact. Drop the hint from the card, or state it once at the section level.",
      "`includes` is dead data for seven of eight services. Only the feature renders 'What is included'; the four grid stories and the three line items never show theirs and there is no expand affordance. A client sizing up 'Website design and build' gets two sentences and no scope while four bullets sit unused in the object. Add a disclosure on the story card, or move inclusions into the ask sheet.",
      "At 1280 the feature art panel is 320 x 460 of pale wash holding a 320 x 240 motif (the SVG keeps its 160x120 aspect and centres). It reads as an empty left column rather than a designed slot. Constrain the panel to the motif's aspect, or let the motif fill the tall box.",
      "375: in the line item the action row (`grid-area: act`) starts at the icon's left edge while the title starts 65px to its right, so 'On its own', the timeline and 'Ask about this' hang left of the content they describe. Indent the action row to the text column.",
      "'Fits Scale' promotes the first story into the feature slot, which keeps the rhythm, but the promoted card has no `why` (that field only exists on the true feature). The feature slot then appears with no reason for being a feature, which is exactly the argument made for creating it.",
      "The editorial weighting (feature / story / line), the family colour set once as `--pm-tone`, the drawn placeholder motifs and dropping the category chips are all right calls. This section is one pass away."
     ]
    },
    {
     "key": "services-addons",
     "verdict": "FIX",
     "issues": [
      "'A third track' is printed twice, back to back, in two different voices. The 'ON YOUR ACCOUNT' panel shows it as an ended line ('Ran for August only, while the harvest campaign page was building'), and the very next card in the offer shelf is also 'A third track', tagged 'You have run this before', re-telling the same August story. At 375 the two cards are about 40px apart. Fold the offer into the account row when the keys match, or suppress the offer.",
      "'ON YOUR ACCOUNT' labels a panel whose only row is not on the account. `on` (active add-ons) is empty and `past` has one, but the panel renders with the fixed label anyway. A client scanning a billing-adjacent page reads it as a live charge. Split the labels, or hide the panel when `on` is empty and show ended add-ons elsewhere.",
      "`'Ended ' + a.since` prints the start month as the end month. It is correct here only because the add-on started and ended in the same month; an add-on running June to August would render 'Ended June 2026'. Needs an `endedAt` on the record.",
      "375: the ended add-on row squeezes. The row is 304px, the pill takes 126px (42%) and never shrinks, so the name and note get 136px and the note wraps over four lines. Stack the pill under the text below 46rem.",
      "The 'no price on the offers, price only on your own bill' rule and the 'Liam confirms every change first, in writing' line are exactly right. Keep the policy, fix the plumbing."
     ]
    },
    {
     "key": "services-loading",
     "verdict": "FIX",
     "issues": [
      "The skeleton is 918px against a 4070px ready page at 1280, a 4.4x jump. It draws two of the five movements (a shrunken stage and two story cards) and omits the usage block, the feature story, the line card, the add-on shelf and the closing ask entirely.",
      "Even the stage it draws is half height: the skeleton left column has a pill, a title and two lines but no checks list and no CTAs, and the right column has two lane blocks but no facts row. `.pm-sk-art` is 7.5rem while the real 2-up art is about 234px at 1280.",
      "The code comment says 'Shapes here hold the real layout so the page does not jump'; measured, it does not. Either extend the skeleton to the full page rhythm, or drop to a single honest 'loading' block and stop claiming layout stability.",
      "The shimmer itself works (`pm-shim` 1.2s) and the stage skeleton correctly uses the white-on-forest gradient rather than the light-surface one. That part is right."
     ]
    },
    {
     "key": "services-member-seat",
     "verdict": "SHIP",
     "issues": [
      "The degrade is the strongest decision on the page. Rate and next invoice stand down, the plan, tracks, includes, delivery count and queue count all stay, and the lock line names who can see the rest. The 4-up facts grid drops to 2 without leaving a hole. No change needed."
     ]
    },
    {
     "key": "services-empty-error-featureoff",
     "verdict": "SHIP",
     "issues": [
      "All three read honestly and stay in the module's voice. Empty correctly hides the usage block, the add-on shelf and the Everything/Fits Scale toggle while keeping the plan stage.",
      "Minor: the page carries three different subtitles depending on state. Ready says 'Your plan, what you have been getting out of it, and everything else Tahi Studio takes on.', error says 'Your plan, how you are using it, and what else we take on.', feature_off says 'What Tahi Studio takes on.' Copy shifts under the user when they hit Try again. Pick one.",
      "Minor: in the empty state 'See what else we do' still scrolls to a catalogue that says 'Nothing published here yet'. Honest, but the CTA is promising something the page cannot deliver."
     ]
    },
    {
     "key": "services-readonly",
     "verdict": "SHIP",
     "issues": [
      "Verified across all eleven ask affordances: every one is `disabled`, opacity 0.5, cursor not-allowed, with the 'Read only while viewing as a client' title. Both button vocabularies (`pm-btn` and `pm-linkbtn`) carry the disabled treatment, so the tell is not hover-only. Nothing to change."
     ]
    },
    {
     "key": "services-375",
     "verdict": "FIX",
     "issues": [
      "No horizontal overflow: `.pm` scrollWidth 369 equals clientWidth 369, so the `display:block` fix on `.pm` is holding.",
      "Touch targets pass. Every control measures 44px or more below 46rem (buttons, seg, link buttons all fall back to the 2.75rem base rule).",
      "Ragged stage CTA stack and the squeezed ended add-on row are the two real 375 defects (detailed under services-plan-stage and services-addons).",
      "The page is 7397px tall at 375, roughly 20 phone screens, with five full-bleed art panels in it. Dense is right for Liam, but this is long rather than dense: the 8 services take about 4300px of it. Consider collapsing the three line items behind a count, or shortening the story card stack on narrow.",
      "Twelve ask affordances on one page (5 story CTAs, 3 line asks, 3 offer asks, 1 closing ask, plus 'Talk about your plan'). The stated intent is 'a showcase, not a shop'; at 375 the repetition of 'Ask about this' every 500px reads closer to a shop than the copy claims.",
      "The feature story stacks well, the section head and the Everything/Fits Scale segment both go full width cleanly, the stat rows switch to the label-left value-right form as designed, and the mix legend wraps without breaking. The narrow-first base layout is well built."
     ]
    },
    {
     "key": "services-dark",
     "verdict": "SHIP",
     "issues": [
      "Dark falls out of the tokens with no regressions found. The forest tile lifts off the near-black canvas via the `--pm-forest-a/b` override rather than sinking into it, and the family colours brighten correctly (gold #D9A441, slate #9FB6C2, teal #6FC3B6), which makes the mix bar read better in dark than in light.",
      "The partial-September column at 42% brand stays legible against the dark track. The art washes, the why callout and the pills all hold.",
      "Observation, not a defect: in dark the tile stops being 'the one loud thing' because the whole canvas is dark, so the intended hierarchy flattens a little. Nothing to fix unless you want a stronger edge or a slightly warmer glow in dark.",
      "The `--text-faint` contrast issue logged under services-usage applies in dark too and should be checked once the token is settled."
     ]
    }
   ],
   "summary": "Verdict: FIX, not REDO. This is a strong redesign and the three-movement structure is the right answer. The plan stage is the best single tile in the prototype, the member-seat degrade and the read-only pass are both correct, dark mode falls out of the tokens with no regressions, and 375 has no horizontal overflow and no undersized targets. The editorial weighting (feature / story / line), the family colour set once as --pm-tone, the drawn placeholder motifs and dropping the category chips are all decisions I would defend.\n\nEight things need work before this ships. Three are correctness, not taste. (1) \"A third track\" is printed twice back to back in two voices, once as an ended line in the \"ON YOUR ACCOUNT\" panel and again as an offer card 40px below it. (2) That panel is labelled \"ON YOUR ACCOUNT\" while its only row has ended, and 'Ended ' + a.since prints the start month as the end month, which is right here only by coincidence. (3) The loading skeleton is 918px against a 4070px page, drawing two of five movements, so the code comment claiming it holds the layout is not true.\n\nThe rest are craft. --text-faint (#9A988F, 2.5 to 2.9:1) carries 27 text elements including the \"we do not publish hours\" note, every fit hint, the chart captions and all the stat and month labels; that is well under AA and the redesign leans on it for sentences, not decoration. The fit hint duplicates the pill on every card and duplicates itself verbatim on \"Landing pages and campaigns\". Seven of eight services never show their includes even though the data is there. At 375 the two stage CTAs stack ragged at 244 and 246px, and the ended add-on row gives its text 136px of a 304px row so the note wraps four times.\n\nSofter notes worth a look: the page carries three different subtitles across states; the two usage charts answer different windows (90 days, 22 vs Apr to Sep, 33) under one headline; \"Fits Scale\" promotes a card into the feature slot without the why that justifies the slot; the 1280 feature art is 320x460 of wash holding a 320x240 motif; and at 375 the page is 7397px with twelve ask affordances, which is closer to a shop than the copy claims. Method: rendered the designer's archive/scratch/portal-services-scratch.html and walked 1280 and 375, light and dark, through ready, loading, member_seat, empty, error, feature_off, read-only, both catalogue scopes and the ask sheet. No project files were changed. Console is clean."
  },
  {
   "pages": [
    {
     "key": "invoices-list",
     "verdict": "FIX",
     "issues": [
      "BLOCKING: the row action menu on the bottom rows is clipped out of existence. .inv-card sets overflow:hidden and .inv-menu is an absolutely positioned child with no flip-up logic. Measured on the last row of the Owed view at 1280: menu height 242px, extending 231px past the card's bottom edge and 192px past .inv-scroll. The dots button visibly toggles to its open state and nothing appears. Void / Mark as paid / Resend / Copy link are unreachable on roughly the last three rows of every view, and the row menu is the only path to those actions on mobile. Fix: render the menu in a portal, or add an 'up' variant plus a flip check, or drop overflow:hidden from .inv-card and clip the corners another way.",
      "The rail chip truncates the very state it exists to show. .inv-rail-s has text-overflow:ellipsis and the Rail column is a fixed 8.5rem, so at 1280 Mahana Orchards reads 'Manual \u2022 bank tran...' in both light and dark. Decision 1 says the chip carries the pay-link state as the one-glance answer; the longest state string is the one that gets eaten. The full text only exists in a title tooltip, which is a hover-only affordance and absent on touch. Widen the column, shorten the state vocabulary, or wrap.",
      "375: every icon-only button measures 32 x 32px (.inv-iconbtn), including the header More menu and every row's action menu. Export and New invoice measure 36px tall. CLAUDE.md requires 44px minimum and this file's own header comment claims 2.75rem targets. The row dots at 32px is the sole entry to per-invoice actions on a phone.",
      "375: the four totals tiles drop to one per row (@container inv max-width:48rem), so roughly 430px of money tiles plus the header sit above the saved-view rail and the first invoice never appears on a first screen. The 2-up grid used between 48rem and 64rem would hold at 375 and keep the strip to two rows.",
      "375: the saved-view rail becomes a horizontal scroller with scrollbar-width:thin, which paints a desktop scrollbar track with arrow buttons inside the card (clearly visible in both light and dark). Other modules hide it. Also .inv-view-t small is hidden in that mode, so the money figure disappears from the saved views on the exact width where the tiles already ate the space.",
      "375: .inv-search is 44px tall but the input inside it is only 18px and has no label or click handler on the wrapper, so the top and bottom 13px of the search field are dead to touch.",
      "Nit: the header's More menu is a bare dots glyph with no border sitting between an outlined Export and a filled New invoice. It reads as an unfinished third button rather than an overflow affordance."
     ]
    },
    {
     "key": "invoice-detail",
     "verdict": "FIX",
     "issues": [
      "The metadata grid renders ghost cells. .inv-metas uses grid-template-columns:repeat(auto-fit,minmax(10rem,1fr)) with gap:1px over a --border-subtle background, so any unfilled cell in the last row shows as a solid slab of border colour. TS-1045 at 1280 leaves five empty cells; TS-1041 in dark leaves six and reads as a broken block; at 375 (2 columns, 9 items) it leaves one. Fix: give .inv-meta:last-child a grid-column:1/-1 fill, or draw the hairlines with inset shadows the way the rest of this file does.",
      "The hero's 'ONE next action' is immediately duplicated. On TS-1045 the hero button reads 'Approve it in Xero' with the sentence 'It is sitting at DRAFT in Xero, so Xero has not issued a pay page. Approve it in Xero.' and the Note directly beneath repeats that sentence verbatim under the title 'The client has no way to pay this' with a second button labelled 'Open Xero'. Two buttons, two labels, one job, stacked. Either drop the Note when the hero action already covers it, or strip the hero sentence to the reason and let the Note carry the fix.",
      "The Bank transfer pay card leads with 'No page yet' for a manual invoice, which is the one case where the client CAN pay. The account number is demoted to the caption under a headline that says the opposite. Give the manual rail its own headline (the account number, or 'Bank transfer, no hosted page').",
      "On a manual invoice, 'What the client sees' shows three cards of which two are permanent 'This invoice does not run on Stripe / Xero' filler. Consider showing only the rails in play plus the one that is missing.",
      "Keyboard: the collapsed Danger zone stays in the tab order. .inv-danger-b.closed is grid-template-rows:0fr with overflow:hidden on the child, so the Void and Delete buttons keep tabIndex 0 and are focusable while the panel measures 0px high. Verified: focusing Void succeeded and scrolled the container. A keyboard user tabbing the Actions section lands on an invisible Void button and Enter opens the confirm. Add inert or visibility:hidden, or unmount when closed.",
      "Hover: a disabled danger button dissolves. .inv-btn:disabled:hover (0,3,0) beats .inv-btn.danger (0,2,0) and there is no .inv-btn.danger:disabled:hover override (only .primary and .outline have one). Verified by cloning the rule as a class: rest state rgb(167,58,49) on white text, hovered state rgba(0,0,0,0) on rgb(18,26,15). The disabled Delete goes from solid red to transparent under the cursor. .ghost has the same gap.",
      "Nit: the 'why it is disabled' sentence in .inv-act small is --text-faint inside a container at opacity .55, which is the lowest-contrast text on the page and it is the text carrying the explanation."
     ]
    },
    {
     "key": "new-invoice-slideover",
     "verdict": "FIX",
     "issues": [
      "BLOCKING: clicking the client card clears the client. Field renders a <label> around composite controls with no htmlFor, so the label's implicit control becomes the first labelable descendant. In the picked state that is the 'Change client' close button. Verified live: picked Kowtow, clicked the client name, the selection was wiped. Clicking a chosen client to check it is the most natural gesture in that row.",
      "BLOCKING: clicking the 'Where it goes' caption silently resets the payment rail. Same label cause, first labelable descendant is the first Seg radio. Verified live: destination set to 'Stripe link', clicked the caption, destination flipped to 'Dashboard only'. This is a money-routing field being reset by a click on a caption. Same mechanism makes 'Currency' and 'Payment terms' captions open their dropdowns. Fix Field once: render a div plus a span caption unless htmlFor is supplied.",
      "The slide over's primary CTA is disabled with no reason anywhere. Verified: the Create invoice button has disabled=true and title=null, and there is no inline note. Decision 7 says every disabled control states its reason in place; the one control the whole panel exists to reach is the exception. The footer note says 'Created as a draft. Sending is a separate step.', which answers a different question. Say 'Pick a client and add at least one line'.",
      "Note on the pattern generally: reasons delivered only via title on a disabled button do not render in Chrome (disabled controls do not fire mouse events) and never render on touch. The list and detail both carry a footnote fallback for the read-only case, but the row-menu and Btn title reasons do not.",
      "SlideOver and Confirm handle Escape, move focus to the panel and stop the overlay mousedown correctly, but neither traps focus or restores focus to the trigger on close, and background scroll is not locked."
     ]
    },
    {
     "key": "chase-drafter",
     "verdict": "FIX",
     "issues": [
      "The draft signs the wrong person. TS-1043 is owned by Staci Bonnie (shown in the metadata grid on the same page) and the generated body ends 'Thanks,\\nLiam'. Verified across samples: the sign-off is hardcoded and ignores inv.owner. On a client-facing email in a two-person studio this is the kind of error that gets noticed.",
      "The firmer variant auto-writes a commercial commitment: 'If I do not hear back this week I will pause new work on the account until it is settled.' That is a policy decision being pre-drafted as if it were settled. Worth a switch, or softer default wording, since the drafter is one click from Send.",
      "Otherwise strong: the idle copy states how many times the invoice has been chased and that the tone escalates, the drafting state uses a skeleton plus an honest 'Reading the invoice, the account and the last three notes', the model and token count are shown, and nothing sends without an explicit press. The teal AI accent reads as intentional rather than decorative."
     ]
    },
    {
     "key": "states",
     "verdict": "FIX",
     "issues": [
      "The loading state asserts facts it does not have. While the table shows skeleton rows, the totals strip renders real tiles reading NZ$0 with the sub-lines 'Nothing out', 'Nothing past due', 'Nothing settled yet in September', 'No drafts waiting', and every saved view shows a 0 count and NZ$0. On a money surface that is a false statement during the first second, not a placeholder. Skeleton the tiles and the view counts too, or blank the sub-lines until state === 'ready'.",
      "Framing is inconsistent across the three failure states. Empty renders inside .inv-card with a border; error and no-scope render bare on the page background with no container, and both also drop the totals strip and the saved-view rail entirely. Pick one frame.",
      "Export and New invoice stay fully enabled in the error and no-scope states. Exporting from a ledger that did not load, or raising an invoice when no client is in scope, are both dead ends.",
      "Good: the filtered-to-nothing state is well judged, distinguishing 'Nothing matches that' (with 'Clear the search') from 'Nothing in overdue' with the line 'That is good news, not an empty page'. The read-only teammate seat greys New invoice and carries the footnote 'You are reading the ledger. Only an owner can send an invoice or move money.' Private mode blurs the money and leaves the counts, which is the right cut."
     ]
    },
    {
     "key": "design-system-conformance",
     "verdict": "SHIP",
     "issues": [
      "Conformance is strong overall. Zero em or en dashes across all four files. Row hairlines are drawn as inset box-shadows rather than single-side borders (the same habit as ops.css). Leaf radius appears only on primary CTAs, avatar wrappers and small icon backgrounds. Manrope throughout, rem spacing throughout, --ease resolves, dark mode falls out of a paired token block and holds up at 1280 and 375. Domain colour is used with intent (money green, work slate, growth gold, teal for the AI section, danger red). prefers-reduced-motion is respected on the skeleton shimmer. No horizontal overflow at 375 (scrollWidth === clientWidth === 375). The .inv- prefix is genuinely unclaimed.",
      "One single-side border remains: .inv-slide { border-left:1px solid var(--border) }. ops.css, the closest sibling module, has zero side borders, and this file avoids them everywhere else deliberately. Swap it for box-shadow:inset 1px 0 0 var(--border) for consistency with the file's own stated habit.",
      "The .inv-btn.primary brand-tinted drop shadow matches ops.css, so it is in-house rather than a deviation. Noting it only because 'borders not shadows' is a stated rule and this is the one sanctioned exception.",
      "The seventh saved view 'No way to pay' (owed plus missing or held pay link) earns its place. It is the only place the surface answers 'why has this client not paid' without opening anything, and it reads cleanly in both themes. Keep it.",
      "The status vocabulary unification (Sent and Opened as one gold family, Draft slate, Overdue red, Paid green) and the D.pair() native-plus-display amount are both clear wins over a single formatted string, and the multi-currency rows (US$4,800 over NZ$7,968, A$1,890 over NZ$2,060) read correctly at every width."
     ]
    }
   ],
   "summary": "Verdict: FIX, not REDO. This is the best-argued surface in the prototype so far and the vocabulary is right: soft cards, borders not shadows, leaf radius only where it belongs, Manrope, intentional domain colour, dark mode straight out of the tokens, no em dashes, no horizontal overflow at 375. The totals strip, the seven saved views, the rail chip carrying the pay-link state, the derived-once overdue, the D.pair() native/display amounts and the Confirm dialogs that name what survives all land. But it is not shippable as-is: I found two blocking interaction bugs plus a set of real defects, all verified live in the designer's scratch mount at 1280 and 375, light and dark, across every state.\n\nBlocking, in order:\n1. LIST. The row action menu is clipped out of existence on the bottom rows. .inv-card has overflow:hidden and .inv-menu is an absolutely positioned child with no flip logic. Measured on the last row: 242px menu, 231px past the card. The dots button toggles open and nothing renders. Void, Mark as paid and Resend are unreachable there, and on mobile that menu is the only route to them.\n2. SLIDE OVER. Kit's Field wraps composite controls in a bare <label>, so the caption's implicit control is the first labelable descendant. Verified live: clicking the picked client's name wipes the client, and clicking the words \"Where it goes\" resets the payment rail from Stripe link back to Dashboard only. A money-routing field reset by a click on a caption.\n\nThen: the detail's metadata grid renders ghost cells (auto-fit grid over a 1px border background) which read as a broken slab at 1280, 375 and worst in dark; the hero's \"one next action\" is immediately duplicated by a Note repeating the same sentence with a differently-labelled second button; the chase drafter signs \"Thanks, Liam\" on an invoice owned by Staci Bonnie and auto-writes \"I will pause new work on the account until it is settled\"; the collapsed Danger zone keeps Void and Delete in the tab order at 0px height; a disabled danger button turns transparent on hover because .inv-btn:disabled:hover has no .danger override; the loading state asserts NZ$0 and \"Nothing out\" while the table is still a skeleton; the rail chip ellipsises the pay state it exists to show (\"Manual \u2022 bank tran...\"); and at 375 every icon-only button is 32px against the 44px house minimum, with four full-width money tiles pushing the first invoice off the first screen.\n\nRoughly a day of fixes, most of them one-line and several shared with the other modules (the Field label pattern and the disabled-danger hover gap almost certainly repeat in ops-kit.jsx and clients-kit.jsx, alongside the .outline utility collision the designer already found). Nothing here questions the design thinking; it is all the last mile."
  }
 ],
 "fixpass": [
  "All fixes are live in the design project and verified in a headless browser at 1280 and 375, light and dark, across every state.\n\n**Files changed (Claude Design project 57bf60cf, \"Tahi dashboard\")**\n`invoices-studio.jsx`, `invoices-studio-kit.jsx`, `invoices-studio-data.jsx`, `invoices-studio.css`. Scratch mount untouched. Verified copies of exactly what was uploaded are on disk at `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\inv-work\\` (each byte-diffed against the served file: identical). Screenshots: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\inv\\`. Shot driver: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\inv-shot.mjs`.\n\nWHAT CHANGED, IN THE CRITIC'S ORDER\n\n1. BLOCKING, row menu clipped by .inv-card overflow. `Menu` in the kit now portals its panel into the surface root (`wrap.closest('.inv')`) with `ReactDOM.createPortal`, measures itself against that box in `useLayoutEffect`, flips above the trigger when there is not enough room below, caps its own max height to the room it has, and closes on scroll of `.inv-scroll` and on resize so it can never drift off its button. `.inv{position:relative}` added; `.inv-menu.up` gets its own reversed drop animation. Outside-click now tests both the wrapper and the portalled panel. Row clicks still do not fall through, because React portals bubble through the React tree into the existing `stopPropagation` on `.inv-c-menu`.\n   Verified: last row of the Owed view at 1280, menu height 243px, top 505 / bottom 153 inside the host, `insideHost:true`, `up:true`, parent element `inv inv-list`. Same at 375 dark, `inside:true`, dots button 44px. Header More menu still opens downward, right aligned, inside the frame (18-headmenu-dark-teammate.png).\n\n2. BLOCKING, Field's bare `<label>` stealing clicks. `Field` now renders a `div`; the caption is a `<span id>` with `role=\"group\" aria-labelledby` on the wrapper when there is no single control, and a real `<label htmlFor>` only when `htmlFor` is supplied (the two chase fields).\n   Verified live: picked Physitrack, clicked the client name, selection intact (\"Physitrack\" before and after). Set destination to Stripe link, clicked the words \"Where it goes\", destination still \"Stripe link\". Caption element is now a SPAN.\n\n3. Rail chip truncating its state. `payState` short strings capped at a dozen characters (\"held at draft\" to \"no pay page\", \"bank transfer\" to \"bank\"), the Rail column widened 8.5rem to 10rem (and 8.25rem to 10rem in the 78rem query), and `overflow:hidden` dropped from `.inv-rail-s`. The ellipsis stays only as insurance and is documented as such.\n   Verified: zero chips clipped (`scrollWidth > clientWidth` returns an empty list) at 1280, 960 and 375. Longest rendered pairs read in full: \"Xero \u2022 no pay page\", \"Stripe \u2022 link live\", \"Manual \u2022 bank\".\n\n4. Metadata ghost cells. `.inv-metas` moved from an auto-fit grid to wrapping flex with `flex:1 1 10rem`; the last row's cells grow into the remainder so the border coloured bed can never show through. The 48rem override is now `flex-basis:calc(50% - 1px)`.\n   Verified: TS-1045 at 1280, 9 cells over rows of 7 and 2, last cell flush with the container (gap at end 0px). TS-1041 in dark, 8 cells over 7 and 1, no slab. 375, rows of 2 with the odd cell spanning, gap 0.\n\n5. Hero duplicating the one next action. The \"The client has no way to pay this\" Note is now gated to `next.key !== 'fixpay'`, so it never repeats the hero's button; and `payState().long` was rewritten to carry the reason only, never the fix (\"It is sitting at DRAFT in Xero, so Xero has not issued a pay page.\" with no trailing \"Approve it in Xero.\").\n   Verified on TS-1045: hero button \"Approve it in Xero\", hero sentence is the reason alone, and `document.querySelectorAll('.inv-note')` is empty.\n\n6. Chase drafter. Sign off is `(inv.owner || 'Liam Miller').split(' ')[0]`, and the firmer close no longer commits the studio to anything: \"If I have the wrong person, point me at whoever handles your payments and I will send it there.\"\n   Verified: TS-1043 (owner Staci Bonnie) signs \"Staci\"; TS-1042 (owner Liam Miller) signs \"Liam\"; the string \"pause new work\" is gone from the generated body.\n\n7. Loading state asserting facts. `Tile` and `ViewRail` take a `loading` prop and render skeletons in place of the value, the sub line, the saved view money and the counts; the FX footnote is suppressed while loading.\n   Verified: in Loading, all four tile values and sub lines are empty with a skeleton present, all seven view counts empty, footnote absent. No NZ$0 and no \"Nothing out\" anywhere.\n\n8. Inconsistent framing of the three failure states. Empty, error and no scope now all sit inside `.inv-card`. Error and no scope also disable Export, New invoice and both Import items with the reason attached (\"The ledger did not load.\" / \"No client is in your scope.\").\n   Verified: all three report `framedInCard:true`; error and no scope report `Export:off, New invoice:off`; empty keeps both live and keeps its (true) zero tiles.\n\n9. Single side border on .inv-slide. `border-left` replaced with `box-shadow:inset 1px 0 0 var(--border)`, and the narrow override with `box-shadow:none`. `grep -c border-left` over the file returns 0.\n\nALSO FIXED FROM THE SAME REPORT\n\n- Collapsed Danger zone in the tab order: the panel now carries `inert` when closed plus `visibility:hidden` transitioned so it only hides after the close animation. Verified: `inert:true`, `visibility:hidden`, height 0, and `first.focus()` does not land (`focusedClosed:false`). Open again: `inert:false`, visible, 131px.\n- Disabled danger button dissolving on hover: added `.inv-btn.danger:disabled:hover` (plus `.ghost` and `.quiet`), and `.inv-btn.danger:hover` is now `:not(:disabled)`. Verified by hovering the disabled Delete: background stays `rgb(167,58,49)`, colour `rgb(255,255,255)`, `filter:none`.\n- Disabled reason as the lowest contrast text: `.inv-act:disabled` no longer dims with opacity; the row greys deliberately and the title and explanation move to `--text-muted`.\n- Slide over CTA disabled with no reason: a `blocker` string now renders in the footer in gold and as the button title. Verified it goes \"Pick a client first.\" then \"Add at least one line with an amount on it.\" as state changes.\n- Title-only reasons on bulk buttons: `BulkBar` takes a `note`. Verified with everything selected: \"Some of these are paid or voided, so only the export applies to all of them.\" with Send and Mark paid off, Export on.\n- No focus trap, no focus restore, no scroll lock: new `useModal` hook (Escape, initial focus, Tab trap, focus handed back to the opener, `.inv-scroll` locked) used by both SlideOver and Confirm; `onClose` is read through a ref so a parent re-render cannot yank focus mid keystroke. Verified: on open `overflow-y:hidden` and focus on the panel; 25 Tabs later focus is still inside; Escape closes, focus returns to \"New invoice\", overflow back to `auto`. Same for Confirm, focus returning to the \"Void\" button that opened it.\n- Bank transfer card leading with \"No page yet\": `PayCard` takes `value` and `none`; the manual rail leads with the account number (`12-3141-0059871-00`) with the bank, account name and reference hint as the caption.\n- Permanent \"does not run on Stripe / Xero\" filler: only the rails in play render now (the invoice's own rail, plus any rail that actually holds a live URL). Verified: TS-1045 shows one card, TS-1041 shows one card.\n- 375: icon buttons and buttons go to 2.75rem (search clear, slide over close, picked client clear too); the four money tiles are 2 up, not 1; the saved view scroller hides its desktop scrollbar and keeps the money on each chip; the search input is `align-self:stretch` so the whole 44px field is live. Verified: zero targets under 44px, tiles 2 per row, `scrollbar-width:none`, five money chips present, `scrollWidth === clientWidth` on `.inv` at 375 light and dark.\n- Nit: the header More menu now carries a border so it reads as an overflow affordance rather than an unfinished third button.\n\nVERIFICATION RUN\nNo console errors or React warnings in any run (the shot driver fails loudly on `console.error` and `pageerror`; nothing was emitted, including for the `inert` attribute under React 18.3). Shots: 01 list 1280 light, 02 last row menu, 03 slide over, 04 detail TS-1045, 05 detail TS-1041 dark, 06/07 danger zone closed and open hover, 08/09 chase TS-1043 and TS-1042 dark, 10 loading, 11 error / no scope / empty, 12 list 375 light, 13 row menu 375 dark, 14/15 modal focus and escape, 16 detail 375, 17 bulk note, 18 header menu dark teammate, 19 list 960, 20 list 375 dark.\n\nRESIDUALS I DID NOT TOUCH (not on the critic's list)\n- On a manual invoice the rail action group is headed \"Manual\" but offers \"Create the payment link\" (a Stripe action, enabled) and \"Open in Manual\" (disabled, \"It has never been pushed to a rail.\"). Both read wrong for a bank transfer invoice. Pre-existing, and fixing it changes action semantics rather than styling, so I left it for a follow up.\n- The Field label pattern and the missing disabled hover overrides almost certainly repeat in `ops-kit.jsx` and `clients-kit.jsx`, as the critic suspected. Out of my file scope.",
  "Done. Report:\n\n**Files changed (Claude Design project `Tahi dashboard`, 57bf60cf-5e6d-450f-9e2f-e25c8d12fd66)**\n- `portal-money-data.jsx` \u2014 services half only. Invoices half byte for byte (verified by re-download + diff: only my intended edits differ).\n- `portal-money.jsx` \u2014 services half only. Lines 1-440 (the invoices list, detail, HowToPay, Thread) diffed byte identical against the pre-edit download.\n- `portal-money.css` \u2014 SERVICES block only. Lines 1-265 (the invoices/shared block) diffed byte identical.\n- `archive/scratch/portal-services-scratch.html` \u2014 untouched, no change needed.\n- `app-shell.jsx`, `app-mount.jsx`, `Tahi App Shell.html` \u2014 untouched.\nEditor link: https://claude.ai/design/p/57bf60cf-5e6d-450f-9e2f-e25c8d12fd66?file=portal-money.jsx\n\n**Every FIX item the services critic listed**\n\n1. \"A third track\" printed twice in two voices (services-addons 0). `AddOnShelf` now derives `offers` by filtering `ADDON_OFFERS` against the keys already on `ADDONS`, so the offer card is suppressed and the story is told once, on the account row. The ask is not lost: the ended row carries \"Ask about running it again\". Measured: `thirdTrackCount` is 1 at every width and theme (was 2).\n2. ON YOUR ACCOUNT over a row that is not on the account (addons 1). The fixed label is gone. The panel now renders two independently-gated groups: \"Running on your account\" (only when `on` is non-empty) and \"You have run before, nothing on your bill now\". Measured: `onYourAccount` count 0 in all 38 rendered states.\n3. `'Ended ' + a.since` printed the start month (addons 2). Added `endedAt: 'August 2026'` to the record with a PROPOSED comment naming the missing column, and the pill reads `a.endedAt || a.since`. Measured: \"Ended August 2026\".\n4. Load-bearing usage copy in `--text-faint` (services-usage 0). Lifted 13 selectors inside the SERVICES block to `--text-muted`: `.pm-svc-l`, `.pm-sec-k`, `.pm-stat-l`, `.pm-use-h span`, `.pm-use-cap`, `.pm-use-note`, `.pm-month-l`, `.pm-story-who b`, `.pm-metaitem svg`, `.pm-lineitem-who`, `.pm-addons-l`, `.pm-addon-p small`, and the ended-row name. Measured computed colour: `rgb(107,106,99)` (#6B6A63, 5.4:1 on #FFFFFF) in light and `rgb(154,152,143)` (#9A988F, ~5.5:1 on #1C1B19) in dark, for every one of them, at all three widths. Nothing in the services block is left at `--text-faint`. I also removed `.pm-addon.ended{opacity:.75}`, which would have dragged the newly-muted name back down to ~3.2:1 on a row that now carries a control.\n5. Two charts, two windows, one headline (usage 1). Chose one window, not two headlines. `USAGE.windowLabel` is now `'since April'`, `delivered90: 22` became `delivered: 33`, and `byKind` rescaled to 13/9/8/3. Verified: headline \"33 things delivered since April\", both card chips read \"since April\", the mix legend sums 33, the six columns sum 33. Design + build is 22 of 33, so the \"two thirds\" caption stays literally true. Side effect the critic also flagged (plan-stage 2): \"Delivered this quarter 18\" and the section count are now clearly different questions rather than two near-identical numbers.\n6. Fit hint duplicating the pill and itself (catalogue 0). `.pm-story-hint` deleted from the card and from the CSS. New `FitKey` component states each of the three fits once, under the catalogue head. Data fix: the campaign card's `forYou` no longer re-says the hint (\"Runs inside your Scale tracks. Your harvest campaign page in July was one of these.\" \u2014 grounded in INV-2026-011 in the same file). Measured: `.pm-story-hint` count 0; \"Submit it as a request and it joins your queue.\" now appears exactly once on the page.\n7. `includes` dead for seven of eight (catalogue 1). New `Includes` component: the feature keeps the open \"What is included\" list, every story and every line item gets a real `<details>/<summary>` disclosure (`.pm-inc`, chevron rotates on open, 2.75rem target narrow / 2.25rem wide, focus-visible ring, marker suppressed). Measured: 7 disclosures + 1 open list = all 8 services expose their scope.\n8. Skeleton at a quarter of the ready page (services-loading, all four sub-items). `ServicesSkeleton` rewritten to draw all five movements by borrowing the real structural classes (`.pm-stage`, `.pm-sec-h`, `.pm-stats`, `.pm-use-card`, `.pm-story`/`.pm-art`, `.pm-stories`, `.pm-linecard`, `.pm-lineitem`, `.pm-addons`, `.pm-offers`, `.pm-svc-foot`), so every container query that shapes the ready page shapes the loading one. The stage skeleton now draws the checks list, both CTAs and the 4-up facts row. Measured height ratio loading/ready: **0.94 at 1312, 0.88 at 820, 0.82 at 375** (was 918/4070 = 0.23). The code comment now matches what it does.\n9. Two stage CTAs stacking ragged at 375 (plan-stage 0). `.pm-stage-cta` is a stretch column below 46rem with `width:100%` on both buttons, and reverts to a row at 46rem. Measured at 375: both buttons 301px (was 244 / 246).\n10. Busy bar at 100 percent reading as an alarm (plan-stage 1). `Lane` sets a `full` modifier at saturation. The fill drops the ramp-to-bright-lime for one calm woven band (`repeating-linear-gradient` at .5/.32 alpha) and the label changes to \"Worked every one of the 4 days\" instead of \"Busy 4 of 4 days\". Track two's partial bar is unchanged, so the two read as different states rather than one pegged meter.\n11. Three subtitles across states (empty-error-featureoff 1). One `SVC_SUB` constant used by ready, loading, empty, error, member_seat and feature_off. Measured identical in all 38 renders.\n\n**Other critic FIX items in the same pages, also done**\n- Feature art 320x460 of wash holding a 320x240 motif (catalogue 2): at 46rem+ the tall panel gets `overflow:hidden` and the svg runs `width:150%` centred, so the motif renders 576x432 in a 384x568 panel at 1312 and 480x360 at 820, bleeding off both edges instead of floating in an empty column.\n- Line-item action row hanging left of its content at 375 (catalogue 3): narrow `grid-template-areas` changed from `\"ic t\" \"act act\"` to `\"ic t\" \". act\"`. Measured: text column left 81px, action row left 81px (was 16 vs 81).\n- \"Fits Scale\" promoting a card with no `why` (catalogue 4): promotion is now gated on `stories[0].why`, so the feature slot only appears when something has earned it.\n- Ended add-on row squeezed at 375 (addons 3): `.pm-addon` stacks below 46rem, pill under the text. Measured: text 279 of a 309px row (was 136 of 304), note wraps twice not four times.\n- Empty state's \"See what else we do\" promising a catalogue that says nothing is published (empty 2): `PlanStage` takes `browsable` and drops that CTA when `all.length === 0`.\n\n**Verification**\n- Re-rendered all 6 states x light/dark x 1312/820/375 (36 renders) plus a read-only pass at 1312 and 375 and a disclosure-open shot. Console clean, 0 errors, 0 page errors.\n- 375: `.pm` scrollWidth equals clientWidth (overflow 0) in every state and theme; document overflow 0.\n- Read-only still holds: 13 of 13 ask affordances disabled with the \"Read only while viewing as a client\" title, including the two new controls (the disclosure summaries are not asks; the ended-row ask is disabled).\n- Dark falls out of the tokens with no regressions; the forest tile and family colours unchanged.\n- All three files re-downloaded after each write and byte-compared against my local copies: `CSS EXACT`, `JSX EXACT`, `DATA EXACT`. Both JS files pass `node --check`; CSS braces balance.\n\n**One thing I did not change, with a reason**\n- `.pm-seg-b` measures 40px in the scratch harness at 375, under the 44px bar. That is a harness artifact, not a shipping defect: the scratch mount does not set `.ash-mobile`, and `portal-money.css` already lifts `.pm-seg-b` to 2.75rem under `.ash-mobile` (which app-shell.jsx applies on mobile). I added `.ash-mobile .pm-inc-s` to that same rule so the new disclosure control is covered too. The base rule lives in the invoices/shared half, which I left byte for byte.\n- The critic's softer notes on page length (7674px at 375) and twelve ask affordances are unchanged. Removing the duplicate offer and the five fit hints took some out; the seven new disclosures are collapsed and add ~40px each. Reworking the catalogue's information density is a design decision, not a defect fix, so I left it for Liam.\n\n**Artifacts** (gitignored, no repo footprint)\n- Full-page matrix: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\svc\\` (38 PNGs, named `<width>-<theme>-<state>.png`)\n- Section crops light and dark, 1312 and 375: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\svc-crop\\`\n- Measurement dump backing every number above: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\tmp-svc\\measure.json`",
  {
   "pages": [
    {
     "key": "services-plan-stage",
     "verdict": "SHIP",
     "issues": [
      "CLOSED (was FIX 0, ragged 375 CTAs): both stage buttons now measure 301px wide at the same x=37 at 375 (were 244 and 246). `.pm-stage-cta` is a stretch column below 46rem with `width:100%` on `.pm-btn`, and reverts to a row at 46rem (176 and 177px, side by side).",
      "CLOSED (was FIX 1, full busy bar reading as an alarm): `Lane` sets a `full` modifier at saturation. Track one renders a soft woven band (repeating-linear-gradient at .5/.32 alpha over a .16 track) and the label reads 'Worked every one of the 4 days'; track two keeps the solid lime ramp at 75%. Visually confirmed at 3x zoom: the two bars now read as different states, not as one pegged meter.",
      "CLOSED (was FIX 2, two near-identical counts 40px apart): the stage still says 'Delivered this quarter 18' but the section below now says '33 things delivered since April'. Different window, different number, no arithmetic for the reader.",
      "The forest tile is unchanged and still the best thing on the page. Member-seat degrade verified intact: rate and next invoice stand down, the 4-up facts row becomes 2-up with no hole, lock note present at 7.21:1 light and 6.20:1 dark."
     ]
    },
    {
     "key": "services-usage",
     "verdict": "FIX",
     "issues": [
      "CLOSED (was FIX 0, load-bearing copy in --text-faint): every named element is now --text-muted and clears AA. Measured worst-case ratios, light then dark: .pm-use-note 5.02 / 6.47, .pm-use-cap 5.43 / 5.95, .pm-month-l 5.43 / 5.95, .pm-stat-l 4.76 / 5.50, .pm-sec-k 5.02 / 6.47, .pm-svc-l 5.43 / 5.95, .pm-story-who 5.43 / 5.95, .pm-lineitem-who 5.43 / 5.95, .pm-addons-l 4.76 / 5.50, .pm-metaitem 5.43 / 5.95, .pm-linkbtn 5.43 / 5.95. Zero `--text-faint` declarations remain anywhere in the SERVICES CSS block.",
      "CLOSED (was FIX 1, two windows under one headline): one window everywhere. Headline '33 things delivered since April', both card chips read 'since April', mix legend sums 13+9+8+3=33, month columns sum 5+6+4+7+9+2=33. Verified at all three widths in both themes.",
      "REMAINING, and it is the worst contrast on the surface: `.pm-ask-note` in the ask sheet still carries `--text-faint` at 11.5px. Measured 2.54:1 in light, 2.93:1 in dark. The sentence is 'This adds one request to your queue.' sitting directly under the Send button, so the one line that tells a client what the primary action does is the least legible text on the page. The sheet opens from all 13 asks on this surface. One declaration at portal-money.css:559."
     ]
    },
    {
     "key": "services-catalogue",
     "verdict": "FIX",
     "issues": [
      "CLOSED (was FIX 0, fit hint duplicating the pill and itself): `.pm-story-hint` count is 0 at every width and theme. 'Submit it as a request and it joins your queue.' appears exactly once on the page, in the new `FitKey` block. The campaign card's forYou was rewritten and no longer restates it.",
      "CLOSED (was FIX 1, includes dead for seven of eight): 7 `details.pm-inc` disclosures plus 1 open `.pm-story-inc` list on the feature = all 8 services expose their scope. The summary is keyboard focusable (tabIndex 0), toggles on click and on Enter, marker suppressed, chevron rotates, focus-visible ring present. Target height 44px at 375, 36px at 1312.",
      "CLOSED (was FIX 2, 320x460 wash holding a 320x240 motif): at 1312 the feature panel is 384x568 holding a 576x432 motif that bleeds off both edges and fills 76% of the panel height. It reads as a designed crop rather than an empty column.",
      "CLOSED (was FIX 3, 375 action row hanging left of its content): the line item action row now starts at x=81, identical to the title column (icon at x=33). Verified on all three line items.",
      "CLOSED (was FIX 4, Fits Scale promoting a feature with no why): promotion is now gated on `stories[0].why`. Under Fits Scale the feature slot is empty (featureCount 0), leaving 3 stories in the 2-up grid and 1 line item. No feature appears without a reason for being one.",
      "NEW, copy contradiction created by the FitKey placement: the section paragraph reads 'Everything below is scoped and quoted for Mahana Orchards before a single hour is booked', and the FitKey sits about 40px under it saying 'In your plan: Submit it as a request and it joins your queue' and 'On its own: Scoped and quoted on its own, outside the retainer'. Two of the three fits contradict the paragraph, in one eyeful. Under the Fits Scale scope all four visible services are plan or add-on, so the paragraph is wrong for every card on screen. Reword the paragraph to something the key does not fight.",
      "MINOR: one of the four family colours misses AA in light. `.pm-story-kind` for STRATEGY is var(--brand) #5A824E on white at 4.43:1, 10px/700. Gold 5.91, slate 6.69, teal 5.09 all pass, and all four pass in dark (6.45 to 8.32). Nudge the green family tone toward --brand-strong in light only.",
      "MINOR, kit level and shared with invoices: the `.pm-pill.info` used for 'Alongside your plan' is 4.14:1 in light (6.25 in dark) at 11.5 to 12px. It appears three times on this page including in the FitKey.",
      "OBSERVATION: at 820 the feature art is the thinnest the fix gets. Panel 320x617 holding a 480x360 motif = 58% of the panel height, so a pale band remains top and bottom in the 46rem to 60rem band. Holds at 1312 (76%) and stacks correctly at 375."
     ]
    },
    {
     "key": "services-addons",
     "verdict": "SHIP",
     "issues": [
      "CLOSED (was FIX 0, 'A third track' printed twice in two voices): the offer shelf now filters `ADDON_OFFERS` against the keys already on `ADDONS`. 'A third track' appears exactly once at every width and theme, on the account row. The shelf carries Priority support and Search and answer visibility, stretched by `auto-fit` so suppressing an offer leaves no hole. The ask is preserved on the ended row as 'Ask about running it again'.",
      "CLOSED (was FIX 1, ON YOUR ACCOUNT over a row that is not): the fixed label is gone. Only 'You have run before, nothing on your bill now' renders, and the 'Running on your account' group is independently gated on a non-empty active list. No 'On your account' string exists in any of the 38 rendered states.",
      "CLOSED (was FIX 2, start month printed as end month): `endedAt: 'August 2026'` added with a PROPOSED comment naming the missing column, pill reads `a.endedAt || a.since`. Renders 'Ended August 2026'.",
      "CLOSED (was FIX 3, 375 squeeze): the row stacks below 46rem. Row 309px, text column 279px (was 136 of 304), note wraps to 2 lines at 33px tall (was 4), pill sits under the text at full width. `.pm-addon.ended{opacity:.75}` was removed, so the name holds 5.43:1 light and 5.95:1 dark on a row that now carries a control.",
      "OBSERVATION on the 375 stack: reading order is now name, note, ask, then the 'Ended August 2026' pill, so the state lands after the CTA. Consider ordering the pill above the ask below 46rem.",
      "OBSERVATION: `o.used` and its 'You have run this before' pill are now unreachable, since any offer whose key matches an add-on record is filtered out. Harmless, but it is dead UI."
     ]
    },
    {
     "key": "services-loading",
     "verdict": "SHIP",
     "issues": [
      "CLOSED (was FIX 0 and 1, two of five movements at a quarter height): the skeleton now draws all five movements by borrowing the real structural classes, so the same container queries shape it. Measured loading/ready height ratio: 0.94 at 1312 (4119 against 4375), 0.88 at 820 (3987 against 4534), 0.82 at 375 (6315 against 7674). Was 918 against 4070 = 0.23.",
      "CLOSED (was FIX 1 specifically): the stage skeleton now draws the checks list, both CTAs (full width at 375, 9rem row at 46rem) and the 4-up facts row with its divider. Verified visually at 1312.",
      "CLOSED (was FIX 2, the comment claiming layout stability was not true): the comment now describes what the code does, and the measurement backs it.",
      "Shimmer confirmed intact (pm-shim 1.2s) and correctly suppressed under prefers-reduced-motion: both `.pm-sk` and `.pm-story.sk .pm-art` resolve to animation-name none."
     ]
    },
    {
     "key": "services-member-seat",
     "verdict": "FIX",
     "issues": [
      "The visible degrade is still correct and still the strongest decision on the page. Rate and next invoice stand down, plan, tracks, includes, delivered count and queue count all stay, the lock line names who can see the rest, the 4-up facts grid drops to 2-up cleanly at 1312 and 375, both themes.",
      "NEW, latent money leak in the code the fixer touched: `AddOnShelf` is called as `h(AddOnShelf, { ro, onAsk: askAddOn })` with no `money` prop, and the active group renders `h(Money, null, D.moneyRound(a.rate, D.PLAN.currency))` plus the cadence unconditionally. It does not show today only because `ADDONS` has no record in state 'active'. The first client with a live add-on would show a member seat a monthly dollar figure on the same page where the plan rate is hidden, which breaks the exact guarantee the lock note makes. Gate the price on `money` the way `PlanStage` does, or render 'Visible to your workspace admin' in its place."
     ]
    },
    {
     "key": "services-empty-error-featureoff",
     "verdict": "SHIP",
     "issues": [
      "CLOSED (was minor 1, three subtitles across states): one `SVC_SUB` constant now serves every state. Verified byte identical across ready, loading, member_seat, empty, error and feature_off at all three widths: 'Your plan, what you have been getting out of it, and everything else Tahi Studio takes on.' Copy no longer shifts under the reader when they press Try again.",
      "CLOSED (was minor 2, empty-state CTA promising a catalogue that is not there): `browsable` is gated on `all.length > 0`, so 'See what else we do' is not rendered in empty. Confirmed: empty exposes only 'Talk about your plan' and 'Ask us anything'.",
      "All three still read honestly and stay in the module's voice. Empty correctly hides the usage block, the add-on shelf and the Everything / Fits Scale toggle while keeping the plan stage. Error and feature_off render a single card with no figures."
     ]
    },
    {
     "key": "services-readonly",
     "verdict": "SHIP",
     "issues": [
      "Re-verified at 1312 and 375: 13 of 13 ask affordances are `disabled`, opacity 0.5, cursor not-allowed, with the 'Read only while viewing as a client' title. Both button vocabularies (`pm-btn` and `pm-linkbtn`) carry the treatment, including the new 'Ask about running it again' on the ended add-on row. The new `details.pm-inc` disclosures stay operable, which is right: reading scope is not a write."
     ]
    },
    {
     "key": "services-375",
     "verdict": "FIX",
     "issues": [
      "CLOSED: no horizontal overflow anywhere. `.pm` scrollWidth equals clientWidth at 375, 820 and 1312 in both themes across all six states.",
      "CLOSED: the two real 375 defects (ragged stage CTA stack, squeezed ended add-on row) are both fixed and measured under services-plan-stage and services-addons.",
      "NEW, small target regression against the previous pass: the Everything / Fits Scale segment measures 40px tall at 375 in this mount. `.pm-seg-b` has `min-height:2.5rem` in the base rule and is only lifted to 2.75rem by `.ash-mobile`, which the standalone scratch mount does not apply. Every other control clears 44px from its own base rule (buttons, link buttons and the new `.pm-inc-s` disclosures all measure 44). It will pass inside the real shell's mobile frame, but the base rule is what governs a narrow desktop window, so move `min-height:2.75rem` onto the base `.pm-seg-b`.",
      "OBSERVATION, the critic's length note is now slightly worse: 7674px at 375, up from 7397, roughly 20.5 phone screens. The growth is the FitKey block plus 8 disclosure rows, which is the cost of two good fixes. Ask affordance count is unchanged at 13 (5 story CTAs, 3 line asks, 1 ended add-on ask, 2 offer asks, 1 closing ask, plus 'Talk about your plan'), so the shop-versus-showcase note stands as written.",
      "The narrow-first base layout is still well built. Feature stacks, section head and segment go full width, stat rows switch to label-left value-right, mix legend wraps, line item action row indents to the text column, ended add-on stacks."
     ]
    },
    {
     "key": "services-dark",
     "verdict": "FIX",
     "issues": [
      "The token-driven half of dark is clean and, as before, the family colours read better in dark than light (gold 7.65, slate 8.15, teal 8.32, green 6.45 against the card). No regressions from any of the ten fixes: the woven full-track band, the FitKey, the disclosures, the stacked add-on row and the five-movement skeleton all hold.",
      "REMAINING, and this is the item the original report parked as 'check the faint token once it settles': the forest tile's own `--pm-ink-faint` (#8FA886) was never lifted, and the dark override lightens `--pm-forest-a/b` to #2E3E26 / #22301b without lifting the ink, so the tile is worse in dark than in light. Measured worst case: `.pm-lane-next` 4.42:1 light and 3.80:1 dark, and it carries a whole sentence ('Next on this track: Spring price list update'); `.pm-lane-size` 4.42 / 3.80; `.pm-stage-l2` 5.15 / 4.43; `.pm-fact-l` 5.15 / 4.43. Everything else on the tile clears AA comfortably (ink-soft 6.19 to 7.21, ink 9.05 to 10.53). Lift `--pm-ink-faint` in the dark block, or drop `.pm-lane-next` to `--pm-ink-soft` since it is a sentence rather than a label.",
      "OBSERVATION unchanged from the original report: in dark the tile stops being the one loud thing because the whole canvas is dark, so the intended hierarchy flattens a little. Nothing to fix unless a stronger edge or warmer glow is wanted in dark."
     ]
    }
   ],
   "summary": "Verdict: FIX, close to SHIP. Every one of the eighteen FIX items the services critic listed is closed, and I could measure each one rather than take it on trust. The three correctness items are gone: \"A third track\" appears exactly once at every width and theme, the add-on panel no longer labels an ended row as being on the account and prints \"Ended August 2026\" from a real endedAt, and the loading skeleton draws all five movements at 0.94 / 0.88 / 0.82 of the ready page (was 0.23) with a code comment that now matches what it does. The craft items are equally real: zero --text-faint declarations survive in the SERVICES CSS block and every element the critic named now measures 4.76:1 or better in light and 5.50:1 or better in dark; the fit hint is stated once in a new FitKey instead of three times under three identical pills; all 8 services expose their scope via 7 keyboard-operable disclosures plus the feature's open list; the two 375 stage CTAs are both 301px; the 375 line item action row indents to the text column; the ended add-on row stacks with the note at 2 lines instead of 4; the feature art fills its panel and bleeds off both edges; and the Fits Scale scope no longer promotes a feature without a why. One subtitle across all six states, \"See what else we do\" hidden in empty, 13 of 13 asks disabled in read only, no horizontal overflow at any width, reduced motion respected, console clean across 38 rendered states.\n\nFour things are left, none of them a redo. One is correctness and worth fixing before this ships: AddOnShelf renders an active add-on's monthly rate with no money gate, so the first client with a live add-on would show a member seat a dollar figure on the page where the plan rate is deliberately hidden. It is invisible today only because the demo data has no active add-on, and it undercuts the single best decision on the surface. Two are contrast, in the two places the sweep did not reach: the ask sheet's \"This adds one request to your queue.\" is still --text-faint at 2.54:1 light and 2.93:1 dark, which makes the line explaining the primary button the least legible text on the page, and the forest tile's own --pm-ink-faint was never lifted, so \"Next on this track: ...\" reads at 4.42:1 light and 3.80:1 dark because the dark override lightens the tile without lightening the ink. The fourth is copy: putting the FitKey directly under a paragraph that says \"Everything below is scoped and quoted before a single hour is booked\" creates a contradiction two of the three fits immediately deny, and under the Fits Scale scope the paragraph is wrong for every card on screen. Minors worth one pass: the Everything / Fits Scale segment measures 40px at 375 because .pm-seg-b's base min-height is 2.5rem and only .ash-mobile lifts it, the STRATEGY family label sits at 4.43:1 in light, the shared info pill at 4.14:1, and the 375 page grew to 7674px. Method: rendered archive/scratch/portal-services-scratch.html at 1312, 820 and 375, light and dark, through ready, loading, member_seat, empty, error, feature_off, read-only and both catalogue scopes, with geometry, computed colour, gradient-aware contrast, keyboard and reduced-motion probes plus component crops. No project files were changed. Evidence in C:\\Users\\Work\\Projects\\tahi-dashboard\\.sa-shots\\svc\\ (measures.json, contrast.json, and the crops)."
  },
  {
   "pages": [
    {
     "key": "invoices-list",
     "verdict": "FIX",
     "issues": [
      "CLOSED, blocking row menu. The panel now portals into the surface root and flips. Last row of Owed at 1280 light: menu 224x243 at top 584 / bottom 827 inside a host of 79 to 1000, up=true, parent element `inv inv-list`, elementFromPoint at the menu's top row returns `inv-menu-i` so the items are hit-testable. Same at 375 dark on the last row (531 to 774, inside, up). First row at 375 opens downward and stays inside (122 to 346 horizontally). Clicking `Mark as paid` from a row menu closed the menu and did NOT open the row detail, so the portal did not reintroduce click fall-through.",
      "CLOSED, rail chip truncation. Zero `.inv-rail-s` report scrollWidth > clientWidth at 1280, 960 and 375, light and dark. Full strings render: `Stripe link live`, `Xero no pay page`, `Manual bank`.",
      "CLOSED, 375 touch targets on the list. Every icon-only button measures 44x44. The only sub-44 elements in the list are the `.inv-open` title links at 38px, and those sit inside a 151px row that is itself the click target, so the affordance is not the only way in.",
      "CLOSED, 375 totals strip. Four tiles now lay out over two rows, not four.",
      "CLOSED, 375 saved-view rail. scrollbar-width computes to `none` and the `.inv-view-t small` money figures still render (`NZ$35,972`, `NZ$9,144`, `NZ$3,220`).",
      "CLOSED, 375 search dead zone. The wrapper is 44px and elementFromPoint at top+3, middle and bottom-3 all return the INPUT.",
      "CLOSED, header More affordance. It now carries a border via `.inv-head-a .inv-iconbtn` (invoices-studio.css:119) and reads as an overflow button rather than a third unfinished one.",
      "NEW, FIX. At 375 the header More menu is clipped off the LEFT edge of the surface in both themes. Measured light and dark: trigger at x 117 to 161, panel 224px wide and right-aligned to it, so the panel spans -63 to 161 against a host of 0 to 375. All three items start at x -57, and the labels render as `ort from Stripe`, `ort from Xero`, `ice settings` (screenshot .claude/shots/invcrit/04-list-375-light-headmenu.png, and 22-headmenu-375-dark.png). The new useLayoutEffect in invoices-studio-kit.jsx:288-305 clamps vertically but computes left/right purely from the trigger with no clamp to the host box. It is inside the frame at 960 and 1280, so this is 375-only. Fix: clamp `left`/`right` so the panel stays within `h.left + gap` and `h.right - gap`."
     ]
    },
    {
     "key": "invoice-detail",
     "verdict": "FIX",
     "issues": [
      "CLOSED, metadata ghost cells. `.inv-metas` is wrapping flex now and the last row grows into the remainder. TS-1045 at 1280 light: 9 cells over rows of 7 and 2, last cell right edge flush with the container (gap 0). TS-1041 at 1280 dark: 8 cells over 7 and 1, gap 0. TS-1045 at 375 dark: 9 cells over 2/2/2/2/1, gap 0. No border-coloured slab in any of the three.",
      "CLOSED, hero duplication on the pay-blocker path. TS-1045 renders one hero button (`Approve it in Xero`), the hero sentence carries the reason only, and `document.querySelectorAll('.inv-note')` is empty.",
      "CLOSED, bank transfer headline. TS-1041 leads with the account number `12-3141-0059871-00` and the caption `ASB, Tahi Studio Limited. Use the invoice number as the reference`.",
      "CLOSED, filler pay cards. `What the client sees` now renders only the rails in play: one card on TS-1041 (manual), one on TS-1045 (Xero). No `This invoice does not run on Stripe` filler.",
      "CLOSED, danger zone keyboard trap. Closed panel reports `inert` on the inner div, visibility hidden, height 0, and focusing the Void button leaves activeElement outside the panel. Opening it removes inert, visibility goes visible, height 131, and Void takes focus.",
      "CLOSED, disabled danger hover. `.inv-btn.danger:disabled:hover` exists (invoices-studio.css:106). The disabled Delete measures rgb(167,58,49) on white at rest and rgb(167,58,49) on white while hovered.",
      "NOT CLOSED, FIX. The `why it is disabled` sentence is still the lowest-contrast text on the page, and the CSS comment at invoices-studio.css:566-568 claims it was fixed when it was not. `_ds_bundle.css` carries `button:disabled, [aria-disabled=\"true\"], .disabled { opacity: .5 }` and nothing in invoices-studio.css overrides opacity for `.inv-act`, so the whole disabled row renders at effective opacity 0.500. Measured on `Edit the invoice` / `Only a draft can be edited. The client has this one.`: light, rgb(107,106,99) over rgb(241,240,236) = 1.97:1; dark, rgb(154,152,143) over rgb(35,34,32) = 2.41:1. Both are under 3:1, let alone 4.5:1, and the enabled sub-lines beside them sit at 2.89:1 and 3.17:1. Visible in .claude/shots/invcrit/25-hero-chase-dup.png. Fix: add `opacity:1` to the `.inv-act:disabled` block and let the greyed bed plus `--text-muted` carry the state.",
      "NEW, FIX. The hero still duplicates its one next action, on the chase path this time. On an overdue invoice the hero primary reads `Draft a chase email` (invoices-studio-data.jsx:259) and the Chase email card's primary reads `Draft a chase email` too (invoices-studio.jsx:184). Verified on TS-1043: two buttons with that exact label on one page, and pressing the hero one does not draft anything, it only scrolls (`draftOpen:false`, scrollTop 0 to 594). A loud primary that does not do what its label says, sitting above an identical one that does. Fix: relabel the hero to the movement it performs (`Go to the chase email`) or wire it to `make()`."
     ]
    },
    {
     "key": "new-invoice-slideover",
     "verdict": "FIX",
     "issues": [
      "CLOSED, both blocking label bugs. Every caption is now a SPAN (`SPAN:Client`, `SPAN:Where it goes`, `SPAN:Currency`, `SPAN:Payment terms`, `SPAN:Notes`). Picked Physitrack, then clicked the client name, the client sub-line, and the `Client` caption: selection stayed `Physitrack` through all three. Set the destination to `Stripe link`, clicked the words `Where it goes`: destination stayed `Stripe link`. Clicked the `Currency` caption: no dropdown opened.",
      "CLOSED, disabled CTA with no reason. The footer note carries it in place and switches with state: `Pick a client first.` before a client, `Add at least one line with an amount on it.` after, both rendered with `inv-slide-note warn`. Visible at 375 dark in .claude/shots/invcrit/18-slideover-375-dark.png.",
      "CLOSED, reasons delivered only by title. Row-menu and header-menu items now print the reason as a sub-line inside the item (`Copy the payment link / There is no page to copy`, `Import from Stripe / The ledger did not load`), and the read-only seat carries the footnote `You are reading the ledger. Only an owner can send an invoice or move money.`",
      "CLOSED, modal plumbing. Opening the panel moves focus into it, sets `.inv-scroll` overflow-y to hidden, and Escape closes it, restores overflow to auto and returns focus to the `New invoice` trigger. Tab from inside stayed inside the panel.",
      "NEW, FIX (minor). At 375 the destination segmented control is 38px tall (`.inv-seg-b` min-height 2.375rem, invoices-studio.css:308, with no bump in the 48rem query) and `Add a line` is 40px (`.inv-btn.sm` min-height 2.5rem in the same query). The list got its 44px pass and the slide over did not. These three radios are the control that decides where the money goes, on the width where the rule matters most."
     ]
    },
    {
     "key": "chase-drafter",
     "verdict": "SHIP",
     "issues": [
      "CLOSED, wrong sign-off. TS-1043 shows OWNER `Staci Bonnie` in the metadata grid and the generated body ends `Thanks,\\nStaci`. Source is `(inv.owner || 'Liam Miller').split(' ')[0]` at invoices-studio-data.jsx:370.",
      "CLOSED, the auto-written commercial commitment. `pause new work` returns false against the generated body. The firmer close now reads `If there is a hold up on your side, tell me what it is and I will work around it. If I have the wrong person, point me at whoever handles your payments and I will send it there.`, which commits the studio to nothing.",
      "Still good: the idle line states `Chased 2 times already, last on 1 Sep. The next draft comes back firmer.`, the section sub-line now says `signed by whoever owns the invoice`, and the actions are Send chase / Regenerate / Dismiss with nothing sending on its own."
     ]
    },
    {
     "key": "states",
     "verdict": "SHIP",
     "issues": [
      "CLOSED, loading asserting facts. All four tiles report empty value and empty sub-line with a skeleton present, all seven saved views report empty count and empty money with a skeleton. No `NZ$0` and no `Nothing out` anywhere in the pad while loading.",
      "CLOSED, inconsistent framing. Empty, error and no scope all report `framedInCard: true`.",
      "CLOSED, live actions on a dead ledger. Error and no scope both disable Export and New invoice with the reason attached (`The ledger did not load.` / `No client is in your scope.`), and both Import items in the overflow menu render disabled with the reason as a visible sub-line. Empty keeps both live and keeps its true zeros.",
      "Still good: private mode blurs 18 money nodes and leaves the counts readable at 375, and the read-only teammate seat greys only New invoice and keeps its footnote."
     ]
    },
    {
     "key": "design-system-conformance",
     "verdict": "SHIP",
     "issues": [
      "CLOSED, the one single-side border. `.inv-slide` now uses `box-shadow:inset 1px 0 0 var(--border)` (invoices-studio.css:422). A runtime scan of every element under `.inv` and `.inv-over` for a computed border on one, two or three sides returns an empty set on the list at 1280 light, on the detail at 1280 dark with the chase drafted and the danger zone open, and in the slide over at 375 dark.",
      "No horizontal overflow at 375 in any state: `.inv` and `.inv-scroll` both report 375/375 on the list, the detail, the slide over and the teammate seat.",
      "No console errors or page errors across every run.",
      "The row hairlines, the section dividers and the metadata grid all still draw with inset shadows rather than borders, so the surface keeps its own habit."
     ]
    }
   ],
   "summary": "Re-verdict: FIX, and much closer than the first pass. The fixer closed more than they claimed. Both blocking bugs are genuinely dead, verified by interaction and not by reading code: the row menu portals into the surface root, flips above the trigger on the last row and stays hit-testable at 1280 light and 375 dark, and clicking a menu item no longer falls through to the row; every Field caption is a SPAN, and clicking the picked client, the client caption, the \"Where it goes\" caption and the Currency caption all leave the selection and the payment rail untouched. Also closed and re-measured: no ghost cells in the metadata grid at any width (last row grows to flush, gap 0), the pay cards show only the rails in play with the bank account number leading on a manual invoice, the danger zone is inert at 0px and focusable when open, the disabled Delete keeps its red under hover, the loading state skeletons the tiles and the view counts so it no longer asserts NZ$0, all three failure states sit in a card with Export and New invoice disabled and their reasons on screen, the drafter signs the invoice owner (\"Staci\" on TS-1043) and no longer pre-writes a work pause, the 375 pass landed (44px icon buttons, two-up tiles, hidden scroller bar, live search hit area), and a runtime scan finds zero single-side borders anywhere in either theme with no console errors.\n\nFour things stand between this and ship, none of them structural. Two are real: at 375 the header More menu is clipped 63px off the left edge of the surface in both themes so every item label loses its first word, because the new flip logic clamps vertically but not horizontally; and the \"why it is disabled\" sentence is still the faintest text on the detail page at 1.97:1 light and 2.41:1 dark, because the DS bundle's blanket `button:disabled{opacity:.5}` still applies to `.inv-act` and the CSS comment claiming that was fixed is wrong. One is a repeat of an original finding on a path the fixer did not cover: the hero's \"Draft a chase email\" is an identical primary to the chase card's, and the hero one only scrolls. One is minor: the slide over missed the 44px pass, so the destination radios that route the money are 38px on a phone.\n\nFix locations: invoices-studio-kit.jsx:288-305 (clamp the menu's left/right to the host box), invoices-studio.css:569 (add `opacity:1` to `.inv-act:disabled`), invoices-studio-data.jsx:259 with invoices-studio.jsx:184 and 274 (the duplicate chase label), invoices-studio.css:308 and the 48rem query around line 659 (`.inv-seg-b` and `.inv-btn.sm` under 44px). Evidence: screenshots in C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\invcrit\\, probes and driver in C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\inv-critic2\\, served copies of the four files byte-identical to the project at C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\inv-critic2\\invoices-studio*."
  }
 ]
}