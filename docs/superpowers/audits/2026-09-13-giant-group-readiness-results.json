[
 {
  "label": "",
  "findings": [
   {
    "id": "MC7-needsOrgList",
    "area": "ManyRequests importer, lib/import/manyrequests/run.ts (needsOrgList) + snapshot-client.ts (dead clients key)",
    "severity": "minor",
    "claim": "A requests-, messages-, or invoices-only import run never fetches the ManyRequests organisations list, so any client whose D1 organisation is matched only through the hand-typed ORG_NAME_MATCHES table (i.e. not yet stamped with manyrequestsId) is refused with 'Could not resolve its organisation ... Run the organisations entity first' instead of matched. Giant Group itself is NOT hit by this tonight, its org (id 54) and its one request (334) were already stamped with manyrequestsId on the 7 Sep apply, so orgIdByKey resolves them from the D1 snapshot alone regardless of whether 'organisations' is in the requested entity list. The bug matters for (a) any of the other 19 ManyRequests clients if their org row's manyrequestsId stamp were ever lost, (b) any brand-new ManyRequests org onboarded after 7 Sep, and (c) running against a fresh/staging D1 that has never had the organisations entity applied. Separately, the snapshot payload/client interface carries a 'clients' key (ManyRequestsClient.listClients / ManyRequestsSnapshotPayload.clients) that fetchImportSource never calls, contacts are read via listOrgMembers per-org instead, so it is dead code that should be dropped or documented.",
    "evidence": "lib/import/manyrequests/run.ts:82-89 (needsOrgList returns true only for 'organisations' | 'contacts' | 'brands' | 'subscriptions', false for 'requests' | 'messages' | 'invoices'); the refusal text lives at lib/import/manyrequests/plan.ts:1137-1144 (requests) and :1530-1537 (invoices), both reading 'Run the organisations entity first'; TASKS.md:188 files this open as 'MC.7 (0.5d) ... today a subset run refuses every hand-mapped client with \"Run the organisations entity first\"' and is still unchecked as of 2026-09-12; docs/superpowers/plans/2026-09-13-mvp-100-run.md:26 (Batch A1) explicitly schedules 'MC.7 fixed first so a subset run resolves names' before the planned fresh messages-only snapshot re-import; dead key confirmed at lib/import/manyrequests/snapshot-client.ts:234 and client.ts:257 (both implement listClients/snapshot.clients) versus lib/import/manyrequests/run.ts's fetchImportSource, which never calls client.listClients() anywhere in its org-scoped or top-level reads (grepped, only test files and the two client implementations reference it).",
    "fix": "In lib/import/manyrequests/run.ts, widen needsOrgList(entities) to also return true when entities has 'requests', 'messages', or 'invoices' (simplest: true unless the only entities selected are 'team' and/or 'services'). This is safe and cheap, fetching the organisations list is idempotent and already happens on every full run. Then in snapshot-client.ts / client.ts / types.ts, either remove listClients from the ManyRequestsClient interface and drop 'clients' from SNAPSHOT_KEYS and ManyRequestsSnapshotPayload (updating snapshot-client.test.ts and client.test.ts's listClients assertions), or wire it in as a genuine fallback contact source and add a code comment explaining when it is consulted. Also add the hostile-input validator tests TASKS.md:188 asks for in lib/import/manyrequests/__tests__/snapshot-client.test.ts (malformed rows per SNAPSHOT_KEYS entry, already partially covered by checkRows but not exhaustively per the task note).",
    "effortDays": 0.5,
    "needsLiam": false,
    "needsRealSession": false
   }
  ],
  "verified_ok": [
   "Giant Group's ManyRequests-side delta since the 7 Sep import is zero. Live MCP reads today (2026-09-12): find_organizations('Giant Group') -> org id 54, 2 members (Mickey Day owner, Mark Ramsey); find_requests(search='Giant Group' / 'Mickey Day' / 'Mark Ramsey', limit 25) each return exactly one request, id 334 / #243 'Create tasks for design + dev', status Submitted; get_request(334, include=[comments,activity]) shows created_at 2026-08-14T09:31:50Z (well before the 7 Sep import), comments: [] / comments_total 0, activity: a single REQUEST_CREATED event / activity_total 1, no status change, no reply, no edit since creation. find_invoices(organization_id=54) returns zero invoices. A messages-only (or full) re-import for Giant Group today would carry nothing new.",
   "Giant Group's cancel-readiness at ManyRequests cancellation (16 Sep) carries no data-loss risk. get_organization(54, include=[members,brands,subscriptions,balance]) returns brands: [] (0), subscriptions: [] (0), balance: {hours:null, credits:null} (never had a bank). The 2026-09-07 report's cancel-readiness risks (Physitrack's 3 brands with no D1 brands-table equivalent stamped; the various hour-bank retainers for Glasswall/Greyhive/Dante Media/Elevate/etc.; per-period pricing_variations[] with no D1 column) are all real for OTHER ManyRequests clients but do not apply to Giant Group, whose actual Scale-plan / GBP / 1 small + 1 large track subscription lives natively in D1 (organisations/subscriptions/tracks), never touched by the ManyRequests import.",
   "Messages-only re-import mechanics are sound by code inspection. Idempotency key: map.ts commentKey() (lines 399-405) hashes (request manyrequestsId, comment created_at, author-name slug) into messages.manyrequestsId (migration 0093, UNIQUE). plan.ts planMessages (lines ~1297-1339) treats an already-present key as plan.unchanged (no write, no updatedAt touch) and only plans an INSERT for genuinely new comments, a re-run cannot duplicate or re-touch existing rows. Mail-silence is enforced by upsert.ts readMailProbe (lines 278-288), which counts notifications and email_suppressions before and after every run (dry or applied); route.ts (lines 259-268) turns any movement into a loud 'MAIL PROBE MOVED' warning, and a missing email_suppressions table degrades gracefully to a named 'MAIL PROBE DEGRADED' warning rather than a false pass. lib/import/manyrequests/__tests__/no-mail-imports.test.ts statically walks the whole reachable module graph of the library AND both route files (app/api/admin/import/manyrequests/route.ts and the cleanup route), banning createNotifications, sendEmail, createInvitation and api.resend.com references by name, per the 2026-09-07 report's review notes.",
   "Snapshot production path confirmed by reading the route docstring (app/api/admin/import/manyrequests/route.ts lines 30-46) and lib/import/manyrequests/snapshot-storage.ts: an operator reads organizations / membersByOrg / brandsByOrg / subscriptionsByOrg / services / requests / invoices read-only through the ManyRequests MCP connector (the requests half is exactly the find_requests + get_request calls used live above), assembles the 8 keys from snapshot-client.ts's SNAPSHOT_KEYS, uploads with `wrangler r2 object put tahi-storage/imports/manyrequests/<name>.json --remote`, and POSTs {snapshotKey} to the import route. snapshot-storage.ts constrains the key to imports/manyrequests/<name>.json (SNAPSHOT_KEY_PATTERN), caps it at 25MB, and reads it server-side through the same validateSnapshotPayload the inline body.snapshot path uses, no MANYREQUESTS_API_TOKEN is needed on this route for a snapshot-driven run."
  ],
  "notes": "Scope: this pass was read-only exploration of lib/import/manyrequests/*, app/api/admin/import/manyrequests/route.ts, TASKS.md, the 2026-09-07 import report, and four live read-only ManyRequests MCP calls (find_organizations, get_organization, find_requests x3, get_request, find_invoices, all reads, no write tool touched, per the brief). No file was modified. No dashboard route was called and no D1 write happened.\n\nCaveat on Q3: find_requests does not expose an updated_at field directly (its docstring says results sort 'most recently updated first' but the returned shape has no updated_at column), so 'nothing changed since 2026-09-07' is inferred from three consistent facts rather than a direct updated_at filter: (a) get_request(334)'s only activity event is REQUEST_CREATED at 2026-08-14, (b) comments/comments_total are both zero, (c) three independent search terms ('Giant Group', 'Mickey Day', 'Mark Ramsey') each return the exact same single request with has_more:false, so there is nothing else on this org's account to have changed. If Liam wants a stronger guarantee than this, get_request's activity_total on 334 (currently 1) is the cheapest single number to recheck before any future re-import.\n\nThe MC.7 finding is filed as 'minor' rather than 'major/blocker' specifically because it does not affect Giant Group's readiness tonight (its org and only request are already stamped), its real cost is to the safety of running import-entity subsets against the OTHER 19 ManyRequests clients or any future onboarding, which is exactly why docs/superpowers/plans/2026-09-13-mvp-100-run.md schedules fixing it as a precondition of Batch A1's planned messages-only re-import, not as a Giant-Group blocker.\n\nNothing in this pass needed Liam's input and nothing needed a real client session, both flags are false throughout because the entire brief was backend/import code plus live ManyRequests portal reads."
 },
 {
  "label": "",
  "findings": [
   {
    "id": "F1-onboarding-existing-retainer-payment-step",
    "area": "onboarding gate / /onboarding step sequencing",
    "severity": "blocker",
    "claim": "Giant Group's two contacts (Mickey Day, Mark Ramsey), once invited through the exact panel Liam will use tonight, are routed through a real Choose-Plan + Stripe-card-payment step instead of straight to kickoff. If either actually pays, it creates a brand-new live Stripe subscription for a client who is already an active Scale retainer invoiced through Xero at a negotiated custom rate (GBP 2,000/mo) -- the pay screen shows a hardcoded 'Scale $4,000/mo' USD card that matches nothing about their real deal. They also never get a kickoff-booking step at all, contradicting the MVP's own acceptance bar ('with the kickoff booked').",
    "evidence": "components/tahi/onboarding-content.tsx buildSteps() (~lines 94-102): for clientType==='existing', the code returns ['welcome','plan','pay'] when engagement==='retainer' and only skips to ['welcome','kickoff'] when engagement==='project' -- the opposite of what lib/onboarding-invites.ts personaForPlanType()'s own doc comment promises ('a client the studio set up is never shown a payment step on their own workspace... alreadyEngaged'). Giant Group planType='scale' -> personaForPlanType() resolves 'existing_retainer' (confirmed via MCP get_client: org.planType='scale', subscription.status='active', customMrr=2000 GBP). app/api/portal/checkout/route.ts never checks for an existing active subscription or org.invoiceChannel before creating a new Stripe customer+subscription. components/tahi/onboarding-payment.tsx PLANS array hardcodes scale at $4,000/mo, unrelated to Giant Group's GBP 2,000/mo custom rate. list_client_contacts confirms both Mickey Day and Mark Ramsey still have clerkUserId=null (never invited yet), so this path is untested against their real accounts.",
    "fix": "In components/tahi/onboarding-content.tsx buildSteps(engagement, clientType): for clientType==='existing', return the same no-payment step list regardless of engagement (e.g. always ['welcome','kickoff'], matching the existing 'project' branch), never 'plan'/'pay'. This is a single conditional fix. After the fix, confirm live (or in the Tahi Test Client harness reconfigured to a retainer/existing persona, since the current T1.4 acceptance script apparently only exercised a persona that already skips 'pay' -- which is why this survived) that an existing_retainer invite lands the contact on welcome -> kickoff with no Stripe UI shown, and that /api/portal/checkout is never called for that persona.",
    "effortDays": 0.25,
    "needsLiam": false,
    "needsRealSession": true
   },
   {
    "id": "F2-clerk-webhook-unset-second-seat-gap",
    "area": "Clerk webhook / second-seat identity linking",
    "severity": "major",
    "claim": "CLERK_WEBHOOK_SECRET is unset in production, so app/api/webhooks/clerk answers 503 to every Clerk delivery. This does NOT block tonight's plan for Mickey/Mark specifically (each gets their own admin-minted onboarding-invites token, and accept-invite writes contacts.clerkUserId synchronously in the same request that consumes the token; a second synchronous fallback, linkContactOnSignIn, also runs on every load of any (dashboard) page before the onboarding redirect). What genuinely stays broken while the secret is unset: (a) a colleague invited later through the in-portal Clerk-native invite (POST /api/portal/invites, used for a client adding their own teammates) has zero server-side recovery if their browser never completes a round trip to a (dashboard) page -- they hold a valid Clerk login with no portal identity (no role, no notifications, messages stamped with a raw Clerk id) until they happen to load a dashboard page; (b) organizationMembership.deleted never fires, so a removed teammate's clerkUserId link is never cleared (stale data/audit only, not an access risk since Clerk itself revokes the session).",
    "evidence": "app/api/webhooks/clerk/route.ts lines 58-65 (503 with 'CLERK_WEBHOOK_SECRET is not configured; refusing delivery' when unset). app/(dashboard)/layout.tsx line 94 + lib/contact-link-server.ts (the synchronous fallback that makes tonight's specific flow work without the webhook). app/api/portal/accept-invite/route.ts lines 170-192 (the admin-invite path writes clerkUserId directly, also independent of the webhook). app/api/portal/invites/route.ts (the Clerk-native colleague-invite path that has no synchronous linker of its own and is the one the webhook exists to backstop).",
    "fix": "Operator-only: set CLERK_WEBHOOK_SECRET on both the production and staging workers, then in the Clerk dashboard register https://portal.tahi.studio/api/webhooks/clerk for user.created, user.updated, organizationMembership.created, organizationMembership.deleted. Already itemised as step 2 of the run's own pre-Sunday checklist; no application code change is required.",
    "effortDays": 0,
    "needsLiam": true,
    "needsRealSession": false
   },
   {
    "id": "F3-clerk-org-creation-task-on-first-signup",
    "area": "Clerk sign-up / org-creation task",
    "severity": "major",
    "claim": "Mickey Day and Mark Ramsey have never signed up (clerkUserId null on both). On their first-ever Clerk sign-up, if this Clerk instance's Organizations settings still have the post-sign-up 'create/select an organization' task enabled, Clerk's own hosted sign-up widget will prompt them to name/create a workspace of their own before the app ever gets a chance to route them into /continue and consume their invite token -- a confusing extra screen for someone who is joining a pre-existing Giant Group workspace, not starting a new one. It self-heals (the invite-accept flow reactivates them into the real org afterward, and any throwaway Clerk org is simply abandoned/orphaned) but it is a real point of first-impression confusion this app's own code comments flag as a known possibility, not a deliberate design.",
    "evidence": "components/tahi/onboarding-content.tsx lines 347-351: 'Self-serve clients need a linked D1 org before the pay step. Clerk may have already created their Clerk org during sign-up (the org-creation task)'. This is a Clerk-instance (Dashboard > Organizations settings) toggle, not something the app code controls via the mounted <SignUp> component's props.",
    "fix": "Liam checks the Clerk Dashboard's Organizations settings for this instance and, if the 'require an organization after sign-up' task is on, turns it off (client sign-ups here always join a pre-created org via invite token, they never need to create their own). Confirm with one real sign-up as either Mickey Day or Mark Ramsey's email in a throwaway/incognito session before the real invite is sent.",
    "effortDays": 0.1,
    "needsLiam": true,
    "needsRealSession": true
   }
  ],
  "verified_ok": [
   "Invite panel (app/(dashboard)/clients/[id]/tabs/people.tsx -> POST /api/admin/onboarding-invites) mints a 24-byte (~192-bit) crypto-random token via generateInviteToken(), binds it to the exact contactEmail, derives persona from the org's plan (personaForPlanType), reuses a live unexpired invite instead of spraying new tokens on retry (ensureClientInvite), and its UI keeps the copy-link fallback visible on a failed send rather than swallowing it.",
   "email.allowedOrgIds is literally \"[]\" in the live settings today (confirmed via MCP get_settings) -- Giant Group is NOT exempt, so any attempt to email Mickey Day or Mark Ramsey an invite is correctly withheld by lib/email-gate.ts/lib/email-delivery.ts before it reaches Resend and is logged to email_suppressions, matching Liam's 'prepare but do not invite' instruction exactly.",
   "POST /api/portal/accept-invite (the token-accept route) is single-use via an atomic UPDATE ... WHERE used_at IS NULL, binds acceptance to the signed-in user's VERIFIED primary Clerk email matching invite.contactEmail, enforces the 14-day expiry, and its admin/member promotion logic never demotes an existing admin.",
   "Giant Group's imported contacts already carry a real (non-null) portalRole from the ManyRequests import -- confirmed via MCP list_client_contacts: Mickey Day = portalRole 'admin' + isPrimary true, Mark Ramsey = portalRole 'member' -- so the accept-invite 'first acceptor becomes owner' race condition described in its own comments cannot occur for this org regardless of which of the two accepts first.",
   "lib/portal-access.ts isOrgAdmin correctly denies a portalRole 'member' contact (Mark Ramsey) write access to organisation settings, people/invites, and subscription changes, matching the deny-by-default model; Mickey Day (admin/primary) is correctly allowed.",
   "POST /api/onboarding/complete's 'invoice me' path (recordInvoiceMe) now entitles the caller and raises a real draft invoice + admin notification instead of returning a 402 that used to strand the client (the historical B5 bug); it is properly restricted to the org's own workspace admin and is idempotent.",
   "POST /api/portal/calls (kickoff booking) writes a real scheduledCalls row with the chosen ISO slot, resolves the org's assigned PM as host, and sends a confirmation email -- this works correctly for any persona whose step list actually includes 'kickoff' (e.g. existing_project and the new-client flows); it just never gets reached for Giant Group's own persona today (see F1).",
   "Sign-in/sign-up are mounted as Clerk's own catch-all components with routing:'path' (components/tahi/clerk-mount.tsx), and middleware.ts's isPublicRoute matcher covers '/sign-in(.*)' and '/sign-up(.*)', so Clerk's built-in forgot-password/reset-password/verification sub-routes are reachable with no auth-wall interference in code; whether the Clerk instance itself has the password-reset strategy turned on still needs one live click-through (T1.3's own stated remaining step)."
  ],
  "notes": "Scope covered: invite-mint panel + POST route, ClientInviteEmail template + the allowlist door (lib/email-delivery.ts, lib/email-gate.ts, lib/email-allowlist.ts, live settings read via MCP), the 192-bit onboarding-invites token + /api/portal/accept-invite, the Clerk webhook route and its 503-when-unset behaviour plus the synchronous fallback that actually covers tonight's flow, /continue's workspace-choice logic, the onboarding gate in app/(dashboard)/layout.tsx, the onboarding step machine and its kickoff/invoice-me/checkout code paths, the Clerk sign-in/sign-up catch-all + middleware public-route matcher for forgot-password, and lib/portal-access.ts against Giant Group's real contact rows (read live via MCP list_client_contacts / get_client / get_settings). No files were modified. The one blocker (F1) is fully provable from source alone and is squarely inside tonight's exact scenario (existing Scale-plan retainer client, invited via the client-detail panel) -- I'd fix that before minting any real invite for Giant Group, independent of the allowlist decision. F2 and F3 are real but do not block tonight's specific two invites; they matter for the next colleague either contact adds later, and for Liam's own Clerk-dashboard configuration respectively."
 },
 {
  "label": "",
  "findings": [
   {
    "id": "F1",
    "area": "Messages feature visibility for Giant Group",
    "severity": "major",
    "claim": "Liam's standing rule is that the standalone /messages page (full conversations/DM/group-chat UI) is hidden for clients and the request thread is the only client channel. In the current code and current production permission state, nothing hides it: the `messages` FEATURE_TREE node applies to both audiences with no `unavailable` note, `decideFeature` defaults an unrestricted client-level caller to `true` with no override, and the client nav model marks Messages `clientVisible: true`. A live read of the permission overrides for Giant Group's own org and for the generic client role both come back empty. Unless this is set before the invite goes out, Mickey Day and Mark Ramsey will see and can use the full standalone Messages page (create direct/group conversations, not just the request thread) the moment they sign in.",
    "evidence": "lib/feature-tree.ts:54-60 (messages node, appliesTo: ['team','client'], no unavailable flag); lib/permissions.ts:188-189 (`if (access.level === 'admin' || access.level === 'client') return true` with no override present); components/tahi/nav-model.tsx:138 (`{ label: 'Messages', href: '/messages', icon: 'messages', clientVisible: true }`); app/(dashboard)/messages/page.tsx:29-38 (one route, client branch renders MessagesContent for any client whose feature check passes); live check via mcp__claude_ai_Tahi_Dashboard__get_feature_visibility(organisation, aa80a2d6-0494-424d-8ccc-9af524c31fa7) -> {\"overrides\":[]} and get_feature_visibility(role, client) -> {\"overrides\":[]}, both run 2026-09-12.",
    "fix": "Apply a feature_visibility deny override on the `messages` key before Giant Group is invited: either subjectType 'role'/'client' (turns it off for every client org, matching 'clients are studio-first via the request thread' as a platform-wide rule) or the narrower subjectType 'organisation'/aa80a2d6-0494-424d-8ccc-9af524c31fa7 if only Giant Group should be held back. Use the existing mcp__claude_ai_Tahi_Dashboard__set_feature_visibility tool or the /permissions builder UI; no code change needed, the enforcement (nav filter + page redirect + both API 403s) already reads this one key per lib/feature-tree.ts's own comment at line 46-52. Confirm afterwards with the same get_feature_visibility call and a nav check that 'Messages' is gone from the client sidebar.",
    "effortDays": 0.1,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "F2",
    "area": "Cross-org isolation e2e coverage (Batch A4 / T1.5)",
    "severity": "major",
    "claim": "No Playwright proof of cross-org isolation exists today. e2e/portal-flow.spec.ts is entirely test.skip(true, ...) (all 8 cases dead). The only working Clerk-based e2e (e2e/onboarding-personas.spec.ts) signs up one org at a time and never compares two. Tenancy assurance today rests entirely on ~90 Vitest unit tests that call route handlers directly with a mocked D1, never on a real running app with two real signed-in client sessions. A4's own acceptance bar ('the spec is green in CI') is 0% built.",
    "evidence": "e2e/portal-flow.spec.ts:9 test.skip(true, 'Requires Clerk auth setup for CI'); grep for org2/otherOrg/cross-org across e2e/*.spec.ts returns nothing; app/api/__tests__ inventory (see notes) shows the tenancy proof is unit-level only.",
    "fix": "New file e2e/tenancy-isolation.spec.ts, run under the default playwright.config.ts (port 3000, webServer auto-starts npm run dev, globalSetup already runs clerkSetup and .env.local carries a pk_test_ Clerk key). Seed: createTestOrg + mintInvite (e2e/helpers/invites.ts) for two orgs, sign up two distinct +clerk_test@example.com Clerk test users through the real invite links exactly like onboarding-personas.spec.ts personas 2/3, save each storageState and build a Playwright APIRequestContext per org's session (faster and more stable in CI than driving two full browser UIs). As the admin bypass context, seed one request, one file (via presign+proxy or admin confirm), one conversation, one non-draft invoice, one contract, and one scheduled call for EACH org, so every org has a real, non-guessable-but-known id to attempt against the other. Assertions, run Org A's session against every Org B id and mirrored back: GET/PATCH /api/portal/requests/{id}, POST .../review, /reads, /steps, /sub-requests, /files all 404 (never 403, matching the existing 'not-org-scoped == not-found' convention in portal-request-projection.test.ts); GET /api/portal/messages/request/{id} 404, POST there inserts nothing (verify via an admin-side read); GET /api/uploads/serve?key=<org B's real storageKey> and a guessed <orgB>/general/...-file.pdf legacy-shaped key both 403; PUT /api/uploads/proxy?key=<orgB prefix> 403; GET /api/portal/invoices/{id} 404 and the list never contains the other org's invoice id; for the id-less routes (project, calls, team, tracks, files, notifications, announcements, conversations) assert the payload contains none of a denylist of Org B's known strings (org name, request id, filename, conversation id, call id) since there is no [id] to 404 on. Contracts have no portal route at all (see F4): test the PUBLIC share-token path instead (Org A's token against Org B's contract id/signer id must 404). Add an adversarial sub-block sweeping every route above with: a never-issued random UUID (expect 404, never 500), a trailing-slash variant, and `?orgId=<other org>` / `{orgId: <other org>}` appended to every JSON body (expect no effect, this specifically re-proves the isService-gated pattern in app/api/portal/messages/_shared.ts cannot be triggered by a real session). Wire into an npm script (test:e2e:tenancy) and STATUS.md once green. Budget ~30-45s per run for the two OTP sign-ups; keep isolated in its own file per the parallel-run-flake lesson in the QA worktree notes (rerun with --workers=1 if flaky).",
    "effortDays": 1,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "F3",
    "area": "e2e harness base-URL mismatch risk",
    "severity": "major",
    "claim": "e2e/helpers/invites.ts hardcodes BASE = 'http://localhost:3000' for its admin bootstrap calls (createTestOrg, mintInvite), independent of whatever server the Playwright browser session is actually driving. Today this matches playwright.config.ts's baseURL (also 3000), so onboarding-personas.spec.ts is fine. But the documented QA workaround for a second dev server on port 3179 (used specifically to avoid two agents fighting over the same dev server, per memory project_qa_worktree_recipe.md) would silently seed orgs/invites against the WRONG server/D1 if the new A4 spec is ever run there, producing confusing false failures rather than an honest error.",
    "evidence": "e2e/helpers/invites.ts:12 `const BASE = 'http://localhost:3000'`; playwright.config.ts:16 baseURL: 'http://localhost:3000'; memory project_qa_worktree_recipe.md describes a parallel harness on port 3179 (`npx next dev --port 3179`, gitignored playwright.local.config.ts) built for exactly the concurrent-session situation tonight's run is in.",
    "fix": "Change line 12 to `const BASE = process.env.PLAYWRIGHT_TEST_BASE_URL ?? 'http://localhost:3000'` (Playwright's own test runner already exports PLAYWRIGHT_TEST_BASE_URL from the active config's baseURL, so no extra wiring is needed) and set it explicitly when invoking the tahi-qa harness: `npx playwright test -c playwright.local.config.ts e2e/tenancy-isolation.spec.ts` with `PLAYWRIGHT_TEST_BASE_URL=http://localhost:3179` in the environment.",
    "effortDays": 0.1,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "F4",
    "area": "Contracts have no portal API surface to test",
    "severity": "minor",
    "claim": "The A4 acceptance list names contracts as one of the client-facing surfaces to prove isolation on, but no GET /api/portal/contracts (or any portal contracts route) exists at all -- only /api/admin/contracts/* (studio) and /api/public/contracts/[token]/* (unauthenticated share link) do. This matches STATUS.md's already-known B4 note that the client nav's Contracts item bounces to /requests. There is nothing an authenticated client session can reach to run a cross-org GET/PATCH test against.",
    "evidence": "`find app/api -path \"*contract*\" -name route.ts` lists only app/api/admin/contracts/... and app/api/public/contracts/[token]/...; no app/api/portal/contracts directory exists.",
    "fix": "Scope A4's contracts assertion to what is actually reachable: the public share-token route (already spot-checked sound -- app/api/public/contracts/[token]/route.ts:16 validates a 20-64 char token pattern and looks up strictly by the random publicShareToken column, never by org or sequential id, 404s on unknown, 410s on cancelled/expired). Write the A4 case as 'Org A's contract token cannot be used to sign or read Org B's contract/signer id' rather than an authenticated cross-org test, and note in STATUS.md that portal-authenticated contract isolation is not applicable until a portal contracts route is built.",
    "effortDays": 0.25,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "F5",
    "area": "Untested (but correctly-scoped-on-read) portal routes",
    "severity": "minor",
    "claim": "Nine portal route handlers a Giant Group contact will hit weekly have zero test coverage at the HTTP-handler level (no Vitest imports the route module, no e2e touches it): GET /api/portal/files, GET+POST /api/portal/conversations, GET /api/portal/team, GET+PATCH /api/portal/notifications, GET /api/portal/announcements, GET /api/uploads/serve, PUT /api/uploads/proxy, GET /api/portal/requests/[id]/sub-requests, and POST /api/portal/requests/[id]/review (its siblings PATCH/reads/team-fanout on the same request DO have an explicit cross-org 404 test; review does not). Direct code review of every one of these shows correct org-scoping (every query filters on the session-derived orgId, never a client-supplied value), so this is a proof gap, not a confirmed bug, but it is currently unproven and it is exactly the surface Liam's month-long trial will exercise.",
    "evidence": "grep across app/api/__tests__ for each route's literal path and for a direct `from '@/app/api/portal/<route>'` import both return nothing for the nine routes listed; each route was read in full and its WHERE clause confirmed org-scoped (e.g. app/api/portal/files/route.ts:106 `eq(schema.files.orgId, orgId)`; app/api/portal/team/route.ts:40-43; app/api/portal/requests/[id]/sub-requests/route.ts:47-51 and 71-75; app/api/portal/requests/[id]/review/route.ts:93-97).",
    "fix": "Add direct-import Vitest cases for each of the nine routes mirroring the existing pattern (seed two orgs' rows, assert org B's row/thread is invisible or 404s to org A's session) -- see portal-request-projection.test.ts and portal-invoice-drafts-hidden.test.ts as templates -- or fold the coverage into the new A4 Playwright spec's per-route sweep (F2) instead of writing a tenth near-duplicate unit-test file.",
    "effortDays": 0.75,
    "needsLiam": false,
    "needsRealSession": false
   }
  ],
  "verified_ok": [
   "Request thread (the client's actual channel per Liam's rule) is the best-proven surface in the codebase: GET/PATCH /api/portal/requests/[id], POST .../review, .../reads, .../steps all 404 a request that does not resolve under the caller's own org (portal-request-projection.test.ts:333, portal-request-patch.test.ts:150, portal-request-reads.test.ts:165, portal-request-team-fanout.test.ts:326), and 403 the Tahi admin org and impersonation writes.",
   "Invoices: GET /api/portal/invoices/[id] and the list route both filter orgId inside the WHERE clause (not a post-check), confirmed both by direct code read (app/api/portal/invoices/[id]/route.ts:109-113) and by explicit cross-org 404 tests (portal-invoice-drafts-hidden.test.ts:165, billing-invoice-flow.test.ts:275).",
   "Uploads: /api/uploads/confirm, /api/uploads/serve and /api/uploads/proxy all route through the shared lib/upload-access.ts helpers (decideUploadRead, resolveD1OrgId, resolveOwnerOrgForUpload, resolveTargetOrgId), which carry 42 unit-test cases across admin-scope, Clerk-vs-D1 id-space, legacy-key and cross-org-deny paths (upload-access.test.ts); uploads/confirm additionally has a direct test proving a spoofed body.orgId from a non-admin is ignored (uploads-confirm.test.ts:210) and that a non-admin cannot confirm a key under, or attach to a request in, another org (lines 247, 258).",
   "Services catalogue: services-org-scope.test.ts explicitly proves org A never sees org B's private row and the reverse (line 172), plus 8 more scoped read/write cases on both the portal and admin sides.",
   "/api/portal/messages/[source]/[id]: the channel/<id> path ignores whatever id is in the URL and always resolves the room from the authenticated org (route.ts:90-103, tested at messages-routes.test.ts:230); the one place a client-suppliable body.orgId is read at all is gated behind userId === 'api-service' (a server-to-server TAHI_API_TOKEN identity minted only for the worker MCP), unreachable from a real client browser session -- verified by reading app/api/portal/messages/_shared.ts:72-78 directly.",
   "Public share tokens (contract sampled): app/api/public/contracts/[token]/route.ts validates a 20-64 char token pattern and looks up strictly by the random publicShareToken column, 404s on unknown and 410s on cancelled/expired -- matches the 2026-08-18 security audit's conclusion that share tokens are non-enumerable and format-validated.",
   "Direct code read of /api/portal/project, /api/portal/calls, /api/portal/tracks/[trackId]/reorder, /api/portal/onboarding, /api/portal/team, /api/portal/files, /api/portal/conversations, /api/portal/notifications: every query is filtered on the session-derived orgId, never a client-supplied value; the track reorder route additionally re-verifies both the track's parent subscription org AND every individual request id's org before writing (app/api/portal/tracks/[trackId]/reorder/route.ts:62-95).",
   "The local Playwright harness genuinely supports two independent real client sessions (not just the single admin dev-bypass identity): e2e/helpers/invites.ts's createTestOrg + mintInvite mint a fresh D1 org and invite token through the admin bypass, and e2e/onboarding-personas.spec.ts proves the full Clerk test-mode sign-up round trip (+clerk_test@example.com email, fixed OTP via enterTestOtpCode()) lands a real, distinct portal session; .env.local's NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY is pk_test_..., confirming this is a genuine Clerk test/dev instance safe to sign up throwaway users against."
  ],
  "notes": "Scope: this pass covered the client-facing PORTAL API surface only (the axis the task asked about: Giant Group vs a second client org), not the separate team-member access-scoping axis (teamMemberAccess / scopedOrgIds on admin routes) -- that is a different, already-partly-covered concern (admin-org-scoping.test.ts, admin-requests-scoping.test.ts, admin-scoping-routes.test.ts, sales-artifact-scope.test.ts) and out of tonight's client-invite question.\n\nTop-line: no reachable cross-org data leak was found anywhere in the portal API surface reviewed (18 of the 19 routes named in the brief were read line-by-line; contracts has no portal route to review, see F4). The portal boundary looks exactly as strong as the 2026-08-18 audit found it 25 days ago -- every read/write is scoped by the session-derived orgId, not a client-supplied one, and the one place a body.orgId is honoured (messages) is correctly gated behind a server-only service-token identity. The two 'major' findings that are NOT proof-gaps but live-today facts are F1 (messages is visible to clients by default, contradicting the stated rule, confirmed via a live permission-overrides read) and F3 (a latent harness base-URL trap that would silently invalidate the new A4 spec if run through the wrong config).\n\nEverything else is 'this isn't proven in CI yet,' not 'this is broken.' F2 is the actual A4 deliverable spec design, written concretely enough to hand to a builder without further exploration. Total new effort if all five are actioned: about 2.25 engineer-days, close to the run plan's own 1-day estimate for A4 alone once F1/F3/F4/F5 are folded in as sub-tasks rather than separate items.\n\nOther agents (main, mc9-fixer) are active in this session per the system reminder; two stale/orphaned agent worktrees (.claude/worktrees/wf_00d221bb-298-3, wf_4b495d30-6a8-1) exist on disk with their own copies of the files this audit reviewed -- I read only the main tree's copies and did not touch either worktree, consistent with 'do not modify any file.'"
 },
 {
  "label": "",
  "findings": [
   {
    "id": "B2-1",
    "area": "Portal money card (home + Services) shows a fabricated rate, not the client's real bill",
    "severity": "blocker",
    "claim": "Giant Group's home page 'Your plan' card and the Services page 'Rate' fact will show a generic Scale-tier stock rate (NZD 4000, FX-converted) instead of their real negotiated GBP 2,000/month (previously GBP 4,120/month, confirmed against three paid Xero invoices). A client who checks their own bill against their own portal will see two different numbers.",
    "evidence": "app/api/portal/subscription/route.ts:70-72 computes `monthlyRate = catalogPlan?.monthlyRate ?? PLAN_MONTHLY_RATES[sub.planType] ?? 0` and never reads `organisations.customMrr`. lib/billing.ts:33 `PLAN_MONTHLY_RATES.scale = 4000`; lib/plan-catalog-shared.ts:56 DEFAULT_PLAN_COPY scale.base = 4000 (comment: 'Monthly base price in NZD'). get_settings shows no `plan_catalog` settings row exists, so the 4000 NZD default is what resolves. MCP get_client(aa80a2d6-0494-424d-8ccc-9af524c31fa7) returns org.customMrr = 2000, org.preferredCurrency = 'GBP', org.healthNote = 'GBP 2,000/mo from September 2026 (was GBP 4,120)... Evidence from Xero: three invoices issued at GBP 4,120, all paid', the real, audited billing truth. This value is never read by app/api/portal/subscription/route.ts. By contrast `customMrr` is the established source of truth used in app/api/admin/clients/[id]/route.ts, app/(dashboard)/clients/[id]/_kit/*, app/api/admin/reports/retainer-health/route.ts, app/api/admin/reports/cash-flow-forecast/route.ts, app/api/admin/billing/financial-health/route.ts and lib/billing-derivation.ts (Decision #037 per project memory), just not in the one route the portal reads. The resulting `monthlyRate` flows straight to components/tahi/overview/homes/client-home.tsx:1618-1619 ('Your plan' card) and components/tahi/portal/services/portal-services.tsx:393 (PlanPanel 'Rate' fact) with no other correction along the way.",
    "fix": "In app/api/portal/subscription/route.ts, select `customMrr` (and ideally `preferredCurrency`) from `schema.organisations` alongside the existing subscription lookup (same query already filters on `orgId`), then compute `const monthlyRate = org.customMrr ?? catalogPlan?.monthlyRate ?? PLAN_MONTHLY_RATES[sub.planType] ?? 0` -- customMrr wins when set, exactly like every admin-side surface already does. No schema change (organisations.customMrr already exists), no other file needs to change: client-home.tsx and portal-services.tsx already just render `subscription.monthlyRate` as given.",
    "effortDays": 0.5,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "B2-2",
    "area": "Portal tracks widget (home + Services) undercounts Giant Group's custom track entitlement",
    "severity": "blocker",
    "claim": "Giant Group is entitled to 2 tracks (1 small + 1 large, tracksMode='custom'), but only 1 real track row exists in the DB (the small one). The home page's 'Your work in motion' board and 'Your plan' card, and the Services page's 'Tracks running' fact, all read raw DB rows with no entitlement reconciliation, so they will show '1 track' / one lane all month. A large_task request Giant Group submits has no lane to render in on the home board at all (its queue filter only ever sees the one track type that has a real row).",
    "evidence": "app/api/portal/tracks/route.ts:44-47 and app/api/portal/subscription/route.ts:96-101 both do a bare `select().from(schema.tracks).where(eq(subscriptionId, sub.id))` and never call resolveTracksConfig/buildEffectiveTracks. Consumers: components/tahi/overview/homes/client-home.tsx:985 (`/api/portal/tracks`, feeds the TrackBoard at line ~1353 and the reorder call at 1248) and :1618/:1632 (`subData.subscription.trackCount`); components/tahi/portal/services/portal-services.tsx:353 (`subscription.trackCount`). Contrast: app/api/admin/clients/[id]/route.ts:212-238 and app/api/portal/capacity/route.ts:120-131 both correctly call `resolveTracksConfig(override, planType, hasPrioritySupport)` then `buildEffectiveTracks(rawRows, config.smallTracks, config.largeTracks)` (lib/plan-utils.ts:90-168) to synthesize a placeholder slot when a custom entitlement outruns real rows. Proof of the actual DB state: MCP get_client(aa80a2d6-0494-424d-8ccc-9af524c31fa7).tracks returns exactly one real row (`112a8f71-...`, type 'small') and one synthesized row (`id: 'synthetic-large-0'`, type 'large') -- the `synthetic-${type}-${i}` id format is buildEffectiveTracks' own placeholder naming (lib/plan-utils.ts:163), which only fires when no real row of that type exists. There is no client nav entry anywhere for /tracks (grep of components/tahi/nav-model.tsx has zero 'tracks' hits), so the home page and Services page are the only two track-count surfaces a Giant Group user will ever reach.",
    "fix": "In app/api/portal/tracks/route.ts, load the org's tracksMode/customSmallTracks/customLargeTracks (same pattern as app/api/portal/capacity/route.ts) and the subscription's planType/hasPrioritySupport, call resolveTracksConfig then buildEffectiveTracks on the raw `tracks` rows before building `items`, so a synthetic large slot appears with its own (empty) queue of large_task requests exactly like the admin client-detail view already does. In app/api/portal/subscription/route.ts, replace `trackRows.length` with `config.smallTracks + config.largeTracks` from the same resolveTracksConfig call added for B2-1's customMrr fix. No schema change; both target files already import from '@/lib/plan-utils' path patterns used two files away.",
    "effortDays": 0.5,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "B2-3",
    "area": "Services catalogue is empty in production; the plan ladder from Batch B2 does not exist in shipped code",
    "severity": "major",
    "claim": "Every one of the 18 services rows imported from ManyRequests has showInCatalog=0 by design, and none is org-scoped to Giant Group, so /api/portal/services returns zero rows for every client today. Giant Group's Services page will render the honest 'Nothing published here yet' empty state under their (buggy, see B2-1/B2-2) Plan panel -- not a lie, but it does not meet Batch B2's own acceptance bar ('the client showcase shows Giant Group's plan and the two neighbours'). The 'plan ladder' (their Scale plan, the Maintain neighbour below, usage-based upsell) that Batch B2 names does not exist anywhere in the actual Next.js component tree; it exists only as a patch against a Claude Design mirror using a completely different render stack.",
    "evidence": "MCP list_services (no orgId, and again with orgId=aa80a2d6-0494-424d-8ccc-9af524c31fa7 -- identical 18 rows both times, all `orgId: null`) shows every row with `showInCatalog: 0`, `manyrequestsId` 2-35, `createdAt: '2026-09-06T20:56:09.161Z'` (the import run). lib/import/manyrequests/plan.ts:916-919: 'Only three of the 18 are is_for_sale upstream, and the client catalogue is a separate decision. Everything lands hidden. showInCatalog: 0'. app/api/portal/services/route.ts:20-30 requires showInCatalog=1 AND visibility='public' AND (orgId IS NULL OR = caller), so all 18 rows are filtered out for every client. components/tahi/portal/services/portal-services.tsx:201-218 renders the honest empty state with an 'Ask us anything' CTA when cards.length===0 -- confirmed not a lie. The staged 'ladder' lives at .claude/qa/svc-ladder-patch.cjs and .claude/qa/svclocal/portal-money-data.jsx / portal-money.jsx / portal-money.css: it is written as `h(...)` React.createElement calls against a Claude Design mirror project (57bf60cf-...) with its own `pm-*` CSS classes, not this repo's TSX components (Card/Badge/TahiButton) or CSS convention -- porting it is a translation exercise, not a copy-paste. The staged data (.claude/qa/svc_data_block.txt) defines LADDER_ORDER=['tune','maintain','scale','launch'] and per-plan RUNGS copy (bestIf/tracks/turnaround/services) authored and reviewed by Liam on 2026-09-06 (quoted inline in the file), plus a `planPressure()` upsell trigger with three signals, one of which ('tracks busy', lanes[].days/studioDays) the same file admits 'has no source yet' -- confirmed true: lib/track-stats.ts only computes `count` and `avgTurnaroundDays`, nothing resembling occupied-vs-available track days exists anywhere in the codebase. Separately, lib/plan-catalog-shared.ts:43-62 already carries a second, independent, already-shipped set of Maintain/Scale copy (tag + feats[]) used today by /api/portal/subscription's `plans` array.",
    "fix": "Two tiers. (1) Minimum honest catalogue, tonight, zero code change: using the existing admin Services page (already built, app/(dashboard)/services/services-content.tsx ServiceDialog) or the `create_service`/`update_service` MCP tools, add two new global (orgId null) rows named 'Scale' and 'Maintain' -- category='service', isRecurring=true/month, showInCatalog=1, visibility='public' -- with the description written in the outcome-then-bullet convention lib/portal-service-view.ts already parses (first paragraph = outcome, `- ` lines = includes, a `Timeline:` line = timeline), reusing the copy that already exists twice over: lib/plan-catalog-shared.ts DEFAULT_PLAN_COPY tag/feats, and .claude/qa/svc_data_block.txt RUNGS.scale/RUNGS.maintain bestIf/tracks/turnaround/services (already Liam-reviewed 2026-09-06). Also flip showInCatalog=1 on 2-3 of the 18 imported add-on rows (e.g. '50 Flexible Webflow Hours (10% Discount)', 'Single Custom Lottie Animation', 'Free Site Audit') so Giant Group has real add-ons to ask about. (2) Full ladder port (optional, matches Liam's own Batch B2 2-day estimate): reimplement LADDER_ORDER/RUNGS/ladder()/rungLive() as real functions in lib/portal-service-view.ts and render a PlanLadder component in portal-services.tsx using this repo's Card/Badge system, sourcing the client's own trackCount (once B2-2 is fixed) and lib/track-stats.ts avgTurnaroundDays as the only two live numbers on the client's own rung, per the design's own rule ('a rung only carries a number if the number is the client's own'). Drop the 'tracks busy' pressure signal (no data source) rather than inventing one; the 'queue longer than tracks' signal is buildable today from the same request/track data B2-2 already touches.",
    "effortDays": 2,
    "needsLiam": false,
    "needsRealSession": false
   }
  ],
  "verified_ok": [
   "Giant Group's D1 org record matches the brief exactly: planType='scale', preferredCurrency='GBP', effectiveInvoiceChannel='stripe' (Xero contact also linked), tracksMode='custom' with customSmallTracks=1/customLargeTracks=1, manyrequestsId='54', contacts Mickey Day (isPrimary, portalRole='admin') and Mark Ramsey (portalRole='member') -- confirmed via MCP get_client.",
   "app/(dashboard)/services/page.tsx correctly branches admin vs client UI on isAdmin, and correctly serves the read-only PortalServices component (not the admin editor) to an admin previewing via the tahi-impersonate-org cookie.",
   "/api/portal/services (app/api/portal/services/route.ts) correctly requires showInCatalog=1 AND visibility='public' AND (orgId IS NULL OR = caller's own orgId) -- a private row for one client can never leak into another client's catalogue.",
   "The client Services page has no create, edit or price control anywhere -- 'Ask about this' / 'Talk about your plan' / 'Ask us anything' are the only affordances, matching the CT.11 default (no order/checkout path shipped).",
   "PortalAskSheet (components/tahi/portal/portal-money-kit.tsx) is honest about consequences: 'Start a request' posts a real row to /api/portal/requests (visible in the client's own Requests thread, matching Liam's rule that the request thread is the client channel, not /messages) and 'Email us' opens a mailto: with copy stating nothing was added to the queue -- no silent action either way.",
   "A member-seat contact (Mark Ramsey) is cleanly denied the money-bearing Plan panel via a 403-by-design in /api/portal/subscription's isOrgAdmin check; the client component treats that 403 as 'no panel', never as an error banner.",
   "The empty-catalogue state on Services (components/tahi/portal/services/portal-services.tsx, cards.length===0 branch) reads as an honest 'still writing this up' message with a working CTA, not a lie or a dead end.",
   "/api/portal/tracks/[trackId]/reorder and /api/portal/capacity/reorder both verify the track's subscription belongs to the caller's own orgId before writing (403 otherwise) -- no cross-org queue tampering path found."
  ],
  "notes": "Scope covered: client-home.tsx tracks/plan cards, /api/portal/tracks + [trackId]/reorder, /api/portal/subscription, /api/portal/capacity(+reorder) for contrast, lib/plan-utils.ts (getTrackEntitlements/resolveTracksConfig/buildEffectiveTracks), lib/billing.ts, lib/plan-catalog(-shared).ts, app/(dashboard)/services/page.tsx + services-content.tsx (admin branch) + components/tahi/portal/services/portal-services.tsx (client branch), lib/portal-service-view.ts, /api/portal/services, /api/admin/services(+coupons), the ManyRequests importer (lib/import/manyrequests/plan.ts), and the staged design at .claude/qa/svc-ladder-patch.cjs + .claude/qa/svclocal + .claude/qa/svc_data_block.txt/svc_jsx_block.txt/svc_css_block.txt. All three findings are derived from reading shipped source plus read-only MCP calls (list_services x2, get_client, get_settings) against the real production D1 -- no write/mutating tool was called and no file was modified, per instructions. The MCP `manyrequests_list_org_services` tool errored ('MANYREQUESTS_API_TOKEN not set on this worker') -- this matches the already-known, already-flagged blocker in project memory (import token pending on Liam) and is unrelated to this audit's two findings; it only would have let me additionally confirm whether Giant Group had its own named ManyRequests plan row (none of the 18 imported rows is Giant-Group-specific, so the recommendation in B2-3 is to add generic Scale/Maintain rows rather than hunt for a missing Giant-specific one). A live browser pass (even just the admin's own impersonation-preview of Giant Group, which needs no real client login) would be a cheap final visual confirmation of all three findings, but was not run here since the code-plus-data evidence already conclusively establishes each one and the task asked me not to modify anything."
 },
 {
  "label": "",
  "findings": [
   {
    "id": "F1",
    "area": "Portal Overview 'Your plan' card + /billing Current Plan (client-facing money)",
    "severity": "blocker",
    "claim": "Giant Group's own portal shows a monthly retainer rate that has nothing to do with what they are actually billed, in the wrong currency label.",
    "evidence": "get_client (MCP) confirms organisations.customMrr=2000 and preferredCurrency='GBP' for Giant Group (internal notes: GBP 2,000/mo from Sept 2026, stepped down from GBP 4,120). But app/api/portal/subscription/route.ts:70 computes `monthlyRate = catalogPlan?.monthlyRate ?? PLAN_MONTHLY_RATES[sub.planType] ?? 0`, never reading organisations.customMrr at all. lib/billing.ts:31-34 hardcodes PLAN_MONTHLY_RATES.scale=4000 (comment: 'Monthly base rates in NZD'), and get_settings confirms no `plan_catalog` override is stored, so the catalog default (NZD 4000) is what ships. This number is (a) rendered on the client Overview home 'Your plan' card via components/tahi/overview/homes/client-home.tsx:1618 `${money(subData.subscription.monthlyRate)}/mo`, where money() (lib: components/tahi/overview/ov-kit.tsx:95,111) treats its input as NZD and FX-converts it to the client's display currency -- so a GBP client sees a converted-from-fiction number; and (b) rendered on app/(dashboard)/billing/billing-content.tsx:213-217 with a literally hardcoded currency label: `${billing.cycleTotal.toLocaleString()} NZD`, i.e. a GBP client would read 'Monthly total: $4,000 NZD' on their own billing page. CLAUDE.md/DECISIONS.md Decision #037 already establishes customMrr as the one source of truth for a client's MRR; this route violates it.",
    "fix": "In app/api/portal/subscription/route.ts, join organisations for customMrr + preferredCurrency alongside the subscription query. When customMrr is truthy, set monthlyRate=customMrr, cycleTotal=customMrr*cycleMonths, and return an explicit `currency` field = organisations.preferredCurrency (falling back to catalog/NZD only when customMrr is unset). Stop piping a customMrr-sourced figure through useOvFormat's money() (which always assumes NZD input) in components/tahi/overview/homes/client-home.tsx -- render the already-native amount + currency symbol directly, the same way components/tahi/invoices/... does via formatCurrency(amount, currency). Replace the hardcoded 'NZD' string in app/(dashboard)/billing/billing-content.tsx:215 with the resolved currency. Add a unit test mirroring lib/__tests__/invoice-how-to-pay.test.ts's 'three callers must agree' pattern.",
    "effortDays": 1,
    "needsLiam": false,
    "needsRealSession": true
   },
   {
    "id": "F2",
    "area": "Portal Overview 'Your plan' card (track count)",
    "severity": "major",
    "claim": "The same card understates Giant Group's tracks: it will say '1 track' when the client's actual, correctly-configured entitlement is 2 (1 small + 1 large), which the Tracks page itself shows correctly -- an internal contradiction a client can notice by comparing two of their own pages.",
    "evidence": "app/api/portal/subscription/route.ts:97-101 computes `trackCount = trackRows.length`, a raw count of physical rows in the `tracks` table for this subscription. get_client shows Giant Group's tracks array has exactly one real DB row (small, id 112a8f71-...) plus one synthetic placeholder (id 'synthetic-large-0', no backing row) -- confirmed by lib/plan-utils.ts buildEffectiveTracks() which only synthesizes a placeholder when a real row is missing. /api/portal/capacity (the Tracks page) correctly resolves both slots via resolveTracksConfig+buildEffectiveTracks (org.tracksMode='custom', customSmallTracks=1, customLargeTracks=1), but /api/portal/subscription never calls that resolver, so its trackCount stays stuck at the physical-row count (1) instead of the configured entitlement (2).",
    "fix": "In the same route, replace the raw `trackRows.length` with resolveTracksConfig(org, sub.planType, sub.hasPrioritySupport) -> smallTracks+largeTracks (the exact call /api/portal/capacity already makes), so both client-facing surfaces report the same number.",
    "effortDays": 0.25,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "F3",
    "area": "Invoice pay path / How-to-pay fallback (lib/invoice-how-to-pay.ts, lib/invoice-channel.ts)",
    "severity": "major",
    "claim": "Giant Group's org-level invoice channel is unset and silently resolves to 'stripe' even though 100% of their real invoices are Xero-rail. Today's live invoice is unaffected only because it happens to already carry a captured Xero pay link; the next invoice that lands in the ordinary pre-approval gap (no Stripe link, no Xero online-invoice URL yet) will show the client a real, due, unpaid bill with absolutely nothing to act on -- no pay button and no bank-transfer details -- because the bank-details fallback block only builds when channel==='xero'.",
    "evidence": "get_client: organisations.invoiceChannel is null for Giant Group. get_settings: invoicing.defaultChannel='stripe' (studio-wide default). lib/invoice-channel.ts resolveInvoiceChannel(orgChannel=null, studioDefault='stripe') returns 'stripe'. lib/invoice-how-to-pay.ts buildHowToPay(): `if (channel !== 'xero') return null` -- the module's own docstring names this exact gap ('a Xero invoice does not [end in a hosted pay page]... between those two moments the client has a real bill... and NOTHING to act on') as the reason it exists, but it only activates for orgs explicitly flagged 'xero'. Giant Group has 4/4 invoices with source='xero' and 0 with source='stripe', and stripeCustomerId is null (list_invoices, get_client).",
    "fix": "Set organisations.invoiceChannel='xero' for Giant Group (one PATCH via the existing admin client route / settings UI -- a factual correction, no new code required for this org). As a durable hardening, consider deriving/warning on channel from actual invoice history (a client with only 'xero'-sourced invoice rows and a null invoiceChannel should not silently inherit a stripe-flavoured studio default) -- audit the same query against every other client before the next invoice cycle for anyone else in the same state.",
    "effortDays": 0.5,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "F4",
    "area": "/billing 'Manage Billing' button (client branch)",
    "severity": "major",
    "claim": "For a Xero-rail client with no Stripe customer (Giant Group), the 'Manage Billing' button on /billing is a silent dead click: it always renders next to the plan card, and on click the API 404s and the failure is swallowed with only a console.error -- the client sees the button spin briefly then nothing happens, no message at all.",
    "evidence": "app/(dashboard)/billing/billing-content.tsx:247-254 renders the TahiButton unconditionally whenever `subscription` exists (no gate on stripeCustomerId/rail). openBillingPortal() (lines 119-137) calls GET /api/portal/billing/session and on !res.ok only does `console.error(...); return` -- no toast, no UI error state. app/api/portal/billing/session/route.ts:52-54 confirms the 404 path: `if (!org.stripeCustomerId) return NextResponse.json({ error: '...' }, { status: 404 })`, and Giant Group's stripeCustomerId is null (get_client). Caveat: STATUS.md/the run plan note /billing currently defaults OUT of the client nav pending Liam's CT.16 call, so this only bites if a client reaches /billing directly or the nav decision flips to 'in'.",
    "fix": "Gate the button on whether the org's resolved invoice channel can actually produce a Stripe portal session (reuse the same channel logic as F3, or simply check for a stripeCustomerId returned by /api/portal/subscription) and hide/replace it with plain text for Xero-rail clients; separately, make openBillingPortal() surface a real error state instead of a silent console.error so this class of failure is never invisible again.",
    "effortDays": 0.5,
    "needsLiam": false,
    "needsRealSession": true
   },
   {
    "id": "F5",
    "area": "Giant Group's only request (#243 'Create tasks for design + dev')",
    "severity": "major",
    "claim": "The single request Giant Group will see on first login is not something they asked for: it has no description, zero comments/messages, zero files, and was created inside ManyRequests by Liam himself (not a client), yet it is not flagged internal, so it will sit in their live request queue and thread list looking like an unexplained, unactioned open item.",
    "evidence": "get_request(2ab13b79-3897-4bef-862c-9fefff7f32c8): description=null, isInternal=false, status='submitted', assigneeId=Liam Miller, subRequestCount=0. get_request_messages returns items:[] and conversationId:null (no thread ever started). Cross-checked against the ManyRequests source of truth: mcp__ManyRequests__get_request(334, include comments/attachments/activity) returns comments_total:0, attachments:[], and the sole activity entry is REQUEST_CREATED by 'Liam Miller' (causer_id 1) on 2026-08-14 -- confirming this is a Tahi-side artifact, not a client submission, and that the D1 import is a faithful 1:1 mirror of ManyRequests (nothing was dropped or half-imported).",
    "fix": "Liam decides before invite: either give the request a real client-facing description/status update, mark it delivered/closed with a note, or flip it internal (isInternal=true) / delete it if it was only ever meant as a Tahi-side reminder. This is real production client data, not a code bug -- a builder should not silently rewrite or delete it.",
    "effortDays": 0.1,
    "needsLiam": true,
    "needsRealSession": false
   },
   {
    "id": "F6",
    "area": "/api/portal/services (client Services catalogue)",
    "severity": "major",
    "claim": "Confirms the known CT.11 gap with exact numbers: Giant Group's Services page will render its empty state on day one -- 0 of 18 catalogue rows are publishable to any client.",
    "evidence": "list_services(orgId=aa80a2d6-...) returns 18 rows (global services + org-scoped), and every single one carries `showInCatalog: 0`. app/api/portal/services/route.ts requires `showInCatalog=1 AND visibility='public' AND (orgId IS NULL OR orgId=caller)` -- with showInCatalog false on all 18 rows, the query returns zero items regardless of the other two conditions. This is not a bug in the route (the gating logic is correct and already tenant-safe per migration 0097); it is that no row has ever been published.",
    "fix": "Already tracked as Batch B2 in docs/superpowers/plans/2026-09-13-mvp-100-run.md: port the plan-ladder design (.claude/qa/svclocal + svc-ladder-patch.cjs), publish the relevant rows with showInCatalog=1, and wire the order path per Liam's pending decision (mint a request vs Stripe checkout, or ship 'Ask about this' only if he stays silent). No new scope beyond what is already planned; listed here only to confirm the count Liam asked for.",
    "effortDays": 2,
    "needsLiam": true,
    "needsRealSession": false
   }
  ],
  "verified_ok": [
   "ManyRequests import for Giant Group is complete and faithful: exactly 1 request exists in ManyRequests (id 334 / #243) and exactly 1 in D1, same title/status ('Submitted'/'submitted'), same 0 comments and 0 attachments in both systems -- nothing was lost or partially imported.",
   "The live sent invoice (GBP 4,225, due 2026-09-15, xeroInvoiceId 940d32dc..., 'Imported from Xero: INV-0065') already carries a captured xeroOnlineInvoiceUrl, so resolveInvoicePayUrl() returns a working Pay Now link on both /api/portal/invoices and /api/portal/invoices/[id] right now, independent of the channel misconfiguration in F3.",
   "Invoice currency rendering on the shared invoice-detail component is correct: it reads invoice.currency (GBP for Giant Group) via formatCurrency/Money, not the misleadingly-named totalUsd column literally as USD.",
   "organisations.customMrr is correctly set to 2000 (GBP) for Giant Group, matching the internal note's confirmed September step-down from GBP 4,120, and get_retainer_health's mrrNzd (4585.47) is consistent with toNzd(2000, 'GBP', rateMap) -- the raw data is right, only the portal projection in F1 fails to use it.",
   "Contacts Mickey Day (portalRole=admin, isPrimary=true) and Mark Ramsey (portalRole=member) both exist with correct roles and clerkUserId=null on both -- consistent with 'prepare tonight, do not invite yet.'",
   "Custom tracks (1 small + 1 large) resolve correctly on /api/portal/capacity via resolveTracksConfig + buildEffectiveTracks (mode='custom', no ghost upsell cards shown), and the synthetic large-track placeholder is handled safely end-to-end, including in the drag-reorder/move write path (app/api/portal/capacity/reorder/route.ts correctly type-derives from a 'synthetic-large-0' id without touching requests.trackId).",
   "/api/portal/team correctly surfaces Liam Miller as 'on your work' from Giant Group's one external, non-internal request -- an honest non-empty state, not a lying empty one.",
   "Both tracks correctly show nothing 'in flight' (currentRequestId=null on both) -- accurate, since the sole request is still status=submitted and has never been picked up onto a track.",
   "The client /messages page (portal_list_conversations) returns the expected shape for Giant Group: the Tahi Studio standing channel plus one thread for request #243, matching the 'request thread is the client channel' model -- no separate free-form DM surface is exposed."
  ],
  "notes": "Scope covered: get_client, list_requests, get_request, get_request_messages, list_request_files, list_invoices, get_invoice (x2), list_client_contacts, get_retainer_health, get_subscription, list_services, portal_list_conversations, get_settings -- all via the Tahi_Dashboard MCP (read-only, no write tool called). Cross-checked the ManyRequests source portal directly (get_organization/find_requests/get_request for org 54 / request 334) to prove the D1 import is a faithful 1:1 mirror rather than assuming it from D1 alone. No file was modified; all fixes are outlined for a builder, not applied. F1/F2/F3/F4 are proven purely from code + production D1 data (no live session needed to establish the defect), but F1 and F4 are flagged needsRealSession because the exact rendered string/number and the literal silent no-op are worth a one-shot visual confirmation in Client-view before calling the fix done. F5 and F6 are operator/product decisions, not code bugs -- flagged needsLiam accordingly. Did not deep-audit the request-thread reply-posting mechanics (POST message on a null-conversationId request) or the invite/accept-invite flow itself: both are called 'live-verified'/'shipped' in STATUS.md as of 2026-09-05/09-10 and Giant Group is deliberately not being invited tonight, so exercising them was out of scope for a read-only D1 audit and would need a real session in any case."
 },
 {
  "label": "",
  "findings": [
   {
    "id": "F1",
    "area": "Invoices (client list + detail), Xero-rail pay path",
    "severity": "blocker",
    "claim": "For a Xero-rail client (Giant Group), an unpaid invoice with no Stripe/Xero pay link yet, which is the ORDINARY starting state of every dashboard-raised Xero invoice, since the push route holds it at DRAFT until Liam approves it by hand in Xero, renders with an amount, a due date, and literally nothing telling the client how to pay it. No Pay button, no bank details, no message. The backend already computes a full 'How to pay' block (bank name/account/reference/amount/hint) for exactly this case, but neither client-facing page reads or renders it.",
    "evidence": "Backend builds and returns `howToPay` in both portal invoice endpoints: app/api/portal/invoices/route.ts:171-190 (`buildHowToPay` call inside the `projected` map) and app/api/portal/invoices/[id]/route.ts:127-152, both via lib/invoice-how-to-pay.ts:201-224 (`buildHowToPay`), whose own doc comment says this is 'Founder decision (2026-09-06)' precisely for a Xero invoice with no link. Frontend never consumes it: app/(dashboard)/invoices/invoice-list.tsx `Invoice` interface (lines 62-84) has no `howToPay` field, `isPayable()` (104-113) and the client Pay column (1179-1198) render `null` when there is no `payUrl`, and `InvoiceMobileCard` (138-206) has no fallback branch either, confirmed via `grep -rn howToPay app/(dashboard)/invoices/` returning zero matches. Same gap in app/(dashboard)/invoices/[id]/invoice-detail.tsx: `InvoiceRow` interface (73-112) omits `howToPay`, and the client Pay CTA block (721-754) is wrapped in `{!isAdmin && payUrl && !settled && (...)}` with nothing rendered in the else case.",
    "fix": "Add `howToPay?: { bankName?: string; accountName?: string; accountNumber?: string; reference: string; amount: number; currency: string; dueDate: string | null; hint: string } | null` (matching `InvoiceHowToPay` in lib/invoice-how-to-pay.ts) to both the `Invoice` interface in invoice-list.tsx and the `InvoiceRow` interface in invoice-detail.tsx. In invoice-detail.tsx, add a 'How to pay' card/rail block rendered when `!isAdmin && !payUrl && !settled && invoice.howToPay` is present (bank name, account name, account number, the reference to quote, the hint sentence) next to/in place of the existing Pay CTA slot around line 721. In invoice-list.tsx, give the Pay column (row 1179) and InvoiceMobileCard (row 192) a non-null fallback when `!isPayable(r) && r.howToPay`, at minimum a 'How to pay' link/badge that opens the invoice detail page, since a table cell has no room for the full block. Also flag to Liam separately that `invoicing.bankDetails` (lib/invoice-pay-settings.ts) is a single global bank account with no per-currency variant, so a GBP invoice's How to pay block will quote whatever single account is configured (verify it can actually receive a GBP wire before Giant Group's first Xero invoice goes out).",
    "effortDays": 1,
    "needsLiam": false,
    "needsRealSession": true
   },
   {
    "id": "F2",
    "area": "Client nav / Messages, policy vs. shipped code",
    "severity": "major",
    "claim": "The brief for this audit states 'the standalone /messages page is hidden for clients, the request thread is the client channel.' That is not what the code does today: /messages is a fully wired, working standalone inbox for the client audience (org-wide standing channel + a thread per request), live in the client sidebar, live as one of only 4 mobile bottom tabs, and on by default for every client org (including a brand-new one like Giant Group) unless someone explicitly flips a feature-visibility override. This is a deliberate, documented reversal of an earlier 'hide Messages for V1' decision, not a bug, but it means the audit's stated premise doesn't match production, and Giant Group would see a second, separate 'Messages' surface alongside every request's own thread unless that's intended.",
    "evidence": "components/tahi/nav-model.tsx:127-156, CLIENT_NAV includes `{ label: 'Messages', href: '/messages', ..., clientVisible: true }` (line ~138). components/tahi/mobile-bottom-nav.tsx:39, `CLIENT_PRIMARY_HREFS = ['/overview', '/requests', '/messages', '/files']`. app/(dashboard)/messages/page.tsx:29-39, renders a real client branch (`MessagesContent audience='client'`), no redirect. lib/feature-tree.ts:53-60, 'messages' key applies to both team and client. lib/permissions.ts:188-189, `access.level === 'client'` defaults to `true` (allow) unless an explicit `feature_visibility` override exists, so a freshly onboarded org is not hidden by default. Git history: commit cc3c41f1 originally hid Messages for V1; commit f29425a9 (2026-09-06 22:42, 'messages: the inbox is a real page again, for both audiences') deliberately reversed that. A now-stale code comment in components/tahi/settings/sections/plan.tsx:395-398 still asserts '/messages redirects a client straight back to /requests', which was true before f29425a9 and is false today.",
    "fix": "This is a decision, not a code defect: confirm with Liam whether (a) the Sep 6 reversal is the intended state going forward (in which case update the standing-rule memory/docs, and fix the stale comment in plan.tsx:395-398), or (b) Messages should in fact stay hidden for Giant Group specifically, in which case the existing per-org feature_visibility override (the same mechanism the Permissions builder uses) should deny the 'messages' key for org aa80a2d6-0494-424d-8ccc-9af524c31fa7 before the invite goes out, with no code change required. If the intent is 'hidden for every client, not just Giant Group', that requires reverting the CLIENT_NAV entry, the mobile primary tab, and the feature default together (they are one invariant, per the code's own comments), roughly half a day plus a re-check of the daily brief's 'client replies overnight' row, which also reads this key.",
    "effortDays": 0.5,
    "needsLiam": true,
    "needsRealSession": false
   },
   {
    "id": "F3",
    "area": "Notifications, invoice deep link",
    "severity": "minor",
    "claim": "A client's invoice notification always lands them on the general /invoices list, never on the specific invoice it was about, even though the invoice detail page has fully supported the client audience since 2026-09-05.",
    "evidence": "lib/notification-links.ts:139, `clientHref()`'s `case 'invoice': return '/invoices'` ignores `entityId` entirely, unlike the team map's own `case 'invoice': return entityId ? '/invoices/${entityId}' : '/invoices'` at line 90. The comment above it (lines 128-131) explicitly says this was deliberate only because 'the invoice detail page still fetches /api/admin/invoices, which 403s a client... make it a deep link in the same change that gives the detail page a portal branch.' That branch was added in commit 41461250 ('feat(billing): portal invoice detail and pay link...', 2026-09-05), five weeks after notification-links.ts was created (2026-07-07), and the deep-link follow-up was never made.",
    "fix": "In lib/notification-links.ts, change line 139 to `case 'invoice': return entityId ? '/invoices/' + entityId : '/invoices'` (mirroring line 90). No other change needed: app/(dashboard)/invoices/[id]/page.tsx and invoice-detail.tsx already resolve for a client session via `/api/portal/invoices/[id]`.",
    "effortDays": 0.25,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "F4",
    "area": "/tracks route, decision not yet executed",
    "severity": "minor",
    "claim": "docs/superpowers/plans/2026-09-13-mvp-100-run.md states the default decision (CT.18) is that /tracks is DELETED for clients if Liam is silent. The nav entry is already gone (correct), but the route, page and FEATURE_TREE key are still fully live and would render a real, working track-queue view for a client who reaches /tracks directly (bookmark, an old link, or typing the URL), it is not deleted, only unlinked.",
    "evidence": "app/(dashboard)/tracks/page.tsx:1-21, client branch calls `requirePageFeature('tracks')` and renders `<TracksContent isAdmin={false} />` (no redirect). lib/feature-tree.ts:64, `{ key: 'tracks', ..., appliesTo: ['client'], route: '/tracks' }` still registered and, per lib/permissions.ts:188-189, defaults to visible for a client level unless explicitly denied.",
    "fix": "Confirm with Liam whether 'deleted' meant 'remove the nav entry' (already done, no further action) or 'remove the surface entirely.' If the latter: delete app/(dashboard)/tracks/ (page.tsx + tracks-content.tsx), drop the 'tracks' FEATURE_TREE node from lib/feature-tree.ts:64, and remove any lingering references (e.g. any admin-side link to a client's /tracks). Low effort, purely a confirm-then-delete.",
    "effortDays": 0.25,
    "needsLiam": true,
    "needsRealSession": false
   }
  ],
  "verified_ok": [
   "Client nav model (components/tahi/nav-model.tsx CLIENT_NAV) correctly omits Schedules/Contracts/Proposals (removed 2026-09-05, all three redirect a client to /requests if the page were reachable) and omits /billing entirely (CT.16 default 'out' already satisfied).",
   "Mobile bottom tab bar for clients (components/tahi/mobile-bottom-nav.tsx) shows exactly Overview/Requests/Messages/Files + More, gated by the same server-resolved feature map as the rail (gateTabs logic drops a tab rather than showing one that would bounce).",
   "Portal home (client-home.tsx + lib/client-home-signals.ts + lib/portal-status.ts) uses one shared status vocabulary with the requests list/detail, holds loading shape instead of flashing empty-state copy, and every 'new request' CTA opens the New Request dialog rather than dropping the client on a list. Destination resolvers (invoicePayDestination, fileOpenDestination, externalLinkDestination) validate https/absolute-path before opening a URL.",
   "New request dialog: intake-form resolution (app/api/portal/request-forms/route.ts) implements the documented org+category > org-global > category-global > default priority exactly; planning fields (start date, estimated hours) and the size-prediction badge are correctly isAdmin-gated away from the client view.",
   "Client Files page (files-content.tsx) has real loading/empty/error/populated states, a working three-step R2 upload with a client-scoped org resolver, and 2.75rem touch targets throughout including the mobile card variant.",
   "Client Services page (portal-services.tsx) is an honest 'showcase, not a shop': no create/edit/price controls, every 'Ask about this' / 'Ask us anything' button opens PortalAskSheet, which either posts a REAL request to /api/portal/requests or opens a mailto:, never a no-op.",
   "Invoice detail and list pages correctly branch client vs admin data source (/api/portal/invoices vs /api/admin/invoices), classify a 403 into a named reason (member-seat denial) rather than a generic retry-forever error, and the Stripe Pay CTA/hosted link path itself (when payUrl exists) is solid.",
   "Offline fallback (app/offline + public/sw.js) is genuinely wired: service worker precaches the route, the page does a real fetch probe rather than trusting navigator.onLine, and its inlined CSS uses var(--token, #hexFallback) pairs rather than bare hardcoded colours.",
   "Settings shell audience/section gating (components/tahi/settings/settings-shell.tsx) correctly restricts People to workspace admins and shows Organization/People/Brand/Plan & billing only to the client audience; Brands section is an honest link-list (name/format/URL), not a broken upload promise.",
   "Client request-detail composer correctly disables writes and explains why during an admin preview-as-client session, and never lets a read-only viewer type into a composer the server would 403."
  ],
  "notes": "Scope covered: CLIENT_NAV/mobile-bottom-nav definitions, client-home.tsx + portal-status.ts + client-home-signals.ts (full read), request detail (targeted grep across all 4907 lines for client-branch logic, approve/request-change, composer, not a full line-by-line read of attachments/mentions internals), new-request-dialog.tsx (targeted read of intake-question and prediction sections, not the full 2114 lines), files-content.tsx (full read), services (page.tsx + services-content.tsx admin header + portal-services.tsx full read), invoices list + detail (full read of both) and the two portal invoice API routes (full read) plus lib/invoice-how-to-pay.ts and lib/invoice-pay-settings.ts, notifications (targeted: destination resolver, full read of lib/notification-links.ts), settings-shell.tsx (full read) plus plan.tsx/brands.tsx (full read), offline page + sw.js wiring, /tracks and /billing page.tsx gate logic, and a targeted hex-color sweep across client-facing directories (all hits were either token-with-fallback patterns or scoped/dark-mode-overridden local tone tokens, not raw violations). Not independently verified with a live browser or real client session: nothing in this list should be treated as proven-in-production without the A5 live lap the run plan already calls for. F1 is the one finding I'd treat as launch-blocking on its own terms, it is provable purely from the code (grep confirms zero references to `howToPay` anywhere under app/(dashboard)/invoices/), and it lands exactly on Giant Group's own rail choice (Xero, GBP), not a hypothetical client. F2 is less a bug than a fact-check on the audit's own premise that the requesting agent should reconcile with Liam before deciding whether tonight's run needs to do anything about it. Did not run wrangler/D1 queries to check whether Giant Group's org already has any feature_visibility overrides set (would need production DB access); if such an override already denies 'messages' for this org, F2 is moot for Giant Group specifically even though the code-level default still stands for every other client."
 },
 {
  "label": "",
  "findings": [
   {
    "id": "EMAIL-1",
    "area": "Notification emails (request thread reply, client_review/delivered, org standing-line message)",
    "severity": "blocker",
    "claim": "Even after Liam does exactly what tonight's plan describes, adds Giant Group's org id to email.allowedOrgIds, none of the client-facing notification emails (studio's reply on a request thread, \"ready for your review\", \"delivered\", or a studio message on the org's standing line) will ever reach Mickey Day or Mark Ramsey. That whole send path never threads an orgId down to the delivery gate, so the per-org allowlist exemption can never match, and the send falls through to the tahi.studio-only address/domain check and is withheld forever regardless of the allowedOrgIds setting.",
    "evidence": "lib/notification-email.ts:442 `sendWithBackoff` calls `sendEmail(target.email, plan.subject, el, text)` with no 5th `context` argument. lib/email.ts:56-64 then passes `orgId: context?.orgId ?? null` into `deliverEmail`, i.e. always null for this path. lib/email-delivery.ts:182 `resolveOrgRecipientScope(req.orgId, policy)` with orgId=null returns `{orgId: null}` (lib/email-gate.ts:110-111). lib/email-allowlist.ts:493-497's org-exemption branch (`if (org && policy.allowedOrgIds.includes(org))`) is gated on `scope?.orgId`, which is always null here, so it never runs. Live evidence: list_email_suppressions already shows two rows from this exact path logged as `template: \"unspecified\", orgId: null` (the [REQ-3] test-client sends to sarah@/james@/staci@tahi.studio), proving orgId genuinely never reaches the gate or the log for this send path today.",
    "fix": "Add an optional `orgId` (and a kebab-case `template` name) to `NotificationEmailPlan` (lib/notification-email.ts) and thread it through `dispatchNotificationEmails` -> `sendWithBackoff` -> the `sendEmail(...)` call's 5th argument `{ template, orgId }`. Populate it at the four call sites that already hold the org id in scope: lib/request-status-effects.ts (`request.orgId`, feeds `clientStatusEmailPlan`), app/api/admin/messages/[source]/[id]/route.ts (`t.orgId`, feeds `threadReplyEmailPlan` + `channelMessageEmailPlan`), app/api/portal/messages/[source]/[id]/route.ts (`orgId` in scope, same two plans), and app/api/admin/requests/[id]/messages/route.ts + app/api/portal/requests/[id]/messages/route.ts (`threadReplyEmailPlan`). Add a regression test asserting `deliverEmail` receives a non-null orgId for a client-audience notification plan, and re-check `email_suppressions` afterwards to confirm template/orgId are no longer 'unspecified'/null for these events.",
    "effortDays": 1,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "BILLING-1",
    "area": "Invoice email (invoice sent / how-to-pay fallback)",
    "severity": "blocker",
    "claim": "Giant Group is meant to be on the Xero rail (per tonight's brief and their xeroContactId / Xero-sourced billing history), but `organisations.invoiceChannel` is NULL and the studio default (`invoicing.defaultChannel`) is 'stripe', so `effectiveInvoiceChannel` resolves to 'stripe' for this client today. The invoice-sent email's bank-transfer fallback only renders when the resolved channel is 'xero', and this client has no Stripe customer at all, so the first invoice raised for them before Liam approves it inside Xero (the normal starting state for a fresh Xero-rail bill) will show the client neither a working pay button nor bank details: a real bill with nothing to click and nothing to reference.",
    "evidence": "get_client(aa80a2d6-0494-424d-8ccc-9af524c31fa7) returns `invoiceChannel: null`, `effectiveInvoiceChannel: \"stripe\"`, `stripeCustomerId: null`, `xeroContactId: \"3666b925-...\"`. get_settings returns `invoicing.defaultChannel: \"stripe\"`. lib/invoice-channel.ts:52-59 `resolveInvoiceChannel` falls back to the studio default whenever the org's own channel is null. lib/invoice-how-to-pay.ts:201-210 `buildHowToPay` returns null unless `channel === 'xero'`. app/api/admin/invoices/[id]/send-email/route.ts:390-397 only renders the how-to-pay block when `hasBankDestination(howToPay)` is true, and `resolvePayUrl` (lines 38-66) has nothing to resolve without a `stripeInvoiceId`, which this client will never have.",
    "fix": "Set `organisations.invoiceChannel = 'xero'` for aa80a2d6-0494-424d-8ccc-9af524c31fa7 before any invoice is raised or sent (via the client detail billing card, or the `update_client` MCP tool). Also confirm `auto_generate_invoices` is not scheduled to run against this client while the field is unset, since it reads the same `effectiveInvoiceChannel`.",
    "effortDays": 0.25,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "MSG-1",
    "area": "Messages page visibility for clients",
    "severity": "major",
    "claim": "Liam's standing rule for tonight is that the standalone /messages page is hidden for clients and the request thread is the only client channel. The code's actual default is the opposite: any client contact with no feature_visibility override, which is Giant Group's exact state today, can open /messages, see and use a fully working \"one line to the studio\" org-wide channel (separate from any request), and post to it, with real bell rows and (once EMAIL-1 above is fixed) real emails on both sides.",
    "evidence": "get_feature_visibility(organisation, aa80a2d6-0494-424d-8ccc-9af524c31fa7) returns `overrides: []`. lib/permissions.ts:168-191 `decideFeature`: with no override and `access.level === 'client'`, it falls through to `return true` (line 189). lib/feature-tree.ts:53-60 and lib/notification-links.ts:97-98 both carry comments stating \"/messages is a real page again for both audiences\" (i.e. it is no longer hidden by default). components/tahi/messages/messages-content.tsx:446-447 literally renders the client-facing copy \"One line to the studio, plus a thread for every request.\"",
    "fix": "Before invite, call `set_feature_visibility({subjectType: 'organisation', subjectId: 'aa80a2d6-0494-424d-8ccc-9af524c31fa7', featureKey: 'messages', effect: 'deny'})` to match the stated policy for Giant Group specifically. Separately decide with Liam whether the studio-wide client default should flip (a one-line change in `lib/permissions.ts` `decideFeature`'s client branch, `access.level === 'client'`), so every future client starts hidden instead of needing a per-org override, this is the part only he can call, since it changes behaviour for every client, not just this one.",
    "effortDays": 0.5,
    "needsLiam": true,
    "needsRealSession": false
   },
   {
    "id": "INTAKE-1",
    "area": "Email-to-request intake (client replies by email instead of the portal)",
    "severity": "major",
    "claim": "Every client-facing thread/status email tells the client to \"reply on the thread rather than by email if you can\", implying a reply-by-email is a working, if secondary, fallback. But the email-to-request intake webhook, if it is wired to a live inbound address, treats ANY email from a known contact as a brand-new, unrelated 'submitted' request titled off the raw subject line, there is no In-Reply-To or [REQ-n] matching back to the original request, and the route explicitly never auto-replies, so the client's actual reply is silently orphaned rather than continuing their thread.",
    "evidence": "emails/new-message.tsx:79-81 \"Reply on the thread rather than by email if you can. Files, feedback and approvals all stay attached to the request.\" app/api/webhooks/email-intake/route.ts:240-270 always INSERTs a fresh `requests` row for a matched contact, keyed only on the raw subject line, with no reference to any existing request id. STATUS.md line 113 already flags this pipeline's live-wiring state as \"unknown since 2026-07-07; verify\".",
    "fix": "Liam confirms whether Resend inbound routing or a Cloudflare Email Worker is actually pointed at this endpoint for tahi.studio's inbound mail today (a DNS/dashboard fact nobody else can check). If it is live, add subject ([REQ-n]) or In-Reply-To/References matching so a reply from a known contact appends to the existing request instead of always creating a new one. If it is not wired, soften the email copy so it stops implying a working fallback that does not exist, and keep STATUS.md's stub note.",
    "effortDays": 1.5,
    "needsLiam": true,
    "needsRealSession": true
   },
   {
    "id": "EMAIL-3",
    "area": "From-address / reply-to configuration",
    "severity": "minor",
    "claim": "The literal From address a real Giant Group email will show cannot be confirmed from the repository, and the two local env files disagree with each other, so nobody can currently state with confidence whether replies to a Giant Group email land in a mailbox anyone reads.",
    "evidence": ".env.local:28 `RESEND_FROM_EMAIL=dashboard@tahi.studio`; .env.migration:24 `RESEND_FROM_EMAIL=business@tahi.studio`; lib/email-from.ts:28-48 falls back to `Tahi Studio <business@tahi.studio>` only when the Worker's own secret is unset; no RESEND_* var appears in wrangler.json, so the live production value is a Cloudflare Worker secret invisible to this audit.",
    "fix": "Liam checks (via `wrangler secret list` / the Cloudflare dashboard, or a real sent test email's From header) which mailbox the production `RESEND_FROM_EMAIL` secret actually points at, and confirms someone reads that inbox, relevant given INTAKE-1 above.",
    "effortDays": 0.1,
    "needsLiam": true,
    "needsRealSession": false
   }
  ],
  "verified_ok": [
   "The allowlist gate itself is closed and working as designed: get_settings shows email.deliveryMode='allowlist', email.allowedAddresses=[\"business@tahi.studio\",\"staci@tahi.studio\"], email.blockedAddresses=[\"nathan@tahi.studio\"], email.allowedOrgIds=[]. Giant Group's contacts (mark.ramsey@giantgroup.com, michael.day@giantgroup.com) pass none of these today, so every Resend-based send that DOES thread orgId correctly (invoice-sent, client-invite onboarding link, Clerk org-invite, announcement fan-out) is cleanly withheld and logged rather than silently claiming success.",
   "Xero's own invoice-email transport (lib/xero-invoice-email.ts via app/api/admin/invoices/[id]/send-email) is separately gated on `orgAuthorisedForBlindTransports` (mode 'all' or the org id on allowedOrgIds) before it is allowed to fire, so Xero cannot leak an address our own gate withheld once BILLING-1 is fixed and Giant Group starts getting real invoices.",
   "Stripe's finalised send_invoice email is refused before creation in app/api/admin/invoices/stripe-create/route.ts via resolveDeliveryPolicy/guardOutboundAddress, same pattern.",
   "The three Clerk-invitation-minting routes (admin team invite, portal invites, portal people) and the onboarding-invites route (client-invite.tsx via sendEmail) all call guardOutboundAddress/deliverEmail before minting or sending, so attempting to invite Mickey or Mark tonight returns a clean 409/`emailed:false` and a matching email_suppressions row rather than a silent or partial send, confirmed live: two real 'clerk-org-invite' suppression rows already exist in the log for an unrelated blocked address.",
   "request_assigned is bell-only by design, never emailed, for both the single-participant route and the bulk-assign route, consistent with tasks/requests staffing being studio-internal.",
   "Bulk request status moves correctly suppress the per-client email when more than one row for the same org is in the batch (bell row only), and still email normally when exactly one row for that org is touched, confirmed in app/api/admin/requests/bulk/route.ts:344 against lib/request-status-effects.ts's documented rule.",
   "Every client-facing notification email's footer \"Notification settings\" link resolves to a real, working per-event email-preference page for a client contact (components/tahi/settings/sections/notifications.tsx, NTF_CLIENT list, reading/writing /api/portal/notifications), not a dead link.",
   "Every request-thread / client_review / delivered email CTA resolves through lib/notification-links.ts's client route map to a real client-reachable page (/requests/[id]), never to the hidden-by-policy /messages path.",
   "Only one Resend client exists in the whole tree (lib/email-delivery.ts), enforced by a repo-walking test (lib/__tests__/no-resend-bypass.test.ts) plus a second test guarding the ManyRequests importer, so there is no other back door capable of mailing Giant Group."
  ],
  "notes": "SCOPE: lib/email-delivery.ts, lib/notification-email.ts, lib/notifications.ts, lib/email-allowlist.ts, lib/email-gate.ts, emails/*, plus every caller (invoice send-email, announcement fan-out, request-status-effects, portal/admin messages routes, onboarding-invites, portal invites, ai-reply-drafts send, email-intake webhook). Live settings and suppression log pulled via MCP get_settings / list_email_suppressions / get_feature_visibility / get_client / list_client_contacts.\n\n## Event table (today, Giant Group not yet on email.allowedOrgIds)\n\n| Event | From (today) | Subject | Withheld for a giantgroup.com contact today? | email_suppressions row logged? | Bell row raised regardless? | Plain-text part |\n|---|---|---|---|---|---|---|\n| Invite (Clerk org invite, 2nd+ seat) | Clerk's own configured sender (not this codebase / not RESEND_FROM_EMAIL) | Clerk's own invitation template | Yes, guardOutboundAddress runs before Clerk is called; 409 returned, nothing minted-and-silent | Yes, template='clerk-org-invite', reason='not_in_allowlist' | N/A (no contact row / clerk user yet) | N/A (Clerk's own email) |\n| Invite (onboarding link, emails/client-invite.tsx, 1st contact) | emailFromAddress() default 'Tahi Studio <business@tahi.studio>' unless RESEND_FROM_EMAIL overrides (see EMAIL-3) | \"Your Giant Group portal is ready\" | Yes, deliverEmail withholds; link is still minted (usable to copy by hand), response reports emailed:false + emailError | Yes, template='client-invite', orgId=aa80a2d6..., reason='not_in_allowlist' | N/A | Rendered from the React element (best-effort, see plainTextAlternative) |\n| Request submitted (studio gets notified) | 'Tahi Studio <business@tahi.studio>' (default) | \"[REQ-n] New request from Giant Group: <title>\" | N/A for THIS event, recipients are Tahi team members (business@/staci@ pass the address allowlist, nathan@ is on blockedAddresses and is withheld) | Only for nathan@ (address_blocked) | Yes, notifyAllAdmins bell rows to every team member regardless of email outcome | Yes |\n| Studio replies on request thread (client gets notified) | default | \"[REQ-n] <Studio person> replied on \\\"<title>\\\"\" | Yes today (domain/address fail) AND STILL yes after Liam flips allowedOrgIds, see EMAIL-1, orgId never reaches the gate for this send | Yes, but logged as template='unspecified', orgId=null (see EMAIL-1) | Yes, notifyOrgContacts bell row regardless | Yes (plainTextAlternative) |\n| Client replies on request thread (studio gets notified) | default | \"[REQ-n] New client message on \\\"<title>\\\"\" | N/A, recipients are the assigned studio team (business@/staci@ pass, nathan@ blocked) | Only for nathan@ | Yes | Yes |\n| Org standing-line message, studio→client | default | \"<name> sent you a message\" | Yes today AND after the flag flips (EMAIL-1, same root cause) | Yes, template='unspecified', orgId=null | Yes | Yes |\n| Org standing-line message, client→studio | default | \"<orgName>: new message from <name>\" | N/A (studio recipients) | Only nathan@ | Yes | Yes |\n| client_review (\"ready for your review\") | default | \"[REQ-n] Ready for your review: \\\"<title>\\\"\" | Yes today AND after the flag flips (EMAIL-1) | Yes, template='unspecified', orgId=null | Yes | Yes |\n| delivered | default | \"[REQ-n] Delivered: \\\"<title>\\\"\" | Yes today AND after the flag flips (EMAIL-1) | Yes, template='unspecified', orgId=null | Yes | Yes |\n| Invoice sent | default (or emailFromAddress('Liam Miller') for the manual chase) | \"Invoice <ref> from Tahi Studio\" | Yes today; correctly reachable once org id is added (this path DOES thread orgId), but see BILLING-1: content will be degraded (no pay link, no bank block) until invoiceChannel is fixed | Yes, template='invoice-sent', orgId=aa80a2d6... | Yes, on first send only | Not explicitly passed as `text`; relies on Resend/React default (no `text` arg in the send-email route) |\n| Invoice overdue / chase | emailFromAddress('Liam Miller') | draft subject, e.g. \"Invoice <ref>, now overdue\" | Yes today; reachable once org id added (orgId threaded correctly, chaseOrgId) | Yes, correctly scoped | No bell row (this is a human-authored one-off, not a notification event) | Human-authored plain text body converted to minimal HTML |\n| Bulk status moves | default | Same as single client_review/delivered subjects | Governed by the same EMAIL-1 defect when it does fire (single-row-per-org batches only) | Same gap | Yes, always (bell never suppressed) | Yes |\n| Announcement fan-out | default | Announcement title | Yes today; reachable once org id added (fanOutAnnouncementEmails passes r.orgId correctly) | Yes, template='announcement', orgId correct | Depends on caller; announcement bell is separate | No `text` passed (HTML only) |\n| request_assigned |, |, | N/A, never emailed by design (bell-only, team members only) | N/A | Yes, team member only | N/A |\n\n## What changes when Liam adds aa80a2d6-0494-424d-8ccc-9af524c31fa7 to email.allowedOrgIds\nOnly the addresses stored on `contacts.email` for that org id become reachable (today: mark.ramsey@giantgroup.com, michael.day@giantgroup.com, confirmed via list_client_contacts), and only for sends whose caller actually passes that orgId into the delivery gate. Confirmed-correct today: invoice-sent, the manual invoice chase, client-invite (onboarding link), Clerk org invites, and the announcement fan-out. Confirmed-broken (EMAIL-1): thread replies, client_review/delivered, and the org standing-line message never carry orgId at all, so flipping the setting changes nothing for them, they remain silently withheld until the plumbing fix lands. No other address on a mixed send (e.g. a cc'd third party) is ever widened by this exemption; RecipientScope only ever covers that one org's own contacts (lib/email-allowlist.ts's RecipientScope doc comment).\n\n## Path that could email a Giant Group person BEFORE the flag flips\nNone found. Every Resend send funnels through the single door (lib/email-delivery.ts, enforced by a repo-walking test), and the two blind transports (Xero's own invoice email, Clerk's own invitation email) both ask the same gate first via guardOutboundAddress / the orgAuthorisedForBlindTransports check. Stripe's finalised-invoice email is refused before the invoice is even created in that mode. This part of the design is sound and evidenced.\n\n## Unsubscribe / preferences\nNo functioning \"Unsubscribe\" link exists anywhere: emails/_components.tsx's EmailFooter accepts an optional `unsubscribeUrl` prop, but grep across every caller in emails/* shows none ever pass it, so the link simply never renders (not a broken link, an unused one). What DOES exist and work: every client-facing template's footer carries a \"Notification settings\" link to /settings, which for a client contact renders a real per-event email-preference toggle list (new request, new message, invoice paid, invoice overdue) reading/writing /api/portal/notifications, verified functional, not a dead end.\n\n## Hidden-route check\nEvery button/link in every client-facing email template resolves to a real, reachable client route: request emails go through lib/notification-links.ts's client map to /requests/[id]; the org standing-line email goes to /messages, which is a real, working page for a client, the point being it should NOT be reachable per Liam's stated policy and currently is (see MSG-1). No template links to any genuinely 404/dead surface.\n\n## Live settings snapshot (get_settings, 2026-09-12)\nemail.deliveryMode=allowlist; email.allowedDomains=[\"tahi.studio\"]; email.allowedAddresses=[\"business@tahi.studio\",\"staci@tahi.studio\"]; email.blockedAddresses=[\"nathan@tahi.studio\"]; email.allowedOrgIds=[]; invoicing.defaultChannel=stripe; invoicing.xeroEmailMode=dashboard. list_email_suppressions currently holds 5 rows, all pre-dating tonight and all unrelated to Giant Group (2 Clerk invites to a blocked staci@ address, 3 rows from a \"[REQ-3] Act-as-client smoke: please ignore\" test send to sarah@/james@/staci@tahi.studio), none of Giant Group's own data is in the log yet, consistent with \"tonight prepares, does not invite.\"\n\n## Data note surfaced while tracing the invoice email (see BILLING-1)\nget_client shows Giant Group's `invoiceChannel` is NULL (not 'xero'), `effectiveInvoiceChannel` resolves to 'stripe', and `stripeCustomerId` is null. This was found purely by following the invoice-sent email's code path per the brief's own instruction to check that event; flagged as BILLING-1 because it directly determines whether the invoice email contains a working pay path.\n\nNothing in this session modified any file or setting; all findings are read-only observations from source plus live MCP reads."
 },
 {
  "label": "",
  "findings": [
   {
    "id": "F1",
    "area": "Client billing truth (Overview, Services, Settings > Plan & billing)",
    "severity": "blocker",
    "claim": "Giant Group's own portal misreports what they pay and how much capacity they have, on every screen that shows it: it says \"NZ$4,000/mo . 1 track\" everywhere, when the real, negotiated contract is GBP 2,000/mo with 2 tracks (1 small + 1 large). A client who checks their bill against this screen will think they are being overcharged relative to what they see (NZ$4,000 looks like ~GBP 1,750, not their real GBP 2,000) and will not be able to see or use their second (large) track at all through the 'Your work in motion' widget on Overview.",
    "evidence": "Live, light mode: C:\\Users\\Work\\AppData\\Local\\Temp\\claude-chrome-screenshots-amOyVs\\screenshot-1789209635373-6.jpg (Overview 'Your plan': 'Scale Active, NZ$4,000/mo . 1 track'). Live, dark mode: screenshot-1789210145711-69.jpg (same card, dark) and screenshot-1789210263096-74.jpg (Settings > Plan & billing: 'Scale Active NZ$4,000/mo', 'Extra tracks 0', 'Scale base NZ$4,000/mo', 'Total NZ$4,000/mo'). Services page accessibility dump: 'Rate / NZ$4,000 per month', 'Tracks running / 1 track'. Ground truth via mcp__claude_ai_Tahi_Dashboard__get_client (aa80a2d6-0494-424d-8ccc-9af524c31fa7): organisations.customMrr=2000, preferredCurrency='GBP', customSmallTracks=1, customLargeTracks=1 (tracks array = 1 small real row + 1 synthetic large row); admin's own client-detail page correctly shows 'MRR GBP 2,000 = NZ$4,585' and '0 of 2 tracks busy'. Code: app/api/portal/subscription/route.ts line 70 `const monthlyRate = catalogPlan?.monthlyRate ?? PLAN_MONTHLY_RATES[sub.planType] ?? 0` (lib/billing.ts line 33: `scale: 4000`) never reads organisations.customMrr or preferredCurrency; lines 96-101 compute `trackCount = trackRows.length` from a raw `SELECT ... FROM tracks WHERE subscriptionId=...`, ignoring organisations.tracksMode/customSmallTracks/customLargeTracks (the large track is synthetic, not a DB row, so this undercounts by exactly 1). components/tahi/overview/homes/client-home.tsx line 1618 (`money(subData.subscription.monthlyRate)`) and components/tahi/portal/services/portal-services.tsx line 393 and components/tahi/settings/sections/plan.tsx (baseRate/Money nzd=) all consume this same broken value; useOvFormat's `money()` (components/tahi/overview/ov-kit.tsx line 94) is explicitly documented to expect an NZD input for the admin's own FX-conversion preference, not a client's billed-currency rate. The 'Your work in motion' trackboard widget on Overview separately reads app/api/portal/tracks/route.ts (lines 44-47), which has the identical naive-row-count bug and is why only 1 of 2 tracks renders as a usable lane; the correct fix already exists and is used elsewhere in app/api/portal/capacity/route.ts (resolveTracksConfig/buildEffectiveTracks from lib/plan-utils.ts) but that endpoint is not the one wired to this widget.",
    "fix": "In app/api/portal/subscription/route.ts, join organisations (customMrr, preferredCurrency, tracksMode, customSmallTracks, customLargeTracks) alongside the subscription query; when customMrr is set, prefer it (with preferredCurrency) over PLAN_MONTHLY_RATES/catalog default, and add a `currency` field to the JSON response. Replace the naive `trackRows.length` count (lines 96-101) with `resolveTracksConfig()` + total of `smallTracks + largeTracks` from lib/plan-utils.ts (same helper already used correctly in app/api/portal/capacity/route.ts). Apply the identical track-count fix to app/api/portal/tracks/route.ts (lines 44-47) since that is what actually feeds the Overview 'Your work in motion' widget -- this also unlocks the client's second/large track lane, not just the label. Update the three UI consumers (client-home.tsx line 1618, portal-services.tsx line 393, settings/sections/plan.tsx line ~355) to format the now-returned `currency` via the client's own billed-currency formatter (the same `useDisplayCurrency().formatNative` pattern invoices already use correctly) instead of the admin-only NZD-assuming `useOvFormat().money()` helper.",
    "effortDays": 1.5,
    "needsLiam": false,
    "needsRealSession": false
   },
   {
    "id": "F2",
    "area": "Overview 'Next call' widget",
    "severity": "blocker",
    "claim": "Giant Group's portal home shows an unrelated internal calendar event ('N8N Content Engine', Tue 19:00, 60 min, no meeting link) as their upcoming call, ahead of their real recurring 'Giant group x Tahi' call two days later. A client checking their next call sees the wrong meeting and, if they tried to join it, would hit a dead end (no Google Meet link at all).",
    "evidence": "Live: screenshot-1789209635373-5.jpg / screenshot-1789210324484-77.jpg (dark) show 'Next call: N8N Content Engine, Tuesday 19:00 . 60 min, TBC'. Backend, mcp__claude_ai_Tahi_Dashboard__list_all_calls: row id 2663a77a-a8b8-478c-9542-d6588c3ea5ab, title 'N8N Content Engine', meetingType 'client', orgId 'aa80a2d6-0494-424d-8ccc-9af524c31fa7' (Giant Group), scheduledAt 2026-09-15T19:00:00+12:00, googleMeetUrl: null -- while the same query shows a correctly-classified, real weekly series 'Giant group x Tahi' (id dcf3a506...) at 2026-09-16 with a populated googleMeetUrl. mcp__claude_ai_Tahi_Dashboard__list_calls (the manually-created scheduled_calls table) returns an empty list, confirming this row only exists via the Google-Calendar-sync classifier, not a studio-created booking. Code: app/api/portal/calls/route.ts lines 106-126 query discovery_calls WHERE orgId = caller's own org AND status='scheduled', ordered by scheduledAt ascending -- a faithful, org-scoped query, so a real Mickey Day/Mark Ramsey login hits this identical row.",
    "fix": "Reclassify (null the orgId, or change meetingType away from 'client') the discovery_calls row id 2663a77a-a8b8-478c-9542-d6588c3ea5ab once Liam confirms it is unrelated to Giant Group, via the update_call MCP tool or an admin edit. Add a systemic safeguard to the Google-Calendar-to-org classifier: require at least one attendee whose email domain matches a real contact of the target org before auto-tagging meetingType:'client' with a real orgId, and/or have /api/portal/calls (or the Overview 'Next call' card) skip surfacing any row with meetingUrl===null as if it were an actionable upcoming call.",
    "effortDays": 0.5,
    "needsLiam": true,
    "needsRealSession": false
   },
   {
    "id": "F3",
    "area": "Messages nav / standalone /messages page",
    "severity": "major",
    "claim": "The standalone /messages page is fully live and reachable in Giant Group's sidebar, bottom tab bar and More sheet, contradicting today's stated rule that it should stay hidden for clients (the request thread being the only client channel) and the studio's own 2026-09-06 audit verdict ('Do not build the Messages page'), which also found this platform sends zero email for any message. A client who posts into the 'Tahi Studio -- your line to the studio' direct channel risks no reply if nobody on the Tahi side is watching in-app notifications.",
    "evidence": "Live: screenshot-1789210344465-82.jpg shows /messages rendering fully for Mickey Day at Giant Group, with a 'Tahi Studio / Your line to the studio' conversation and the 'TR-243 Create tasks for design + dev' request thread listed side by side. screenshot-1789210334302-81.jpg shows 'Messages' present in the mobile More sheet alongside Overview/Requests/Notifications/Files/Services/Invoices/Settings. docs/superpowers/audits/2026-09-06-next-surfaces-assessment.json verdict: 'Do not build the Messages page... this platform sends zero email for any message, any status change, or any client-submitted request.'",
    "fix": "This is a decision item for Liam: either (a) keep /messages live now that the conversations model (app/api/admin/conversations/route.ts, lib/org-channel.ts) is built, and prioritise wiring email delivery for messages so nothing sent through it goes silently unanswered, or (b) gate clients back off the route/nav (mirroring how Contracts/Proposals used to bounce clients to /requests) until that delivery gap is closed.",
    "effortDays": 0.25,
    "needsLiam": true,
    "needsRealSession": false
   },
   {
    "id": "F4",
    "area": "Invoice reference number (portal invoice detail)",
    "severity": "major",
    "claim": "The reference Giant Group sees on their September invoice ('E1162445') is a synthetic id, not the real Xero invoice number ('INV-0065') that a bank transfer or accounting reconciliation would expect, which could cause a payment to be hard to match on the studio's side.",
    "evidence": "Live: invoice detail page shows 'REFERENCE E1162445' (screenshot-1789209876197-40.jpg / -876198-41/42/43.jpg). mcp__claude_ai_Tahi_Dashboard__get_invoice for this invoice: number: null, notes: 'Imported from Xero: INV-0065'.",
    "fix": "Already scoped and awaiting Liam's approval per docs/superpowers/plans/2026-09-13-mvp-100-run.md item 6: run POST /api/admin/invoices/backfill-numbers with {\"dryRun\":false} as Liam from the dashboard (or say 'go' so the run does it with a fresh D1 export first). No new engineering needed.",
    "effortDays": 0,
    "needsLiam": true,
    "needsRealSession": false
   },
   {
    "id": "F5",
    "area": "Client Services page catalogue",
    "severity": "major",
    "claim": "Giant Group's Services page ('What we take on') is empty -- 'Nothing published here yet' -- so they cannot see the plan ladder (what they have, lower/higher plans, suggestions) that the MVP plan calls for. Honest empty state, not a lie, but a page they will visit and find hollow.",
    "evidence": "Live accessibility dump on /services: heading 'Nothing published here yet', body 'We are still writing this up. In the meantime, ask us anything and we will tell you straight whether we do it.' Screenshots: screenshot-1789209947242-53.jpg / -960263-61.jpg / -62.jpg.",
    "fix": "Matches Batch B2 already named in docs/superpowers/plans/2026-09-13-mvp-100-run.md: port the plan ladder from the staged design (.claude/qa/svclocal, svc-ladder-patch.cjs) into the studio's service catalogue rows that mcp__claude_ai_Tahi_Dashboard__list_services reads, with per-plan suggestions and upsell only near limits.",
    "effortDays": 2,
    "needsLiam": false,
    "needsRealSession": false
   }
  ],
  "verified_ok": [
   "View-as-client read-only preview correctly blocks every write: Pay/Submit/Upload controls render disabled with an explicit 'Read only while viewing as a client' / 'Replies are read-only in client view' marker rather than silently doing nothing.",
   "New request dialog opens cleanly (category picker, title field, rich-text description, 'We will confirm where it sits in your queue'), and Cancel closes it without leaving any draft or console error.",
   "Request detail page (#243 Create tasks for design + dev) renders the correct delivery stepper, thread, files and team card for Giant Group; no leaked internal-only requests appeared on the kanban board (only the one real request shows).",
   "Files page shows an honest, correctly-worded empty state with an Upload CTA -- not the hardcoded stub the March/August audits found.",
   "Invoices list and detail show the correct currency and amounts throughout (GBP 4,225 still to pay, GBP 12,360 paid in 2026, 3 invoices settled at GBP 4,120 each) -- unlike the plan-rate cards, this surface reads real per-org data correctly.",
   "Invoice line items ('Twenty cloud test server', 'Extra figma seats') are real, Xero-sourced charges (INV-0065) that sum exactly to the invoice total -- confirmed via get_invoice, not a data-quality bug.",
   "Invoice Pay button correctly falls back to Xero's own hosted pay page (xeroOnlineInvoiceUrl) when there is no Stripe-hosted invoice, per lib/invoice-how-to-pay.ts resolveInvoicePayUrl -- confirmed in code, so the effectiveInvoiceChannel='stripe' default does not strand a Xero-rail client.",
   "Settings > Profile, Organization, People and Brand sections all correctly scope to Giant Group / Mickey Day (contact name, email, org name, and both contacts still shown as 'Pending invite', matching the closed-allowlist decision).",
   "Dark mode toggles cleanly across Overview, Requests, Invoices and Settings with no obvious contrast regressions at desktop width, and was restored to light mode at the end of the walk.",
   "Partial mobile captures (~540-740px viewport) of Overview and Request Detail showed no horizontal scroll, a working bottom tab bar, and a functioning 'More' sheet listing every client nav item (Overview, Requests, Notifications, Messages, Files, Services, Invoices, Settings)."
  ],
  "notes": "Walk performed as Liam via 'View as client' (Mickey Day at Giant Group), read-only, per the strict rules given. No form was submitted, no Pay/Upload/Invite/Delete control was clicked, and the dialog opened for the New request test was cancelled without submitting. Cross-referenced everything seen in the browser against the live database/backend through the read-only Tahi_Dashboard MCP tools (get_client, list_invoices, get_invoice, list_all_calls, get_settings) rather than trusting the UI alone, and against the actual source in the repo (grep/read) to root-cause the top two findings precisely rather than guess.\n\nTwo things I deliberately did NOT report as findings because I could trace them to intentional, documented preview-mode behaviour that will not reproduce for a real client login: (1) the Overview greeting read 'Kia ora, Liam.' instead of the previewed contact's name, and (2) the Notifications page showed Liam's own studio inbox (stale CRM leads, Xero-vs-Airwallex bank-drift figures in three currencies, cron failure alerts, another client's invoice notification) rather than anything scoped to Giant Group. Both are explained in the code itself: app/(dashboard)/overview/page.tsx resolves the greeting name from the real signed-in Clerk user regardless of the impersonation cookie, and app/(dashboard)/notifications/page.tsx has an explicit comment that notification audience deliberately follows the real Tahi-org membership rather than the impersonation cookie, 'because clearing a real bell from a preview would be a surprise.' A real Mickey Day/Mark Ramsey login would resolve their own name and their own (empty, since neither has ever signed in) notification history. I flag this only as a methodology note: these two specific surfaces cannot be verified through the 'View as client' preview tool, and a real client session would be needed to give them a clean pass (not because I have any evidence of a problem, but because the preview genuinely cannot show them).\n\nMobile-width (375px) testing was only partially reliable in this session: resize_window occasionally produced a clean narrow viewport (which I captured and found no responsive problems in) but repeatedly snapped back to a desktop-width render on the next screenshot, and once produced a tiled/duplicated render that was clearly a screenshot-tooling artifact rather than a real bug. I did not chase this further given the strong signal already gathered at the two narrow widths that did render cleanly (Overview and Request Detail, both dark mode, ~540-740px, no horizontal scroll, correct bottom-tab and More-sheet behaviour). A dedicated mobile-device-toolbar pass on Requests list, Invoices and Services at a locked 375px would be worth a short follow-up before the invite goes out, purely to fully close out the CLAUDE.md mobile checklist -- I have no evidence of a problem there, just incomplete coverage.\n\nAll money/rate/track findings (F1) were verified end-to-end from the database value through the exact line of code to the exact rendered string, across three separate pages (Overview, Services, Settings > Plan & billing) and both light and dark mode, so I am highly confident this is one root cause with three visible symptoms, not three separate bugs."
 }
]