{
 "results": [
  "Account page rebuilt on the segmented slider with the settings-page card look. Verified at 1280/1400 and 375, light and dark.\n\n**What changed**\n\n`portal-account-kit.jsx` \u2014 `Seg` is now the measured sliding pill the Requests/Tasks rail uses (`useLayoutEffect` + `ResizeObserver` + `document.fonts.ready`, pill positioned from the live button's `offsetLeft/offsetTop/offsetWidth/offsetHeight`). Because it carries a top offset, a wrapped two-row track works with no extra code. Added `wrap`, `icon`, `tone`, `idPrefix` props, roving `tabIndex`, and Arrow/Home/End keyboard nav. `CardHead` was re-cut as the settings ledger bar (uppercase label + count chip + right-hand action, tinted, hairline bottom); the old icon tile is gone.\n\n`portal-account.jsx` \u2014 `Account` is four rooms behind one slider instead of five cards down a scroll. The anchor nav and its scroll-spy are deleted. Tabs are Liam's names (You / Sign-in / What reaches you / Your team); the heading under each is the fuller one so the two never repeat inside sixty pixels (`Your profile`, `Sign-in and security`, `Notifications and email`, `Your team at Mahana Orchards`). Sign out moved into Sign-in as its own card. Each room takes one accent, worn only by the section glyph and by its tab icon while selected: green / blue / amber / purple. Full `role=\"tablist\"`/`tabpanel\"` wiring with `aria-labelledby`. New optional `section` prop for deep links and previews; `ctx`, `state`, `seat`, `openPanel` unchanged, so the shell wiring is untouched. Content and the Ana Rewiri / Mahana Orchards data spine are intact.\n\n`portal-account.css` \u2014 card is now the settings card (`var(--bg)`, one hairline border, `.875rem` radius, `var(--shadow)`, `overflow:hidden`). Rows inside it are hairline-separated edge to edge instead of each row carrying its own bordered box: `.pa-row`, `.pa-pref`, `.pa-prow` all lost their borders and radii. Added `.pa-cardbody` (settings field rhythm), tinted `.pa-cardhead`/`.pa-cardfoot`/`.pa-legend`, `.pa-tabs`, `.pa-sec` + accents, `.pa-sech`. Removed `.pa-anchors`/`.pa-anchor`. Tab glyphs are hidden under 30rem so two tabs a row still fit at 375.\n\n**Fixed after looking at the shots**\n- Dark mode: `--brand` lifts to a light green, so white on the primary button was about 2.6:1. Label now takes forest ink (`#14210E`, ~6.6:1) in dark only, declared before the disabled rules so a disabled primary keeps its own muted colour.\n- A disabled primary (`Save changes`, `Send invite`) took `--bg-secondary`, which is exactly the tinted card foot it now sits on, so it vanished. It takes `var(--bg)` plus a border instead.\n- Section lede was capped at 44ch and orphaned \"portal.\" onto its own line; now 56ch with `text-wrap:pretty`.\n\n**Verified**\n375 viewport, all four rooms plus dark and the invite sheet: `padOverflow 0`, `scrollerOverflow 0`, `segOverflow 0`, document overflow 0, zero controls under 44px, seg height 96px (two rows). Pill lands within 0.5px of the selected tab on every room at both widths, including after a click that moves it to the second row. Keyboard: two ArrowLefts from Your team lands on Sign-in with focus following. No console or page errors.\n\n**Screenshots** (all under `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\acct\\`)\n- 1400: `d2-00-you.png`, `d2-01-sign-in.png`, `d2-02-what-reaches-you.png`, `d2-03-your-team.png`\n- 1400 dark: `d2-04-you-dark.png` \u2026 `d2-07-your-team-dark.png`\n- states: `d2-08-member-seat-no-invite.png`, `d2-09-invite-panel.png`, `d2-10-change-email-panel.png`, `d2-11-change-password-panel.png`, `d2-12-loading.png`, `d2-13-read-only-client-view.png`\n- 375: `m2-00-375-you.png` \u2026 `m2-03-375-your-team.png`, `m2-04-375-dark-you.png`, `m2-05-375-dark-your-team.png`, `m2-06-375-invite-sheet.png`\n\n**Heads up: another agent is writing the same three files.** Mid-run, `portal-account.jsx` and `portal-account.css` were rewritten by a concurrent writer rebuilding Notifications onto a left rail (`pa-nrail`, `pa-ritem`, `pa-sheet`, Views/Kinds groups). My first JSX write was refused on the etag; I re-based and kept their Notifications byte-for-byte rather than clobbering it, and they re-based their CSS onto my Account block. Current state is a clean merge of both, and they have since adopted my new `Seg wrap` for their phone view track. If more Account edits land, re-read before writing.\n\n**Left open**\n- I did not touch `portal-account-preview.html` (the other agent's Notifications frames live there and a write race was likely). The Account review harness is a new file, `portal-account-sections.html`, with a `?m=1` mode that strips the gallery chrome so a 375 viewport really is 375 wide \u2014 the old harness measures 375 frames inside a desk viewport, so its media queries lie. The integrator should archive both.\n- The `Invite a colleague` primary goes full width in the card head at 375, which puts leaf corners on a very wide button. It reads fine but is worth a look.\n- Person rows take three lines at 375 (name, email, then level pill and menu). That matches what settings.css does on mobile, so I left it.\n- Scripts used: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\acct-shot.mjs` (screenshots plus an overflow / pill-alignment / 44px probe) and `.claude\\acct-interact.mjs` (click-through and keyboard nav).",
  "Done. Report:\n\n**WHAT CHANGED \u2014 Notifications page, Claude Design project 57bf60cf (Tahi dashboard)**\n\nFiles I own and edited: `portal-account.jsx`, `portal-account.css`, plus `portal-account-preview.html` (notification frames only, so the rail gets a real desk width to be reviewed at). `portal-account-kit.jsx` and `portal-account-data.jsx` were not changed.\n\n**The rebuild**\n\nThe page now uses the Requests/Tasks RailLayout shape instead of a toolbar plus a scrolling chip row:\n\n- Header band (`.pa-nhead`) holds still. Below it `.pa-ncrow` splits into `.pa-nrail` (13.75rem, own scroll box) and `.pa-nmain` (own scroll box). Only the feed scrolls.\n- Rail, top to bottom: **Views** (All 12 / Unread 3 / Past 5), **Kinds** (Requests 5, Replies 2, Invoices 2, Studio notes 2, Documents 1 for a client; seven for the studio), then a foot with **Clear filters** (only when a kind is on) and **Email preferences** as a quiet settings link pinned to the bottom.\n- Main column: a single quiet line, \"12 notifications \u00b7 3 unread\", with **Mark all as read** on the right, then the day-grouped list with sticky day headers, then Load older / the endcap.\n- Both audiences. The kinds list and the feed both swap with `ctx.audience`, unchanged from before.\n\n**Email preferences moved.** It was a lone bordered button sitting above the list in the page header. It is now the quiet link in the rail's foot on a desk, and a settings row at the end of the feed on a phone (`.pa-nprefs`, icon + \"Choose what lands in your inbox and what stays in the bell.\" + arrow). It is not in the header on either.\n\n**One decision worth flagging.** My first pass had three groups: Views (All/Past), Read state (Everything/Unread only), Kinds (All kinds + each kind). That lit three brand-tinted rows at rest and read loud. Unread is now a **third view** rather than a second lens, and the two \"reset\" rows are gone (Clear filters in the foot does that job). At rest exactly one row is lit. Selecting Past drops the read-state question entirely, which it should, because Past holds nothing unread.\n\n**Phone (375).** The rail folds: the three views become the segmented track on one line, and the kinds sit behind a **Filters** button with a count badge, opening a bottom sheet that reuses the same rail rows at 2.75rem and closes on \"Show 12\". Mark all as read becomes a bordered icon button next to Filters so it does not float as a bare glyph. Verified at a real 375-wide viewport: no sub-40px targets, no horizontal overflow, no console errors.\n\n**States checked** (light and dark, 1280 and 375): populated, Past grouped by month with kind counts recomputed and zero-count kinds dimmed and disabled, Unread, Unread + a kind filter, \"Nothing unread.\" with a \"Show everything\" action after Mark all, \"Nothing matches those kinds.\", loading (rail present, counts withheld until a read lands), could-not-load, empty, and read-only client view (banner spans the header band, every write control dimmed).\n\n**Screenshots** (all absolute, on this machine):\n- `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\ntf-desk-00-all-views-read-state-and-kinds-in.png` \u2014 client, All, light\n- `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\ntf-desk-01-past-grouped-by-month-the-read-sta.png` \u2014 Past\n- `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\ntf-desk-06-owner-audience-light-seven-kinds-i.png` \u2014 studio, light\n- `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\ntf-desk-07-owner-audience-dark.png` \u2014 studio, dark\n- `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\ntf-desk-unread-invoices.png`, `ntf-desk-unread-empty.png`, `ntf-desk-02-loading.png`, `ntf-desk-05-read-only-viewing-as-the-client.png`\n- `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\shots\\ntf-phone-24-notifications.png`, `ntf-phone-25-notifications-dark.png`, `ntf-phone-sheet.png`, `ntf-phone-30-notifications-past-tab.png`\n\nShot scripts: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\notif-shot.mjs` (frames + target/overflow audit, `desk|phone`) and `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\notif-states.mjs` (drives the rail through Unread, a kind, and Mark all).\n\n**Coordination notes for whoever picks this up**\n\n- The Account designer rewrote `portal-account.css` and `portal-account.jsx` twice while I worked. I hit two write conflicts and re-based both times; their Account work (settings-shaped cards, `.pa-sec` accents, the measured `Seg`, the dark primary-button contrast fix) is intact, and my Notifications block survived their last write. Anyone writing these files next should re-read immediately before writing.\n- One deliberate seam: the notifications Seg is rendered with `wrap:true` (the Account tabs need it) and `.pa-nmob .pa-seg.wrap` overrides it back to one nowrap line, because three one-word views fit at 375 where four sentence-length tab labels do not. The CSS comment says so. If someone removes one half, the phone track breaks to two rows.\n- `.pa-bar`, `.pa-bar-sp`, `.pa-chips` and `.pa-chips-sp` were the old toolbar and chip-row classes; I removed them. `.pa-chip*` and `.pa-clear` are kept because the kit still exports `Chip`.",
  "WIRED. All three areas are now reachable in the app shell, previews archived, shell renders clean.\n\n**Files changed (Claude Design project 57bf60cf-5e6d-450f-9e2f-e25c8d12fd66)**\n\n1. `Tahi App Shell.html` \u2014 added 3 stylesheets after `portal-account.css` (`sales-pipeline.css`, `sales-artifacts.css`, `ops.css`) and 9 module scripts before `app-mount.jsx`. Load order is a hard constraint I found and commented in the file: `sales-pipeline-*` and `sales-artifacts-*` BOTH publish `window.TahiSalesData` and `window.TahiSalesKit` and capture them once at load, so each trio must load as an uninterrupted block (data, kit, pages) or one area gets the other's dataset. Order: pipeline trio, artifacts trio, ops trio, then `app-mount.jsx`.\n\n2. `app-shell.jsx` \u2014 12 additive edits, nothing removed:\n   - TEAM_NAV Sales: `{ id: 'artifact-templates', label: 'Templates', icon: 'docs' }` after Contracts. Leads/Calls/Deals/Proposals/Schedules/Contracts already existed.\n   - TEAM_NAV Operations: `{ id: 'tracks', label: 'Tracks', icon: 'sitemap' }`. Time stayed in Finance (2 of the 3 ops wiring notes said leave it; the third wanted it moved, which is a design call not a wiring one).\n   - CLIENT_NAV: `Your tracks` + `Schedule` in \"Your project\", `Proposals` + `Contracts` in \"Billing\". The comment above CLIENT_NAV said those three were removed because they fell through to studio pages; they now resolve to `ClientPapers`, so I updated that comment rather than leaving it stale.\n   - `App()` signature: 15 new render props (leads/calls/deals, proposals/contracts/schedules/artifactTemplates, the 3 viewers, time/team/capacity/tracks/portalTracks).\n   - Page registry: 14 new branches after `clientsContent`, with `effectiveAudience === 'client' && active === 'tracks'` before the studio Tracks branch so a client can never land on the studio board. None wrapped in `page-inner` (each module draws its own full-height frame, same as requests/tasks).\n   - New `VIEWER_IDS` set + 3 branches in the `bare` chain, so the public viewers render outside `.ash` on their own `.sa-tokens` root the way a token URL does.\n   - `viewerBack` ref + one line in `go()` so the bare stage has a Back that returns you where you came from. I gave the bare stage a shell-owned exit bar reusing the existing `.imp-banner` classes (no new CSS, no edits to the designers' modules) because the viewers take only `{ctx, demo}` and had no way out.\n   - `+ (priv ? ' priv' : '')` on the shell root: `sales-pipeline.css` ships `.ash.priv .sp [data-private]` and 40 `data-private` marks, but the class was never applied, so Private mode was inert.\n\n3. `app-mount.jsx` \u2014 registers all three areas. Pipeline gets a `go` adapter (pages navigate by page key, shell by nav id). Artifacts get an `Area` component doing list\u2192builder routing, with `key: kind` on each `createElement` (without it React reuses the instance across proposals/contracts/schedules and the open builder leaks between them) and a module-level `openBy` map so the builder you had open is still open after a viewer round trip. Client audience gets `ClientPapers`.\n\n**Files moved** (copy_files then delete_files, sizes verified intact) into `archive/previews-2026-09-06/`: `ops-preview.html`, `portal-account-preview.html`, `portal-files-preview.html`, `portal-home-preview.html`, `portal-money-preview.html`, `sales-artifacts-preview.html`, `sales-pipeline-preview.html`. Root now holds only shell pages and modules. I left `portal-account-sections.html` alone: it is not a `*-preview.html` and two designers touched `portal-account.*` minutes before me. Flagging it for you.\n\n**Verification** (headless Chromium via `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\wire\\walk.mjs`, `probe.mjs`, `regress.mjs`)\n- Zero console errors and zero page errors across every pass.\n- Studio at 1280 and in the 390 device frame: Leads, Calls, Deals, Proposals, Schedules, Contracts, Templates, Time, Capacity, Tracks, Team all render their real module, none falls through to the `PageBody` placeholder.\n- Client audience: Your tracks renders `TracksClient`; Schedule / Proposals / Contracts render `ClientPapers`.\n- Route round trip confirmed for all three artifacts: list \u2192 builder \u2192 Preview \u2192 bare viewer with the Back bar \u2192 back lands on the builder still open. Client path confirmed too: Contracts \u2192 \"Read it\" \u2192 viewer \u2192 Back \u2192 Contracts.\n- Templates reachable from the Templates button on each list.\n- Dark mode checked on Deals, Time, Contracts: tokens resolve, no contrast breakage.\n- Regression pass: Overview, Requests, Tasks, Messages, Notifications, Clients, Files still mount for studio; Overview, Requests, Messages, Files, Services, Invoices, Account still mount for client. Docs Hub is still a placeholder, as it was before (no module exists).\n\n**Screenshots** (40 PNGs) in `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\wire\\shots\\` \u2014 `1280-*.png` (11 studio pages), `375-*.png` (same 11 in the device frame), `client-*.png`, `edit-*.png`, `viewer-*.png`, `dark-*.png`, `templates-from-list.png`, `regress-client-account.png`.\n\n**Two things I did not touch, both design not wiring**\n- `ClientPapers` hardcodes `orgId = 'kowtow'` while the shell's signed-in client is Ana Rewiri at Mahana Orchards, so the client Proposals/Contracts/Schedule pages show another company's paperwork. Needs the designer to read the org off ctx.\n- The three viewers pass no `theme` to `DocFrame`, so they always render light even when the shell is dark. Deliberate for a public document, but worth a decision.",
  "All four passes are done. Writing up.\n\n**QA WALK \u2014 Tahi App Shell, Claude Design project 57bf60cf-5e6d-450f-9e2f-e25c8d12fd66**\nPlaywright/Chromium headless. 20 surfaces x 4 combinations (1280 and 375, light and dark) = 80 screenshots.\nScripts: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\qa\\qa.mjs` (walk), `measure.mjs`, `probe-cap*.mjs`, `probe-read2.mjs`, `crop.mjs`\nScreenshots: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\qa\\shots\\<width>-<theme>-<key>.png`\nMetrics: `C:\\Users\\Work\\Projects\\tahi-dashboard\\.claude\\qa\\report-<width>-<theme>.json`\n\n**READ THIS BEFORE TRUSTING ANY EARLIER MOBILE SHOT \u2014 two harness artifacts, not design bugs**\n\n1. The shell's mobile mode always draws a hard-coded 390x844 device frame (`.ash-stage.framed .ash-device` in `app-shell.css`). Shoot it in a 375x812 window and the frame is clipped 7.5px each side and 16px top and bottom, which cuts the bottom tab-bar labels in half. That is the harness, not the tab bar. My run overrides the frame to 375x812 in a 520x980 window and clips each shot to the frame, so these are true 375 renders.\n2. The Claude Design preview host wraps static JSX text in `<span class=\"__om-t\">` that computes to `display:block`. Inside any flex container with a gap each one becomes its own flex item and injects a phantom space: \"Back to proposal s\", \"9  slides\", \"38h billable\" with an overlapping white disc on Capacity. All fake. `.__om-t{display:contents}` plus suppressing `[data-om-id]::before/::after` removes them; my shots have that injected. Verified: `.sa-back.top` textContent is exactly \"Back to proposals\".\n\n**ZERO console errors and zero page errors across all four passes. Zero document-level horizontal overflow anywhere. No page fell through to the PageBody placeholder. Every surface is reachable from the sidebar at 1280 and from the More sheet at 375.**\n\n---\n\nSTUDIO\n\n**Leads \u2014 FIX**\nRenders, reachable, dark clean.\n- 1280: table scrollWidth 1108 in a 992 box, so the Touched column is cut mid-value (\"8m ag\", header reads \"TOUC\"). It is `overflow-x:auto` but there is no scrollbar or edge fade, so it reads as broken. Fits from 1440 up. Either drop `--min:62rem` or collapse Touched under 1400.\n- 375: the three selects render as \"An\", \"An\", \"La\". Unreadable.\n`shots\\1280-light-leads.png`, `shots\\375-light-leads.png`, `crops\\leads-right.png`\n\n**A lead (Aroha Tipene) \u2014 SHIP**\n- 375: the overflow \"...\" wraps onto its own row, alone and left-aligned, under the Promote button.\n`shots\\1280-light-lead-detail.png`, `shots\\375-light-lead-detail.png`\n\n**Calls \u2014 SHIP**\n- 1280: the Starting soon hero leaves about 100px of dead space under Join/Open call because the digest panel on the right is taller. Bottom-align or let the digest set the height.\n`shots\\1280-light-calls.png`, `shots\\375-light-calls.png`\n\n**A call (Discovery, Mahana Orchards) \u2014 SHIP**\n- Save call notes reads muted enough to look disabled while the form is empty.\n`shots\\1280-light-call-detail.png`, `shots\\375-light-call-detail.png`\n\n**Deals \u2014 FIX**\n- 1280: Negotiation is sliced in half at the right edge with no scroll affordance, cards clipped mid-word (\"Hnry, campaign page sy\"). Needs a right-edge fade or a peek column.\n- 375: the stat band's 6th grid slot is painted as a filled grey rectangle. Reads as a broken card. Also defaults to Board on a phone, which is the worse of the two views.\n`shots\\1280-light-deals.png`, `shots\\375-light-deals.png`\n\n**A deal (Mahana Orchards, season build) \u2014 FIX**\n- Both widths: the stage chip row wraps and leaves \"Stalled 4%\" orphaned with a dangling connector dash between rows.\n- 375: \"Stage / Odds come from how these stages have actually converted\" is squeezed into a 5-line ribbon by the Won/Lost buttons. Stack the card header under 30rem.\n- Taste: \"New proposal\" is the top-right primary and also the first of three buttons in the Sales kit card.\n`shots\\1280-light-deal-detail.png`, `shots\\375-dark-deal-detail.png`\n\n**Proposals \u2014 FIX**\n- 1280: the Physitrack row's secondary \"accepted Accessibility retainer\" is hard-clipped mid-word and runs into the Status column. No ellipsis.\n- Both widths: `.sa-read` is `display:flex;gap:6px` wrapping bare text nodes, so every Reading cell renders \"14 views , 4 people\" with a space before the comma and a wide one after the count. Real, survives the artifact fix.\n- Taste: five identical stat tiles in a row, with \"no figure\" and \"nothing here\" as filler subtitles under the zeros.\n`shots\\1280-light-proposals.png`, `crops\\prop-row.png`, `shots\\375-light-proposals.png`\n\n**A proposal (editor) \u2014 FIX**\n- 375: the document title is cut at the frame edge with no ellipsis (\"Mahana Orchards ecommerce replatfor\").\n- 1280 is the strongest editor of the three: slide rail, package rail, live \"How it reads\" preview.\n`shots\\1280-light-proposal-editor.png`, `shots\\375-light-proposal-editor.png`\n\n**Contracts \u2014 FIX**\n- Terminal states still chase someone. Northwind is Cancelled and shows \"0 of 1, waiting on Unknown\" with an avatar. Physitrack is Expired and shows \"waiting on Jonty\". Cause is `sales-artifacts.jsx:633`: `'waiting on ' + D.person((signers.filter(pending)[0] || {}).personId).name` with no guard for cancelled/expired or an empty pending list.\n`shots\\1280-light-contracts.png`, `shots\\375-light-contracts.png`\n\n**A contract (editor) \u2014 FIX**\n- 375: title clipped, same as the proposal editor (\"...statement of wor\").\n- 375: the Signers intro paragraph keeps a desktop measure and wraps at roughly 26 characters inside a 340px card, leaving a wide right gutter.\n- 1280: the left column holds one card then about 350px of nothing while the right rail stacks four.\n`shots\\1280-light-contract-editor.png`, `shots\\375-light-contract-editor.png`\n\n**Schedules \u2014 SHIP**\n- 375: the off-track banner's \"Open the schedule\" becomes centred plain text with no button affordance. Same on the Contracts banner.\n`shots\\1280-light-schedules.png`, `shots\\375-light-schedules.png`\n\n**A schedule (editor, gantt) \u2014 FIX**\n- 1280: bar labels truncate to nothing. \"Creative \u2026\", \"M\u2026\", \"Copy an\u2026\". 14 weeks in a 400px track. Put the label outside the bar or reveal on hover.\n- 1280: the header says \"12 rows across 14 weeks\" but only 12 week columns are visible before the clip.\n- 375: the gantt needs a 630px scroller inside a vertically scrolling page, so it is two-finger panning on a phone. Worth a decision rather than a fix.\n`shots\\1280-light-schedule-editor.png`, `shots\\1280-dark-schedule-editor.png`, `shots\\375-light-schedule-editor.png`\n\n**Templates \u2014 SHIP**\nClean at both widths and themes, reachable from the Templates button on each list and from the sidebar. The \"Back to proposal s\" everyone will see in older shots is the host artifact, not a copy bug.\n- Taste: four identical cards in a 3-up grid leaves the fourth alone on row two and about 280px of empty page below.\n`shots\\1280-light-templates.png`, `shots\\375-light-templates.png`\n\n**Time \u2014 SHIP**\nBest page in the set. The running timer, the unpriced warning, the week grid and the no-rate red zero all survive 375 and dark intact.\n`shots\\1280-light-time.png`, `shots\\1280-dark-time.png`, `shots\\375-dark-time.png`\n\n**Team \u2014 SHIP**\n`shots\\1280-light-team.png`, `shots\\375-light-team.png`\n\n**Capacity \u2014 FIX**\n- 375: \"This week, person by person\" plus its description is crushed into a 5-line ribbon by the legend sitting on the same row. Stack the legend under the heading below 30rem.\n- The overlapping white discs on the per-person legend in any earlier screenshot are the host artifact. Verified clean in the DOM and in `crops\\cap-b.png`.\n`shots\\1280-light-capacity.png`, `shots\\375-light-capacity.png`, `crops\\cap-legend.png` (artifact), `crops\\cap-b.png` (clean)\n\n**Tracks (studio) \u2014 SHIP**\n`shots\\1280-light-tracks.png`, `shots\\375-light-tracks.png`\n\n---\n\nCLIENT\n\n**Account \u2014 SHIP**\nThe segmented slider, the four rooms, the accent-per-room and the Ana Rewiri / Mahana Orchards spine all hold at 375 and in dark. The 2x2 wrapped tab track at 375 is the best mobile detail in the build.\n- 1280: one 845px card in a 1040px column with 300px of empty page under it. Reads like a phone layout stretched, not a dense desk page.\n- Disabled \"Save changes\" is #9A988F on white (about 2.6:1) light and #6B6A63 on #1C1B19 (about 3.2:1) dark. Exempt under WCAG but it is the resting state of the page.\n`shots\\1280-light-c-account.png`, `shots\\1280-dark-c-account.png`, `shots\\375-light-c-account.png`\n\n**Notifications \u2014 SHIP**\nRail plus feed at 1280, segmented views plus a Filters sheet at 375, both themes clean. Email preferences correctly moved out of the header.\n- 1280: about 380px of void in the rail between \"Documents 1\" and the pinned Email preferences.\n- Unread is signalled twice, by a dot on the icon tile and by the check button.\n`shots\\1280-light-c-notifications.png`, `shots\\1280-dark-c-notifications.png`, `shots\\375-dark-c-notifications.png`\n\n**Your tracks \u2014 FIX (data identity, same class as ClientPapers)**\nRenders and is reachable, and the shell correctly routes a client to `TracksClient` rather than the studio board. But `ops.jsx:802` hardcodes `D.client('physitrack')` and `D.tracksFor('physitrack')` while the signed-in client is Ana Rewiri at Mahana Orchards. The page shows Physitrack's queue (TR-1015, TR-1051, TR-1042, TR-1033, TR-1029, TR-1021, byte for byte the same rows as the studio Tracks page's Physitrack card) and Physitrack's plan label under Mahana Orchards' identity. Needs to read the org off `ctx`, exactly like the `orgId = 'kowtow'` note the integrator left on ClientPapers.\n`shots\\1280-light-c-tracks.png` next to `shots\\1280-light-tracks.png`, `shots\\375-light-c-tracks.png`\n\n---\n\nCROSS-CUTTING FOR LIAM\n\n1. Two client-facing pages ship another company's data (`ClientPapers` = kowtow, `TracksClient` = physitrack). One fix pattern covers both.\n2. Every artifact list opens with five or six identical stat tiles. Proposals, Contracts, Leads, Deals, Time, Team, Capacity, Tracks all do it. That is the same-same card wall, and it is the single loudest thing separating these pages from the v3 bento direction.\n3. 1280 with the rail expanded is the width that breaks things: the Leads table, the Deals board and the schedule gantt all clip without a scroll affordance. Everything is fine from 1440. Worth deciding whether 1280-with-rail is a supported width.\n4. Terminal states keep asking for action: cancelled contracts chase \"Unknown\", expired ones chase a signer, and Deals defaults to a board on a phone.\n5. Touch targets at 375 all clear 44px. The only sub-40px hits are the search input's inner box and the currency pill in the mobile top bar.\n6. Dark mode holds on all twenty surfaces. Tokens resolve, no contrast breakage beyond the disabled primary noted above."
 ]
}